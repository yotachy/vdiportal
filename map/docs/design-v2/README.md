# 시안 2차 — 인테이크 지점

**이 폴더가 `시안2.zip` 이 도착할 자리다.** 세션이 끊겨도 이 문서만 읽으면 이어서 할 수 있다.
최종 갱신 2026-08-19.

---

## 0. 지금 어디까지 왔나

디자인 시안 작업에 들어가기 직전이고, **시안이 참조할 문서 두 종이 완성·배포되어 있다.**

| 문서 | 버전 | 무엇 | URL |
|---|---|---|---|
| [`map/app-spec.html`](../../app-spec.html) | **v0.8** | 화면별 요건정의 — 18화면 · 스토리라인 4막 · 상태 카탈로그 18종 · 정직 표기 10항 · 전제 8개 | `https://parksvc.mycafe24.com/map/app-spec.html` |
| [`map/design-kit.html`](../../design-kit.html) | **v0.3** | 시안 작업 재료 — 디자인 원칙 · 연결도 · 컴포넌트 33종 · 표기 규격 · 어휘집 · 카피 덱 · 더미 데이터 · 세로 예산 · 접근성 실측 | `https://parksvc.mycafe24.com/map/design-kit.html` |

근거 문서(둘의 출처): `docs/2026-08-19-forge-information-architecture.md`(PC 정보구조 전수조사) ·
`docs/2026-08-19-chart-sharing-feasibility.md`(작도 공유 타당성) · `backtest/measure-sample-regimes.js`(샘플 국면 실측).

## 1. 확정된 것 (시안이 뒤집을 수 있음 — 전제 7)

- **별도 모바일 개발 없음.** 앱은 `forge.html` + 스크립트 6종을 그대로 실행하고 세로 배치만 같은 파일에서 만든다.
- **골격**: 스티키 차트 3단 접힘 + 하단 세그먼트 4칸(근거 · 시점별 · 지표 · 판독문).
- **보드 13종 전량**이 결과 콘텐츠(오실레이터 7 + 파동 2는 S17).
- **샘플 7종**: NVDA · AAPL · MSFT · GOOGL · AMZN · BTC/USD · ETH/USD.
- **경제**: 기본 0(무제한) / 심화 2 / 커스텀 3 · 개설 지급 15 · 광고 +3 · 출석 1/6h · 상한 30.
- **스토리 축**: 첫 90초 → 첫 15스쿱 → 다음 날 아침 채점(S16) → 7일째 성적표.
- **디자인 감각**: 개방감 기본, 밀도는 작도 영역·심오한 분석 콘텐츠만. 조작은 탭 기본, 롱프레스 금지.

## 2. 봉쇄 (⛔ 반드시 지킬 것)

**`map/mobile/` 의 `www`·`android`·`test`·`tools`·`sync-engine.mjs` 는 폐기 자산이다. 읽지도 복사하지도 않는다.**
`mobile/docs/` 만 예외(1차 시안 원본 `design_handoff/` · 실측 · 이력).
전문 [`mobile/DEPRECATED.md`](../../mobile/DEPRECATED.md) · 규율 `map/CLAUDE.md §④-3`.

## 3. 시안이 도착하면 — 인테이크 절차

```bash
cd ~/projects/vdiportal/map/docs/design-v2
unzip -O cp949 시안2.zip -d raw/        # 한글 파일명 대비. 안 되면 -O utf-8
find raw -type f | sort                  # 인벤토리
```

그 다음 순서대로:

1. **인벤토리** — 화면 몇 장인지, 형식이 무엇인지(HTML · 이미지 · Figma 내보내기 · PDF).
2. **대조표 작성** — 시안의 화면 ↔ `app-spec.html` 의 S0~S17. **어느 쪽에만 있는 것**을 양방향으로 뽑는다.
   - 시안에만 있음 → **새 기능 요건 후보**(전제 7 — 수용하고 문서를 고친다)
   - 요건에만 있음 → 시안 누락인지 의도적 제외인지 확인
3. **시각 규격 추출** — 여백 · 타이포 스케일 · 색 토큰 · 컴포넌트 치수. `design-kit.html` §2·§3 을 **시안의 실제 값으로 갱신**한다.
4. **세로 예산 재계산** — `design-kit.html` §7 은 추정치다. 시안의 실제 행 높이로 다시 계산해 갱신한다.
5. **접근성 재측정** — 시안이 새 팔레트를 주면 `design-kit.html` §8 의 대비비를 다시 잰다(현재 `--muted`·`--faint` 미달).
6. **충돌 판정** — 어긋나는 것이 나오면 **시안을 우선**하고 요건을 고친다.
   협상 불가는 셋뿐: **정직 표기 · 법적 고지 · 엔진 단일 원본**. 이 셋도 거절이 아니라 *의도를 지키며 불변을 유지할 방법*을 찾는다.
7. **문서 갱신 + 배포** — `app-spec.html` · `design-kit.html` 개정 이력에 한 줄, 커밋, cafe24 업로드, 아티팩트 재발행.

## 4. 열려 있는 미결 (시안이 답을 줄 수도 있는 것)

| # | 사안 |
|---|---|
| M1 | 앱 셸 — 기존 Capacitor 프로젝트의 `www` 교체 vs 신규 |
| M2 | 가로 전체화면(폰을 눕히면 차트만) |
| M3 | 리스크 도구 전용 고지 문구(법무) |
| M4 | 최적화(워크포워드 그리드) 폰 성능 실측 |
| M5 | 목록 스와이프 삭제 도입 여부 |
| M7 | PC 에서도 신호 스캔 제거할지 |
| — | S3 스티키 기본값을 L1 로 둘지 L2 로 둘지(L1 은 695px 로 한 화면 초과) |
| — | S5 가 4.4화면 — 오실레이터를 접을지 한 번 더 나눌지 |

## 5. 1차 시안은 참조하지 않는다

`map/mobile/docs/design_handoff/`(1차 시안)는 **읽지 않는다.** 사용자 지시 2026-08-19 — *“오히려 혼란만 줄 것”*.

- **1차와 2차를 나란히 두면 “어느 쪽을 따르나”가 생긴다.** 2차가 나오는 순간 그것이 유일한 시각 정본이다.
- 1차는 온보딩 7단계에만 실제로 반영됐고, 리포트 화면군은 아예 그 시안을 안 보고 지어져 실패했다.
  즉 **1차는 “구현과 대조할 기준”으로도 이미 무효**다.
- 그 폴더는 **이력 보관 목적으로만** 남는다. 비교표를 만들지 않는다.

**2차 시안이 이 프로젝트의 유일한 시각 정본이다.**
