# 패널 2 시각화 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. 체크박스(`- [ ]`) 단계.

**Goal:** 중앙 데이터 영역에 인라인 SVG 도넛·반원 게이지·원형 미니게이지·스파크라인을 추가해 "표 느낌"을 줄인다(수치 로직 불변).

**Architecture:** `forge-app.js`에 순수 SVG 헬퍼 4종 추가 → `renderVerdict`/`renderHorizons`/`fcDrawRsi·Cci·Williams·Mfi`에서 호출. `forge.css`에 viz 스타일. 색은 토큰(bull/bear/gold/eth/line).

**Tech Stack:** 바닐라 JS + 인라인 SVG. 검증 = SIG 기능지표 불변 + 7테마·시각 스크린샷.

## Global Constraints

- **수치 로직 불변** — 표현만 추가. 예측 target·확률·시그널·컨플루언스 값 동일.
- 인라인 SVG만(외부 차트 라이브러리 금지). 색: `--bull`#46c28e·`--bear`#e06a6a·`--gold`·`--eth`·`--muted`·`--faint`·`--line`. **좌측 accent line 금지**.
- `forge-core.js`·데이터 미변경 → `node --test` 199/199. 배포 세트 7파일(변경분 forge-app.js·forge.css).
- 검증: SIG 기능지표(target/dir/pathLen/nodes/ell/themes) 불변 확인(staticEls는 새 SVG로 증가=의도).

---

### Task 1: SVG 헬퍼 4종 + CSS

**Files:** Modify `map/forge-app.js`(헬퍼 추가), `map/forge.css`(스타일)

**Interfaces:**
- Produces: `_donutSVG(segs, opts)` · `_gaugeSVG(val,min,max,opts)` · `_ringSVG(pct,color,size)` · `_sparkSVG(vals,opts)` — SVG 문자열 반환(순수).

- [ ] **Step 1: 헬퍼 추가.** `forge-app.js` 상단 근처(예: `_lazyDraw` 인근)에:

```javascript
  /* ── 시각화 인라인 SVG 헬퍼(순수·테마 토큰색) ── */
  function _donutSVG(segs, opts) {
    opts = opts || {}; const size = opts.size || 46, th = opts.thickness || 7, r = (size - th) / 2, cx = size / 2, C = 2 * Math.PI * r;
    const tot = segs.reduce((s, x) => s + Math.max(0, x.v), 0) || 1; let off = 0, arcs = "";
    segs.forEach(s => { const len = Math.max(0, s.v) / tot * C;
      arcs += `<circle cx="${cx}" cy="${cx}" r="${r}" fill="none" stroke="${s.color}" stroke-width="${th}" stroke-dasharray="${len.toFixed(2)} ${(C - len).toFixed(2)}" stroke-dashoffset="${(-off).toFixed(2)}" transform="rotate(-90 ${cx} ${cx})"/>`; off += len; });
    const ct = opts.centerText ? `<text x="${cx}" y="${cx}" text-anchor="middle" dominant-baseline="central" font-size="${opts.centerSize || 11}" font-weight="800" fill="${opts.centerColor || 'currentColor'}">${opts.centerText}</text>` : "";
    return `<svg class="viz-donut" width="${size}" height="${size}" viewBox="0 0 ${size} ${size}"><circle cx="${cx}" cy="${cx}" r="${r}" fill="none" stroke="var(--line)" stroke-width="${th}"/>${arcs}${ct}</svg>`;
  }
  function _gaugeSVG(val, min, max, opts) {
    opts = opts || {}; const w = opts.w || 66, h = opts.h || 38, r = opts.r || 26, cx = w / 2, cy = h - 5, th = opts.thickness || 6;
    const ang = f => Math.PI * (1 - Math.max(0, Math.min(1, f))), pt = f => [cx + r * Math.cos(ang(f)), cy - r * Math.sin(ang(f))];
    const arc = (f0, f1, col, wd) => { const a = pt(f0), b = pt(f1), lg = (f1 - f0) > 0.5 ? 1 : 0; return `<path d="M${a[0].toFixed(1)} ${a[1].toFixed(1)} A${r} ${r} 0 ${lg} 1 ${b[0].toFixed(1)} ${b[1].toFixed(1)}" fill="none" stroke="${col}" stroke-width="${wd || th}" stroke-linecap="round"/>`; };
    const norm = v => (Math.max(min, Math.min(max, v)) - min) / ((max - min) || 1);
    let zoneArcs = ""; (opts.zones || []).forEach(z => { zoneArcs += arc(norm(z.from), norm(z.to), z.color, th); });
    const track = (opts.zones && opts.zones.length) ? "" : arc(0, 1, "var(--line)", th);
    const vf = norm(val), valArc = (opts.zones && opts.zones.length) ? "" : arc(0, vf, opts.color || "var(--gold)", th);
    const n = pt(vf); const needle = `<line x1="${cx}" y1="${cy}" x2="${n[0].toFixed(1)}" y2="${n[1].toFixed(1)}" stroke="${opts.color || 'currentColor'}" stroke-width="2" stroke-linecap="round"/><circle cx="${cx}" cy="${cy}" r="2.5" fill="${opts.color || 'currentColor'}"/>`;
    return `<svg class="viz-gauge" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}">${track}${zoneArcs}${valArc}${needle}</svg>`;
  }
  function _ringSVG(pct, color, size) {
    size = size || 30; const th = 4, r = (size - th) / 2, cx = size / 2, C = 2 * Math.PI * r, len = Math.max(0, Math.min(100, pct)) / 100 * C;
    return `<svg class="viz-ring" width="${size}" height="${size}" viewBox="0 0 ${size} ${size}"><circle cx="${cx}" cy="${cx}" r="${r}" fill="none" stroke="var(--line)" stroke-width="${th}"/><circle cx="${cx}" cy="${cx}" r="${r}" fill="none" stroke="${color}" stroke-width="${th}" stroke-linecap="round" stroke-dasharray="${len.toFixed(1)} ${(C - len).toFixed(1)}" transform="rotate(-90 ${cx} ${cx})"/><text x="${cx}" y="${cx}" text-anchor="middle" dominant-baseline="central" font-size="9" font-weight="700" fill="currentColor">${Math.round(pct)}</text></svg>`;
  }
  function _sparkSVG(vals, opts) {
    opts = opts || {}; const w = opts.w || 120, h = opts.h || 24, pad = 2; if (!vals || vals.length < 2) return "";
    const mn = Math.min.apply(0, vals), mx = Math.max.apply(0, vals), sp = (mx - mn) || 1;
    const x = i => pad + i / (vals.length - 1) * (w - 2 * pad), y = v => h - pad - (v - mn) / sp * (h - 2 * pad);
    const pts = vals.map((v, i) => `${x(i).toFixed(1)},${y(v).toFixed(1)}`).join(" "), col = opts.color || "var(--gold)";
    const area = opts.fill ? `<polygon points="${pad},${h} ${pts} ${(w - pad)},${h}" fill="${col}" opacity="0.12"/>` : "";
    const zero = (mn < 0 && mx > 0) ? `<line x1="${pad}" y1="${y(0).toFixed(1)}" x2="${w - pad}" y2="${y(0).toFixed(1)}" stroke="var(--line)" stroke-width="1" stroke-dasharray="2 2"/>` : "";
    return `<svg class="viz-spark" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}">${zero}${area}<polyline points="${pts}" fill="none" stroke="${col}" stroke-width="1.6" stroke-linejoin="round"/></svg>`;
  }
```

- [ ] **Step 2: CSS.** `forge.css`에:

```css
  .viz-donut,.viz-gauge,.viz-ring,.viz-spark{display:block;color:var(--ink)}
  .fcv-viz{display:flex;align-items:center;gap:16px;flex-wrap:wrap;padding:6px 2px 2px;width:100%}
  .fcv-viz-item{display:flex;align-items:center;gap:7px}
  .fcv-viz-lab{font-size:10.5px;color:var(--eth);line-height:1.25}
  .fcv-viz-lab b{color:var(--ink);font-size:12px}
  .hz-spark{display:flex;align-items:center;gap:8px;padding:2px 2px 6px;font-size:10.5px;color:var(--eth)}
  .os-gauge{display:inline-flex;vertical-align:middle;margin-left:8px}
```

- [ ] **Step 3: 문법·SIG.** `node --check forge-app.js` + SIG 기능지표 불변(헬퍼는 미호출이라 화면 무변화 → SIG 완전 동일).

- [ ] **Step 4: 헬퍼 단독 검증(헤드리스).** `_donutSVG([{v:20,color:'#46c28e'},{v:3,color:'#e8b463'}]).indexOf("<svg")===0` 등 4종이 SVG 문자열 반환·에러 0.

- [ ] **Step 5: Commit** — `git commit -am "feat(forge): 시각화 SVG 헬퍼(donut·gauge·ring·spark) + viz CSS"`

---

### Task 2: 판정 헤더 시각 스트립 (renderVerdict)

**Files:** Modify `map/forge-app.js` (`renderVerdict` ~1284–1306)

**Interfaces:** Consumes Task 1 헬퍼 + 기존 `bl/ne/be/tt`·`verdict.confluence`·`score`·`col`·`regime`.

- [ ] **Step 1: `_bd` 블록에 시각 스트립 추가.** 현 `_bd`(스택바) 계산부(bl/ne/be/tt 있는 `try` 내부)에서, 스택바 `.fcv-break` 뒤에 `.fcv-viz` 행을 덧붙인다:

```javascript
          const dirCol = regime === "bull" ? "#46c28e" : regime === "bear" ? "#e06a6a" : "var(--eth)";
          const cf = verdict.confluence;
          const cfDonut = (cf && cf.total) ? `<div class="fcv-viz-item">${_donutSVG([{ v: cf.agree, color: dirCol }, { v: Math.max(0, cf.total - cf.agree), color: "var(--faint)" }], { centerText: cf.score + "%" })}<span class="fcv-viz-lab">컨플루언스<br><b>${cf.agree}/${cf.total}</b></span></div>` : "";
          const bnbDonut = `<div class="fcv-viz-item">${_donutSVG([{ v: bl, color: "#46c28e" }, { v: ne, color: "#e8b463" }, { v: be, color: "#e06a6a" }], { centerText: (regime === "bull" ? "▲" : regime === "bear" ? "▼" : "▸"), centerColor: dirCol, centerSize: 13 })}<span class="fcv-viz-lab">지표 방향<br><b style="color:#46c28e">${bl}</b>·<b style="color:var(--gold)">${ne}</b>·<b style="color:#e06a6a">${be}</b></span></div>`;
          const sigGauge = `<div class="fcv-viz-item">${_gaugeSVG(score, -100, 100, { color: dirCol })}<span class="fcv-viz-lab">시그널<br><b style="color:${dirCol}">${score >= 0 ? "+" : ""}${score}</b></span></div>`;
          _bd += `<div class="fcv-viz">${cfDonut}${bnbDonut}${sigGauge}</div>`;
```
> 주의: `score`는 renderVerdict 내 이미 계산됨(`Math.round(verdict.score)` 등, 실제 변수명 확인해 사용). `_bd`는 기존 스택바 문자열에 **이어붙임**(스택바 유지).

- [ ] **Step 2: 문법·SIG.** `node --check` + SIG 기능지표 불변(target/dir/pathLen/nodes/themes 동일, staticEls 증가).

- [ ] **Step 3: 시각 확인(헤드리스).** 실데이터 주입(차트뷰)·runForge → `#fcVerdictBar` 안에 `.fcv-viz` + `svg.viz-donut`×2 + `svg.viz-gauge`×1 존재. 스크린샷: 도넛 2·게이지 1 표시·색 방향 정합. 에러 0.

- [ ] **Step 4: Commit** — `git commit -am "feat(forge): 판정 헤더 시각 스트립(컨플루언스·상중하 도넛 + 시그널 반원 게이지)"`

---

### Task 3: 예측 시점별 표 시각화 (renderHorizons)

**Files:** Modify `map/forge-app.js` (`renderHorizons` ~139–147)

**Interfaces:** Consumes `_ringSVG`·`_sparkSVG`.

- [ ] **Step 1: 달성확률 셀을 링 게이지로.** 현 `<td>`의 `<span class="dbar">…</span><b …>${up}%</b>` 를 `_ringSVG(up, probCol, 30)` + 방향 화살표로 교체:

```javascript
        <td><span class="dash-cell os-ringcell">${_ringSVG(up, probCol, 30)}<b class="dval ${pc}">${_parw}${up}%</b></span></td>
```

- [ ] **Step 2: 변화% 스파크라인 추가.** rows 조립 후 표 위에 스파크라인 삽입. `hs.map` 전에 `chg` 배열 수집:

```javascript
    const chgs = hs.map(h => { const vF = path[h - 1]; const v = anchor + (vF - anchor) * u; return anchor ? (v - anchor) / anchor * 100 : 0; });
    const spark = chgs.length >= 2 ? `<div class="hz-spark">${_sparkSVG(chgs, { color: (chgs[chgs.length - 1] >= 0 ? "#46c28e" : "#e06a6a"), fill: true, w: 130, h: 26 })}<span>변화% 경로 +${hs[0]}~+${hs[hs.length - 1]}${unit}</span></div>` : "";
    host.innerHTML = spark + `<table class="dash-table hz-table"><thead>${head}</thead><tbody>${rows}</tbody></table>`;
```
> 기존 `host.innerHTML = \`<table…>\`` 를 위 spark 포함본으로 교체.

- [ ] **Step 3: CSS 보정.** `forge.css`에 `.os-ringcell{display:inline-flex;align-items:center;gap:6px}`.

- [ ] **Step 4: 문법·SIG + 시각.** `node --check` + SIG 기능지표 불변. 헤드리스: `#fcHorizons`에 `svg.viz-ring`×N + `svg.viz-spark`×1. 스크린샷. 에러 0.

- [ ] **Step 5: Commit** — `git commit -am "feat(forge): 예측 시점별 표 시각화(달성확률 링 게이지 + 변화% 스파크라인)"`

---

### Task 4: 오실레이터 미니게이지 (fcDrawRsi/Cci/Williams/Mfi)

**Files:** Modify `map/forge-draw.js` (각 fcDrawX의 meta 갱신부)

**Interfaces:** Consumes `_gaugeSVG`(forge-app.js — 전역 공유). zones=존밴드.

- [ ] **Step 1: 각 meta에 게이지 병기.** 각 fcDrawX의 `meta.innerHTML = "…"` 끝에 `+ '<span class="os-gauge">' + _gaugeSVG(last, min, max, {zones:[…], w:52, h:30, r:22}) + '</span>'`:
  - RSI(`fcDrawRsi`): `_gaugeSVG(rsi.last, 0, 100, {zones:[{from:0,to:30,color:"#46c28e"},{from:70,to:100,color:"#e06a6a"}]})`.
  - MFI: `_gaugeSVG(mfi.last, 0, 100, {zones:[{from:0,to:20,color:"#46c28e"},{from:80,to:100,color:"#e06a6a"}]})`.
  - Williams: `_gaugeSVG(w.last, -100, 0, {zones:[{from:-100,to:-80,color:"#46c28e"},{from:-20,to:0,color:"#e06a6a"}]})`.
  - CCI: `_gaugeSVG(Math.max(-200,Math.min(200,cci.last)), -200, 200, {zones:[{from:-200,to:-100,color:"#46c28e"},{from:100,to:200,color:"#e06a6a"}]})`.

- [ ] **Step 2: 문법·SIG + 시각.** `node --check forge-draw.js` + SIG 기능지표 불변. 헤드리스(서브패널 가시): 각 meta에 `svg.viz-gauge` 존재·존밴드 표시. 스크린샷. 에러 0.

- [ ] **Step 3: Commit** — `git commit -am "feat(forge): 오실레이터 서브패널 헤더에 존밴드 미니 게이지(RSI·MFI·Williams·CCI)"`

---

### Task 5: 7테마 검증 + 배포

- [ ] **Step 1: 7테마 순회.** 각 테마 applyTheme 후 도넛/게이지/링/스파크 색 적응(bull/bear 상수·나머지 토큰·라이트에서도 판독) + 에러 0. 스크린샷 midnight·paper·daylight.
- [ ] **Step 2: SIG 기능지표 최종 확인** (target/dir/pathLen/nodes/ell/themes 불변).
- [ ] **Step 3: 코어 테스트** `node --test forge-core.test.js` 199/199.
- [ ] **Step 4: 배포.** forge-app.js·forge.css 캐시버스터 갱신(forge.css·forge-app.js `?v=` bump) + forge.html 갱신. cafe24 `put` forge.html·forge.css·forge-app.js·forge-draw.js. curl 200.
- [ ] **Step 5: 메모리 기록** — 패널2 시각화 완료.

---

## 자체 검증(계획 작성자)

- 스펙 커버리지: 헬퍼(T1)/판정 도넛·게이지(T2)/예측표(T3)/오실레이터 게이지(T4)/테마·배포(T5). ✅
- 수치 불변: 모든 태스크 표현만 추가, SIG 기능지표 불변 확인 단계 존재. ✅
- 함수명 일관: `_donutSVG`/`_gaugeSVG`/`_ringSVG`/`_sparkSVG`. forge-draw.js가 forge-app.js 헬퍼 호출(전역 공유·로드순서 app이 draw 뒤 → **주의**: draw는 app보다 먼저 로드됨!). ⚠️ 아래 참조.
- 비목표(매트릭스 히트맵·애니메이션) 범위 밖. ✅

> ⚠️ **로드 순서 이슈**: 헬퍼를 forge-app.js에 두면 forge-draw.js(먼저 로드)의 fcDrawX가 참조. classic script 전역 공유라 **런타임 호출 시점**(fcDrawX 실행=분석 시)엔 app 로드 완료되어 정의됨 → 안전. 단 파싱 시점 즉시호출 아님(함수 내부 참조)이라 문제없음. T4 검증에서 확인.
