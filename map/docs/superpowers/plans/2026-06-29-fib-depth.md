# 피보나치 심화 (Fibonacci Depth) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 창 hi/lo 되돌림뿐이던 피보를 자동 스윙 앵커(방향 인지)·확장 레벨·골든포켓·현재구간·스윙 간 합류·종합 bias로 심화하고, 예측(TF가중)·작도·시연(Plan B 프레임워크)에 일관 반영한다.

**Architecture:** 피보 수학을 코어 순수 함수 `ForgeCore.analyzeFib`로 모아 예측(`run`)·작도(`_drawFibLayers`)가 공유. 시연 텍스트는 코어 `fibSteps`로 분리해 forge.html `analysisSteps`의 fib 케이스가 호출 → Plan B의 HUD 단계/누적 로그/레이어 reveal 프레임워크가 자동 적용. MA(`analyzeMA`/`_drawMALayers`) 패턴을 그대로 따른다.

**Tech Stack:** Vanilla JS(무빌드), `node:test`/`node:assert`, HTML5 Canvas.

## Global Constraints

- 바닐라 JS · 빌드도구/외부 라이브러리 금지 · 단일 `forge.html` + 단일 `forge-core.js`.
- UI 텍스트 한국어. 다크 토큰 색만: 골드 `#ffd24d`/`#e8b463`, 골든포켓 채움 `rgba(232,180,99,.16)`, 잉크 외곽 `rgba(11,15,20,.85)`.
- `analyzeFib`·`fibSteps`는 **순수 함수**(부수효과 없음). `P<2`→빈 결과(bias 0). `||1` 0-나눗셈 가드.
- 작도·예측은 같은 `analyzeFib` 공유. 작도 좌표 규약·`M`({fiToX,pToY,nowFi,fiMin,reveal})은 MA `_drawMALayers`와 동일. `c.save()/restore()` 격리.
- 예측 피보 기여는 **한 곳**(`run`의 fibDrift 보조항)에서만(이중계상 방지), `trendProfileForTF(tf).trendScale`로 TF 가중.
- 합류는 **피보 스윙 간만**(피보+MA 제외).
- 단위 테스트: `node --test forge-core.test.js`.

---

### Task 1: 코어 `analyzeFib()` + `fibSteps()` + export + 테스트

**Files:**
- Modify: `forge-core.js` (`detectSwings`(`:316`) 다음에 `analyzeFib`·`fibSteps` 삽입; export `:647`)
- Test: `forge-core.test.js`

**Interfaces:**
- Consumes: 기존 `detectSwings(arr, sens)→[{idx,price}]`.
- Produces:
  - `analyzeFib(price, {len=120, swing=0.05, srPct=0.01}) → { dir, swing:{fromIdx,toIdx,fromPrice,toPrice}|null, levels:[{ratio,price,kind:"retr"|"ext",golden,confluent}], zone:{nearest:{ratio,price,side}|null, inGolden, lower, upper, goldenLo, goldenHi}, bias }`
  - `fibSteps(fib) → string[5]`. 둘 다 순수.

- [ ] **Step 1: 실패 테스트 작성** — `forge-core.test.js` 끝에 추가

```js
test("analyzeFib: 상승 스윙 → dir up, 0.618 되돌림·1.618 확장 존재", () => {
  const up = Array.from({ length: 50 }, (_, i) => 100 + i * 2);   // 100→198
  const price = up.concat([190, 185, 180]);                       // 되돌림
  const f = ForgeCore.analyzeFib(price, { swing: 0.05 });
  assert.strictEqual(f.dir, "up");
  assert.ok(f.swing.toPrice > f.swing.fromPrice);
  assert.ok(f.levels.some(L => L.kind === "retr" && Math.abs(L.ratio - 0.618) < 1e-9 && isFinite(L.price)));
  assert.ok(f.levels.some(L => L.kind === "ext" && Math.abs(L.ratio - 1.618) < 1e-9 && isFinite(L.price)));
  assert.ok(isFinite(f.bias));
});

test("analyzeFib: 하락 스윙 → dir down, 유한 bias", () => {
  const down = Array.from({ length: 50 }, (_, i) => 200 - i * 2);
  const price = down.concat([110, 115, 120]);
  const f = ForgeCore.analyzeFib(price, { swing: 0.05 });
  assert.strictEqual(f.dir, "down");
  assert.ok(isFinite(f.bias));
});

test("analyzeFib: 골든포켓 구간 현재가 → inGolden true", () => {
  // 상승 100→200 후, 0.618~0.65 되돌림 가격대(135~138.2)에 현재가
  const up = Array.from({ length: 60 }, (_, i) => 100 + (100 * i / 59));   // 100→200
  const price = up.concat([137]);
  const f = ForgeCore.analyzeFib(price, { swing: 0.04 });
  // dir up, golden band ~[135,138.2], 137 내부
  assert.strictEqual(f.zone.inGolden, true);
});

test("analyzeFib: 소량/피벗부족 → 폴백, 유한 bias·레벨 배열", () => {
  const f = ForgeCore.analyzeFib([10, 11, 12, 13], {});
  assert.ok(isFinite(f.bias));
  assert.ok(Array.isArray(f.levels) && f.levels.length > 0);
});

test("fibSteps: 5단계, bias 반영", () => {
  const up = Array.from({ length: 50 }, (_, i) => 100 + i * 2).concat([190, 185]);
  const s = ForgeCore.fibSteps(ForgeCore.analyzeFib(up, { swing: 0.05 }));
  assert.strictEqual(s.length, 5);
  assert.ok(/bias/.test(s[4]));
});
```

> 골든포켓 테스트가 환경에서 `inGolden:false`로 나오면(스윙 검출이 다른 피벗을 잡는 경우) **합성 데이터의 마지막 값(137)·`swing` 민감도만** 조정해 현재가가 골든포켓 가격대에 들도록 하라(단언 유지, 구현 로직 불변).

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test forge-core.test.js`
Expected: FAIL — `ForgeCore.analyzeFib is not a function`

- [ ] **Step 3: `analyzeFib`·`fibSteps` 구현** — `forge-core.js`의 `detectSwings` 함수(`:316`) **다음**에 삽입

```js
  function analyzeFib(price, opts) {
    opts = opts || {};
    const len = opts.len || 120, swing = opts.swing != null ? opts.swing : 0.05, srPct = opts.srPct != null ? opts.srPct : 0.01;
    const P = price.length;
    const EMPTY = { dir: null, swing: null, levels: [], zone: { nearest: null, inGolden: false, lower: null, upper: null, goldenLo: null, goldenHi: null }, bias: 0 };
    if (P < 2) return EMPTY;
    const RETR = [0, .236, .382, .5, .618, .786, 1], EXT = [1.272, 1.414, 1.618, 2.0, 2.618];
    const sw = detectSwings(price, swing);
    let fromIdx, toIdx, fromPrice, toPrice, dir;
    if (sw.length >= 2) {
      const a = sw[sw.length - 2], b = sw[sw.length - 1];
      fromIdx = a.idx; fromPrice = a.price; toIdx = b.idx; toPrice = b.price;
      dir = toPrice >= fromPrice ? "up" : "down";
    } else {
      const s0 = Math.max(0, P - len), seg = price.slice(s0);
      const hiV = Math.max(...seg), loV = Math.min(...seg);
      const hiIdx = s0 + seg.indexOf(hiV), loIdx = s0 + seg.indexOf(loV);
      dir = hiIdx >= loIdx ? "up" : "down";
      if (dir === "up") { fromIdx = loIdx; fromPrice = loV; toIdx = hiIdx; toPrice = hiV; }
      else { fromIdx = hiIdx; fromPrice = hiV; toIdx = loIdx; toPrice = loV; }
    }
    const hi = Math.max(fromPrice, toPrice), lo = Math.min(fromPrice, toPrice), rng = (hi - lo) || 1;
    const priceAt = (r, kind) => kind === "retr"
      ? (dir === "up" ? hi - rng * r : lo + rng * r)
      : (dir === "up" ? hi + rng * (r - 1) : lo - rng * (r - 1));
    // 2차(장기 창 hi/lo) 레벨 — 합류
    const s2 = Math.max(0, P - len), seg2 = price.slice(s2);
    const hi2 = Math.max(...seg2), lo2 = Math.min(...seg2), rng2 = (hi2 - lo2) || 1;
    const sec = RETR.map(r => dir === "up" ? hi2 - rng2 * r : lo2 + rng2 * r);
    const levels = [];
    for (const r of RETR) { const p = priceAt(r, "retr"); levels.push({ ratio: r, price: p, kind: "retr", golden: (r >= 0.618 && r <= 0.65), confluent: sec.some(q => Math.abs(p - q) / (Math.abs(p) || 1) <= srPct) }); }
    for (const r of EXT) levels.push({ ratio: r, price: priceAt(r, "ext"), kind: "ext", golden: false, confluent: false });
    const pl = price[P - 1];
    let nearest = null, nd = Infinity;
    for (const L of levels) { const d = Math.abs(pl - L.price) / (Math.abs(pl) || 1); if (d < nd) { nd = d; nearest = { ratio: L.ratio, price: L.price, side: pl >= L.price ? "support" : "resistance" }; } }
    const gpA = priceAt(0.618, "retr"), gpB = priceAt(0.65, "retr");
    const goldenLo = Math.min(gpA, gpB), goldenHi = Math.max(gpA, gpB);
    const inGolden = pl >= goldenLo && pl <= goldenHi;
    let lower = null, upper = null;
    const rs = levels.filter(L => L.kind === "retr").slice().sort((x, y) => x.price - y.price);
    for (let i = 0; i < rs.length - 1; i++) { if (pl >= rs[i].price && pl <= rs[i + 1].price) { lower = rs[i].ratio; upper = rs[i + 1].ratio; break; } }
    const srDir = nearest ? (nearest.side === "support" ? 1 : -1) : 0;
    const proximity = nearest ? Math.max(0, 1 - nd / srPct) : 0;
    const goldenBoost = inGolden ? (dir === "up" ? 0.25 : dir === "down" ? -0.25 : 0) : 0;
    const bias = Math.max(-1, Math.min(1, srDir * proximity + goldenBoost));
    return { dir, swing: { fromIdx, toIdx, fromPrice, toPrice }, levels, zone: { nearest, inGolden, lower, upper, goldenLo, goldenHi }, bias };
  }

  function fibSteps(fib) {
    const fmt = v => (Math.abs(v) >= 100 ? Math.round(v) : Math.round(v * 100) / 100);
    const d = fib.dir === "up" ? "상승" : fib.dir === "down" ? "하락" : "중립";
    const ext = fib.levels.find(L => L.kind === "ext" && Math.abs(L.ratio - 1.618) < 1e-9);
    const conf = fib.levels.filter(L => L.confluent).length;
    const near = fib.zone.nearest;
    const zTxt = near ? (near.side === "support" ? "지지" : "저항") + " 근접(" + near.ratio + ")" : "구간 중앙";
    const bTxt = fib.bias > 0.1 ? "상승" : fib.bias < -0.1 ? "하락" : "중립";
    return [
      fib.swing ? d + " 스윙 식별 (" + fmt(fib.swing.fromPrice) + "→" + fmt(fib.swing.toPrice) + ")" : "스윙 식별 폴백",
      "되돌림 레벨 7개" + (conf ? " · 합류 " + conf + "곳" : ""),
      ext ? "확장 목표 1.618 = " + fmt(ext.price) : "확장 레벨",
      (fib.zone.inGolden ? "골든포켓 진입 · " : "") + zTxt,
      "종합 방향 " + bTxt + " (bias " + fib.bias.toFixed(2) + ")"
    ];
  }
```

- [ ] **Step 4: export 추가** — `forge-core.js:647` return 객체 끝에 `analyzeFib, fibSteps` 삽입

```js
  return { version, makeDemoSeries, buildDAG, evalBlocks, detrendNorm, pdmTheta, scanPeriod, run, runSteps, visionBiasFrom, sampleSeries, sampleGraph, analyzeTrend, trendProfileForTF, analyzeMA, maSteps, analyzeFib, fibSteps };
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test forge-core.test.js`
Expected: PASS (신규 5개 포함 전체). 골든포켓 테스트가 빡빡하면 데이터만 조정(단언 유지).

- [ ] **Step 6: 커밋**

```bash
git add forge-core.js forge-core.test.js
git commit -m "feat(forge): ForgeCore.analyzeFib+fibSteps — 자동스윙·확장·골든포켓·합류·bias"
```

---

### Task 2: 예측 `run` — 피보 드리프트(TF 가중) + 테스트

**Files:**
- Modify: `forge-core.js` (`run` 내: maDrift 줄(`:546`) 다음에 fib 산출; `m` 합산 줄(`:558`))
- Test: `forge-core.test.js`

**Interfaces:**
- Consumes: `analyzeFib`(Task 1), `trendProfileForTF`. `run` 스코프 `price`, `graph`, `_prof`, `k`, `futW`, `maDrift`.
- Produces: 피보 블록 있으면 예측이 피보 S/R 방향으로 보조 편향.

- [ ] **Step 1: 실패 테스트 작성** — `forge-core.test.js` 끝에 추가

```js
test("run: 피보 블록 유무가 예측 타깃을 가른다 (격리) + TF", () => {
  const base = [{ id: "p", kind: "block", blockType: "price" }, { id: "o", kind: "block", blockType: "predict" }];
  const up = { price: Array.from({ length: 60 }, (_, i) => 100 + i * 1.5) };
  const withFib = { nodes: [...base, { id: "f", kind: "block", blockType: "fib", params: { len: 120 } }], edges: [{ from: "p", to: "o" }, { from: "p", to: "f" }] };
  const without = { nodes: base, edges: [{ from: "p", to: "o" }] };
  const rW = ForgeCore.run(withFib, up, { futW: 12, timeframe: "월봉" });
  const rN = ForgeCore.run(without, up, { futW: 12, timeframe: "월봉" });
  assert.ok(rW.prediction.path.every(isFinite) && rN.prediction.path.every(isFinite));
  assert.notStrictEqual(rW.prediction.target, rN.prediction.target);   // 피보 기여로 달라짐(드리프트 격리)
});

test("run: 피보 블록 없으면 기여 0(기존 동작)", () => {
  const G = { nodes: [{ id: "p", kind: "block", blockType: "price" }, { id: "o", kind: "block", blockType: "predict" }], edges: [{ from: "p", to: "o" }] };
  const r = ForgeCore.run(G, { price: Array.from({ length: 40 }, (_, i) => 100 + i) }, { futW: 8, timeframe: "월봉" });
  assert.ok(r.prediction.path.every(isFinite));
});
```

> 첫 테스트가 `notStrictEqual` 실패(=두 타깃 동일)하면 피보 bias가 0인 데이터다 — 현재가가 어떤 레벨에도 근접 안 함. 합성 데이터를 현재가가 레벨에 근접하도록 조정(예: 마지막 봉을 되돌림 레벨 근처로). 단언·로직 불변.

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test forge-core.test.js`
Expected: FAIL — `notStrictEqual` (현재 피보가 예측에 반영 안 됨 → 두 타깃 동일)

- [ ] **Step 3: 피보 드리프트 산출** — `forge-core.js`의 `const maDrift = ...` 줄(`:546`) **다음**에 삽입

```js
    const _fn = (graph.nodes || []).find(nd => nd.kind === "block" && nd.blockType === "fib");
    const _fib = _fn ? analyzeFib(price, { len: (_fn.params && _fn.params.len) || 120, swing: (_fn.params && _fn.params.swing) || 0.05 }) : null;
    const fibDrift = _fib ? _fib.bias * _prof.trendScale * 0.08 : 0;   // 피보 S/R 방향 드리프트(±8% 상한·TF가중)
```

- [ ] **Step 4: 예측 루프 합산** — `forge-core.js:558`의 `const m = ... + maDrift * (k / futW), sd = ...` 줄을 교체

```js
      const m = rev + mom + trend + sig + seas + maDrift * (k / futW) + fibDrift * (k / futW), sd = Math.sqrt(sigBand * sigBand + 0.36 * trChSig * trChSig) * Math.sqrt(k) * 0.85;
```

> 기존 항·`sd` 불변, `+ fibDrift * (k / futW)`만 추가.

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test forge-core.test.js`
Expected: PASS (전체)

- [ ] **Step 6: 커밋**

```bash
git add forge-core.js forge-core.test.js
git commit -m "feat(forge): 예측 run에 피보 방향 드리프트(analyzeFib.bias·TF가중) 보조항"
```

---

### Task 3: 작도 `_drawFibLayers` + 차트 모드 + 편집기 `swing`

**Files:**
- Modify: `forge.html` (헬퍼는 `_drawMALayers`(`:2543`) 근처에 추가; 차트 fib 분기; 편집기 `:1231`)

**Interfaces:**
- Consumes: `ForgeCore.analyzeFib`(Task 1). 차트 기하 `g`/`toXh`/`toY`/`Hn`/`off`/`P`/`price`/`col`.
- Produces: `_drawFibLayers(c, fib, M)` — `M={fiToX,pToY,nowFi,fiMin,reveal}`. 두 모드 공용(Task 4가 오버레이 호출).

- [ ] **Step 1: 공용 헬퍼 추가** — `forge.html`의 `_drawMALayers` 함수 근처에 삽입

```js
  // 피보 작도(차트·오버레이 공용, reveal 인지). M=좌표 매퍼.
  function _drawFibLayers(c, fib, M) {
    c.save();
    const { fiToX, pToY, nowFi, fiMin = 0, reveal = Infinity } = M;
    const xL = fiToX(Math.max(fiMin, 0)), xR = fiToX(nowFi), GOLD = "#ffd24d", DIM = "#e8b463";
    function levelLine(L) {
      const y = pToY(L.price); if (![y, xL, xR].every(isFinite)) return;
      const ext = L.kind === "ext";
      c.setLineDash(ext ? [5, 4] : (L.confluent ? [] : [2, 3]));
      c.strokeStyle = L.golden ? GOLD : ext ? DIM : "rgba(232,180,99,.55)";
      c.lineWidth = L.confluent ? 2.2 : ext ? 1.2 : 1; c.globalAlpha = ext ? .6 : .85;
      c.beginPath(); c.moveTo(xL, y); c.lineTo(xR, y); c.stroke();
      c.globalAlpha = 1; c.setLineDash([]);
      c.fillStyle = L.golden ? GOLD : DIM; c.font = "700 9px ui-monospace,monospace"; c.textAlign = "left";
      c.fillText((L.confluent ? "✦" : "") + L.ratio.toFixed(3) + (ext ? " 목표" : ""), xL + 3, y - 2);
    }
    if (reveal >= 1) fib.levels.filter(L => L.kind === "retr").forEach(levelLine);
    if (reveal >= 2) fib.levels.filter(L => L.kind === "ext").forEach(levelLine);
    if (reveal >= 3) {
      const z = fib.zone;
      if (z && isFinite(z.goldenLo) && isFinite(z.goldenHi)) {
        const yH = pToY(z.goldenHi), yL = pToY(z.goldenLo);
        if (isFinite(yH) && isFinite(yL)) { c.globalAlpha = .16; c.fillStyle = "#e8b463"; c.fillRect(xL, Math.min(yH, yL), Math.max(2, xR - xL), Math.abs(yL - yH)); c.globalAlpha = 1; }
      }
      if (z && z.nearest) {
        const y = pToY(z.nearest.price);
        if (isFinite(y)) { c.fillStyle = z.inGolden ? GOLD : DIM; c.font = "700 9.5px ui-monospace,monospace"; c.textAlign = "right"; c.fillText((z.inGolden ? "골든포켓 · " : "") + (z.nearest.side === "support" ? "지지" : "저항"), xR - 3, y - 3); }
      }
      if (fib.swing) {
        [[fib.swing.fromIdx, fib.swing.fromPrice], [fib.swing.toIdx, fib.swing.toPrice]].forEach(pr => {
          const x = fiToX(Math.max(fiMin, pr[0])), y = pToY(pr[1]);
          if (isFinite(x) && isFinite(y)) { c.fillStyle = DIM; c.beginPath(); c.arc(x, y, 3, 0, 7); c.fill(); }
        });
      }
    }
    c.restore();
  }
```

- [ ] **Step 2: 차트 fib 분기 교체** — `forge.html`의 두 fib 분기 중 **차트 모드**(주변에 `toXh`/`toY`/`Hn`/`off` 선언) 분기 내부를 아래로 교체

```js
          const fib = ForgeCore.analyzeFib(price, { len: (n.params && n.params.len) || 120, swing: (n.params && n.params.swing) || 0.05 });
          _drawFibLayers(c, fib, { fiToX: fi => toXh(Math.max(0, Math.min(Hn - 1, fi - off))), pToY: v => toY(v), nowFi: P - 1, fiMin: off, reveal: _playing ? (_evReveal[n.id] || 0) : Infinity });
          legend.push({ col, t: EV_LABEL.fib + "(전문)" });
```

> `forge.html`엔 fib 분기가 둘(차트 `if`/오버레이 `else if`). **차트 분기**(toXh/toY 사용)만 이 태스크. 기존 단일 레벨 그리기 코드는 위로 전체 대체. 오버레이는 Task 4 — 건드리지 말 것.

- [ ] **Step 3: 편집기 `swing` 파라미터 추가** — `forge.html:1231` 의 fib 행을 아래로 교체

```js
    if (n.blockType === "fib") {
      rows.push(numRow("len", "피보 구간", (n.params && n.params.len) ?? 120));
      rows.push(numRow("swing", "스윙 민감도(%)", (n.params && n.params.swing != null) ? Math.round(n.params.swing * 100) : 5));
    }
```

> 표시는 %(5). 저장은 `numRow` 핸들러가 `Math.round(Number)` → 정수 % 저장. **소비측이 `n.params.swing`을 비율(0.05)로 기대**하므로, Task 3는 표시만 %로 하고 저장값은 정수 %가 된다 → **소비측(Task 1/2/3/4의 `(n.params.swing)||0.05`)과 단위 불일치 발생**. 이를 피하려고 편집기는 % 정수를 저장하되 **소비측 호출을 모두 `((n.params && n.params.swing) || 5) / 100`로 통일**해야 한다. ⚠️ 본 태스크에서 차트 fib 호출(Step 2)·이후 Task 2의 `run`·Task 4의 오버레이/`analysisSteps`의 swing 인자를 **`/100` 형태로 맞춘다**. (아래 "스윙 단위 규약" 참조)

**스윙 단위 규약(중요):** 편집기는 정수 %(기본 5)를 `n.params.swing`에 저장한다. 모든 소비측은 `swing: ((n.params && n.params.swing) != null ? n.params.swing : 5) / 100`로 비율 변환해 `analyzeFib`에 넘긴다. → Task 2 Step 3, Task 3 Step 2, Task 4 Step 1, analysisSteps fib 케이스 모두 이 형태를 쓴다. (Task 1의 `analyzeFib` 기본 `swing=0.05`는 비율 그대로 — 코어는 비율을 받는다.)

- [ ] **Step 4: 차트 fib 호출의 swing 단위 정정** — Step 2의 차트 호출에서 `swing`을 비율로:

```js
          const fib = ForgeCore.analyzeFib(price, { len: (n.params && n.params.len) || 120, swing: ((n.params && n.params.swing) != null ? n.params.swing : 5) / 100 });
```
(Step 2의 `swing` 인자를 이 줄로 대체 — 나머지는 동일)

- [ ] **Step 5: 구문 검증**

`forge.html` 인라인 `<script>` 추출 → `node --check` 무오류. 편집 후 `git diff <base> HEAD -- forge.html`로 **곡선/straight 따옴표 부수변경 없음** 확인(과거 사고 이력). 오버레이 fib 분기 미변경 확인. 보고에 기재.

- [ ] **Step 6: 커밋**

```bash
git add forge.html
git commit -m "feat(forge): 차트모드 피보 작도(_drawFibLayers·확장·골든포켓·합류) + swing 파라미터"
```

---

### Task 4: 오버레이 배선 + 시연 단계(analysisSteps) + 범례

**Files:**
- Modify: `forge.html` (오버레이 fib 분기; `analysisSteps`(`:3039`); `EV_LABEL` 확인)

**Interfaces:**
- Consumes: `_drawFibLayers`(Task 3), `ForgeCore.analyzeFib`/`fibSteps`(Task 1). 오버레이 기하 `xOf`/`yOf`/`clipY`/`P`/`price`/`col`.

- [ ] **Step 1: 오버레이 fib 분기 교체** — `forge.html`의 **오버레이 모드 fib 분기**(주변에 `xOf`/`yOf`/`clipY` 선언) 내부를 아래로 교체

```js
          const fib = ForgeCore.analyzeFib(price, { len: (n.params && n.params.len) || 120, swing: ((n.params && n.params.swing) != null ? n.params.swing : 5) / 100 });
          _drawFibLayers(c, fib, { fiToX: fi => xOf(fi), pToY: v => clipY(yOf(v)), nowFi: P - 1, fiMin: 0, reveal: _playing ? (_evReveal[n.id] || 0) : Infinity });
          legend.push({ col, t: EV_LABEL.fib + "(전문)" });
```

> 오버레이 fib 분기(xOf/yOf/clipY)만. 차트 분기(Task 3) 미변경. 기존 로그축 기하 피보 단일 레벨 코드는 전체 대체(레벨 가격은 `yOf`가 로그축 매핑 처리).

- [ ] **Step 2: `analysisSteps`에 fib 케이스 추가** — `forge.html:3039` `analysisSteps` 함수의 MA 분기 **다음**에 fib 분기 삽입

기존:
```js
  function analysisSteps(n, result, priceLast, price) {
    if (n.blockType === "ma" && Array.isArray(price) && price.length >= 2) {
      ...
    }
    return [{ text: nodeReadText(n, result, priceLast), layer: 0 }];
  }
```
MA `if` 블록과 `return` 사이에 삽입:
```js
    if (n.blockType === "fib" && Array.isArray(price) && price.length >= 2) {
      const fib = ForgeCore.analyzeFib(price, { len: (n.params && n.params.len) || 120, swing: ((n.params && n.params.swing) != null ? n.params.swing : 5) / 100 });
      const texts = ForgeCore.fibSteps(fib), layers = [1, 1, 2, 3, 4];
      return texts.map((t, i) => ({ text: t, layer: layers[i] }));
    }
```

- [ ] **Step 3: 범례 상수 확인** — `EV_LABEL.fib`는 "피보나치" 유지(작도에서 "(전문)" 동적 부착). 변경 없음 — 확인만.

- [ ] **Step 4: 구문 검증**

`forge.html` 인라인 `<script>` 추출 → `node --check` 무오류. `git diff <base> HEAD -- forge.html`로 따옴표 부수변경 없음 + 차트 fib 분기 미변경 확인. 보고에 기재.

- [ ] **Step 5: 커밋**

```bash
git add forge.html
git commit -m "feat(forge): 오버레이 피보 작도 배선 + analysisSteps 피보 5단계(시연 프레임워크 적용)"
```

---

## Self-Review

**Spec coverage:**
- §4 `analyzeFib`(자동스윙·되돌림+확장·골든포켓·합류·zone·bias) → Task 1 + 테스트 ✅
- §4.1~4.4 스윙/레벨/합류/구간·bias → Task 1 구현 ✅
- §5 예측(fibDrift 단일경로·TF가중·없으면 0) → Task 2 + 격리 테스트 ✅
- §6 `_drawFibLayers`(되돌림1·확장2·골든포켓+구간3·합류강조·reveal) → Task 3 ✅; 차트 Task 3·오버레이 Task 4 ✅
- §7 시연(코어 `fibSteps` + `analysisSteps` fib 케이스 → Plan B 프레임워크) → Task 1(fibSteps)·Task 4(analysisSteps) ✅
- §8 테스트(스윙·레벨·골든포켓·폴백·fibSteps·예측 TF) → Task 1·2 ✅
- 편집기 `len`/`swing` → Task 3 Step 3 ✅

**Placeholder scan:** 코드/명령/기대출력 구체값. 골든포켓·예측 격리 테스트는 데이터 튜닝 여지를 단언 불변 조건으로 한정. 작도는 단위테스트 불가 → 구문검사+라이브/헤드리스 시각검증.

**Type consistency:** `analyzeFib` 반환(`dir`/`swing`/`levels[{ratio,price,kind,golden,confluent}]`/`zone{nearest,inGolden,lower,upper,goldenLo,goldenHi}`/`bias`)을 Task 2(`_fib.bias`)·Task 3/4(`fib.levels`,`fib.zone.*`,`fib.swing`)·`fibSteps`에서 동일 사용. **스윙 단위 규약**: 편집기 정수%(기본 5) 저장, 소비측 모두 `/100` 변환 후 `analyzeFib`(비율) 호출 — Task 2/3/4 전부 동일 형태. `M` 매퍼는 `_drawMALayers`와 동일.
