# RSI 심화 (RSI Depth) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 텍스트뿐이던 RSI를 다이버전스·구간·50선/추세·종합 bias로 심화하고, 예측(TF가중)·작도(hero 다이버전스선)·RSI 오실레이터 서브패널·시연(Plan B)에 반영한다.

**Architecture:** RSI 분석을 코어 순수 함수 `analyzeRSI`로 모아 예측(`run`)·hero 작도(`_drawRsiLayers`)·서브패널(`fcDrawRsi`)이 공유. 시연 텍스트는 코어 `rsiSteps`(테스트 가능). 기존 `rsiSeries`(블록 값) 불변. 트렌드/MA/피보/엘리어트 패턴 + 신규 오실레이터 패널.

**Tech Stack:** Vanilla JS(무빌드), `node:test`/`node:assert`, HTML5 Canvas.

## Global Constraints

- 바닐라 JS · 빌드도구/외부 라이브러리 금지 · 단일 `forge.html` + 단일 `forge-core.js`.
- UI 텍스트 한국어. 다크 토큰 색만: RSI 곡선 골드 `#e8b463`/`#ffd24d`, 강세 `#46c28e`, 약세 `#e06a6a`, 중립 `#8a92b2`, 격자 `#2b3647`, 과열음영 `rgba(224,106,106,.10)`·과매도음영 `rgba(70,194,142,.10)`.
- `analyzeRSI`·`rsiSteps`는 **순수 함수**(부수효과 없음). `P<2`/피벗부족 → 폴백.
- 예측 RSI 기여는 **한 곳**(`run`의 rsiDrift 보조항)에서만(이중계상 방지), `trendProfileForTF(tf).trendScale`로 TF 가중, 계수 **0.06**(역추세 신호라 보수적).
- 기존 `rsiSeries`(블록 값) 변경 금지. RSI 블록 `params.period` 유지.
- 작도 `M`·`_evLabel`·`xRight` 규약은 기존 `_draw*Layers`와 동일. 서브패널은 RSI 블록 있을 때만 표시.
- 단위 테스트: `node --test forge-core.test.js`. 따옴표 위생: 각 forge.html 태스크 후 `git diff`로 부수변경 0 확인.

---

### Task 1: 코어 `analyzeRSI()` + `rsiSteps()` + export + 테스트

**Files:** Modify `forge-core.js` (`rsiSeries`(`:293`) 다음에 삽입; export `:775`). Test `forge-core.test.js`.

**Interfaces:**
- Consumes: `detectSwings(arr,sens)`, `linfit(y)→{a,b}`(b=기울기).
- Produces:
  - `analyzeRSI(price,{period=14,swing=0.03}) → { series:number[], last, zone:"overbought"|"oversold"|"neutral", trend, cross50:"above"|"below"|"cross_up"|"cross_down", divergence:{type:"bullish"|"bearish"|null, pricePts:[{idx,price},{idx,price}]|null}, bias }`
  - `rsiSteps(rsi) → string[5]`. 둘 다 순수.

- [ ] **Step 1: 실패 테스트 작성** — `forge-core.test.js` 끝에 추가

```js
test("analyzeRSI: 단조 상승 → overbought, trend>=0", () => {
  const price = Array.from({ length: 40 }, (_, i) => 100 + i * 2);
  const r = ForgeCore.analyzeRSI(price, { period: 14 });
  assert.ok(r.last > 70);
  assert.strictEqual(r.zone, "overbought");
  assert.ok(r.trend >= 0);
});

test("analyzeRSI: 강세 다이버전스(가격 LL·RSI HL) → bullish", () => {
  const seg = (from, to, n) => Array.from({ length: n }, (_, i) => from + (to - from) * (i + 1) / n);
  const price = [100, ...seg(100, 70, 10), ...seg(70, 85, 6), ...seg(85, 68, 8)];  // 저점1≈70, 반등85, 저점2≈68(LL)
  const r = ForgeCore.analyzeRSI(price, { period: 5, swing: 0.05 });
  assert.strictEqual(r.divergence.type, "bullish");
  assert.ok(r.divergence.pricePts && r.divergence.pricePts.length === 2);
});

test("analyzeRSI: 약세 다이버전스(가격 HH·RSI LH) → bearish", () => {
  const seg = (from, to, n) => Array.from({ length: n }, (_, i) => from + (to - from) * (i + 1) / n);
  const price = [100, ...seg(100, 130, 10), ...seg(130, 115, 6), ...seg(115, 134, 8)];  // 고점1≈130, 조정115, 고점2≈134(HH)
  const r = ForgeCore.analyzeRSI(price, { period: 5, swing: 0.05 });
  assert.strictEqual(r.divergence.type, "bearish");
});

test("analyzeRSI: 소량 → 폴백(divergence null, bias 유한)", () => {
  const r = ForgeCore.analyzeRSI([10, 11], {});
  assert.ok(isFinite(r.bias));
  assert.strictEqual(r.divergence.type, null);
});

test("rsiSteps: 5단계, bias 반영", () => {
  const r = ForgeCore.analyzeRSI(Array.from({ length: 40 }, (_, i) => 100 + i * 2), { period: 14 });
  const s = ForgeCore.rsiSteps(r);
  assert.strictEqual(s.length, 5);
  assert.ok(/bias/.test(s[4]));
});
```

> 다이버전스 합성 데이터가 `detectSwings`/RSI와 어긋나 type이 null이면 **테스트 데이터(세그먼트 값·길이·swing)만** 조정해 의도한 다이버전스를 만든다(단언·로직 불변). 보고에 명시.

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test forge-core.test.js`
Expected: FAIL — `ForgeCore.analyzeRSI is not a function`

- [ ] **Step 3: 구현** — `forge-core.js`의 `rsiSeries` 함수(`:293`) **다음**에 삽입

```js
  function analyzeRSI(price, opts) {
    opts = opts || {};
    const period = opts.period || 14, swing = opts.swing != null ? opts.swing : 0.03;
    const P = price.length;
    const EMPTY = { series: [], last: 50, zone: "neutral", trend: 0, cross50: "above", divergence: { type: null, pricePts: null }, bias: 0 };
    if (P < 2) return EMPTY;
    const series = new Array(P); let avgG = 0, avgL = 0;
    for (let i = 0; i < P; i++) {
      if (i === 0) { series[i] = 50; continue; }
      const ch = price[i] - price[i - 1], g = Math.max(0, ch), l = Math.max(0, -ch);
      if (i <= period) { avgG += g / period; avgL += l / period; }
      else { avgG = (avgG * (period - 1) + g) / period; avgL = (avgL * (period - 1) + l) / period; }
      const rs = avgL === 0 ? 100 : avgG / avgL;
      series[i] = 100 - 100 / (1 + rs);
    }
    const last = series[P - 1];
    const zone = last >= 70 ? "overbought" : last <= 30 ? "oversold" : "neutral";
    const w = Math.max(2, Math.min(period, P - 1)), seg = series.slice(P - w), f = linfit(seg);
    const trend = Math.max(-1, Math.min(1, Math.tanh(f.b * 0.5)));
    const prev = series[P - 2];
    let cross50 = last >= 50 ? "above" : "below";
    if (prev < 50 && last >= 50) cross50 = "cross_up";
    else if (prev >= 50 && last < 50) cross50 = "cross_down";
    const sw = detectSwings(price, swing), pts = sw.map(p => ({ idx: p.idx, price: p.price }));
    const lows = [], highs = [];
    for (let i = 0; i < pts.length; i++) {
      const pr = pts[i - 1], nx = pts[i + 1], pv = pts[i].price;
      const isHigh = (pr && nx) ? (pv >= pr.price && pv >= nx.price) : (nx ? pv >= nx.price : (pr ? pv >= pr.price : true));
      (isHigh ? highs : lows).push(pts[i]);
    }
    const rsiAt = idx => series[Math.max(0, Math.min(P - 1, idx))];
    let divergence = { type: null, pricePts: null };
    if (lows.length >= 2) { const a = lows[lows.length - 2], b = lows[lows.length - 1]; if (b.price < a.price && rsiAt(b.idx) > rsiAt(a.idx)) divergence = { type: "bullish", pricePts: [a, b] }; }
    if (!divergence.type && highs.length >= 2) { const a = highs[highs.length - 2], b = highs[highs.length - 1]; if (b.price > a.price && rsiAt(b.idx) < rsiAt(a.idx)) divergence = { type: "bearish", pricePts: [a, b] }; }
    const divDir = divergence.type === "bullish" ? 1 : divergence.type === "bearish" ? -1 : 0;
    const zoneDir = zone === "oversold" ? 0.5 : zone === "overbought" ? -0.5 : 0;
    const crossDir = cross50 === "cross_up" ? 0.3 : cross50 === "cross_down" ? -0.3 : 0;
    const bias = Math.max(-1, Math.min(1, 0.5 * divDir + zoneDir + 0.3 * crossDir));
    return { series, last, zone, trend, cross50, divergence, bias };
  }

  function rsiSteps(rsi) {
    const zTxt = rsi.zone === "overbought" ? "과열" : rsi.zone === "oversold" ? "과매도" : "중립";
    const c50 = (rsi.cross50 === "above" || rsi.cross50 === "cross_up") ? "50선 위" : "50선 아래";
    const tTxt = rsi.trend > 0.1 ? "상승" : rsi.trend < -0.1 ? "하락" : "횡보";
    const dv = rsi.divergence.type === "bullish" ? "강세 다이버전스" : rsi.divergence.type === "bearish" ? "약세 다이버전스" : "다이버전스 없음";
    const bTxt = rsi.bias > 0.1 ? "상승" : rsi.bias < -0.1 ? "하락" : "중립";
    return [
      "RSI " + Math.round(rsi.last) + " · " + zTxt,
      c50 + " · 추세 " + tTxt,
      dv,
      "RSI 오실레이터 갱신",
      "종합 방향 " + bTxt + " (bias " + rsi.bias.toFixed(2) + ")"
    ];
  }
```

- [ ] **Step 4: export 추가** — `forge-core.js:775` return 객체 끝에 `analyzeRSI, rsiSteps` 삽입

```js
  return { version, makeDemoSeries, buildDAG, evalBlocks, detrendNorm, pdmTheta, scanPeriod, run, runSteps, visionBiasFrom, sampleSeries, sampleGraph, analyzeTrend, trendProfileForTF, analyzeMA, maSteps, analyzeFib, fibSteps, analyzeElliott, elliottSteps, analyzeRSI, rsiSteps };
```

- [ ] **Step 5: 테스트 통과** — `node --test forge-core.test.js` → PASS(신규 5개 포함). 다이버전스 데이터 빡빡하면 데이터만 조정.

- [ ] **Step 6: 커밋**

```bash
git add forge-core.js forge-core.test.js
git commit -m "feat(forge): ForgeCore.analyzeRSI+rsiSteps — 다이버전스·구간·50선·추세·bias"
```

---

### Task 2: 예측 `run` — RSI 드리프트(TF 가중) + 테스트

**Files:** Modify `forge-core.js` (`run` 내: ewDrift 줄(`:674`) 다음; `m` 줄(`:686`)). Test `forge-core.test.js`.

**Interfaces:** Consumes `analyzeRSI`(Task 1), `trendProfileForTF`. `run` 스코프 `price`, `graph`, `_prof`, `k`, `futW`, `ewDrift`.

- [ ] **Step 1: 실패 테스트 작성** — `forge-core.test.js` 끝에 추가

```js
test("run: RSI 블록 유무가 예측 타깃을 가른다(격리) + TF", () => {
  const seg = (from, to, n) => Array.from({ length: n }, (_, i) => from + (to - from) * (i + 1) / n);
  const data = { price: [100, ...seg(100, 70, 10), ...seg(70, 85, 6), ...seg(85, 68, 8)] };  // 강세 다이버전스
  const base = [{ id: "p", kind: "block", blockType: "price" }, { id: "o", kind: "block", blockType: "predict" }];
  const withR = { nodes: [...base, { id: "r", kind: "block", blockType: "rsi", params: { period: 5 } }], edges: [{ from: "p", to: "o" }, { from: "p", to: "r" }] };
  const without = { nodes: base, edges: [{ from: "p", to: "o" }] };
  const rW = ForgeCore.run(withR, data, { futW: 12, timeframe: "월봉" });
  const rN = ForgeCore.run(without, data, { futW: 12, timeframe: "월봉" });
  const rI = ForgeCore.run(withR, data, { futW: 12, timeframe: "5분" });
  assert.ok(rW.prediction.path.every(isFinite) && rN.prediction.path.every(isFinite) && rI.prediction.path.every(isFinite));
  assert.notStrictEqual(rW.prediction.target, rN.prediction.target);
  const gain = r => r.prediction.target / r.prediction.anchor;
  assert.ok(Math.abs(gain(rW) - 1) > Math.abs(gain(rI) - 1) - 1e-9);   // 월봉 가중 ≥ 5분(부호 무관 크기)
});

test("run: RSI 블록 없으면 기여 0", () => {
  const G = { nodes: [{ id: "p", kind: "block", blockType: "price" }, { id: "o", kind: "block", blockType: "predict" }], edges: [{ from: "p", to: "o" }] };
  const r = ForgeCore.run(G, { price: Array.from({ length: 40 }, (_, i) => 100 + i) }, { futW: 8, timeframe: "월봉" });
  assert.ok(r.prediction.path.every(isFinite));
});
```

> `notStrictEqual` 실패(RSI bias 0)면 강세 다이버전스가 잡히도록 데이터만 조정.

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test forge-core.test.js`
Expected: FAIL — `notStrictEqual`(RSI 미반영 → 두 타깃 동일)

- [ ] **Step 3: RSI 드리프트 산출** — `forge-core.js`의 `const ewDrift = ...` 줄(`:674`) **다음**에 삽입

```js
    const _rn = (graph.nodes || []).find(nd => nd.kind === "block" && nd.blockType === "rsi");
    const _rsi = _rn ? analyzeRSI(price, { period: (_rn.params && _rn.params.period) || 14 }) : null;
    const rsiDrift = _rsi ? _rsi.bias * _prof.trendScale * 0.06 : 0;   // RSI 다이버전스/구간 방향 드리프트(±6%·TF가중·보수적)
```

- [ ] **Step 4: 예측 루프 합산** — `forge-core.js:686`의 `const m = ... + ewDrift * (k / futW), sd = ...` 줄을 교체

```js
      const m = rev + mom + trend + sig + seas + maDrift * (k / futW) + fibDrift * (k / futW) + ewDrift * (k / futW) + rsiDrift * (k / futW), sd = Math.sqrt(sigBand * sigBand + 0.36 * trChSig * trChSig) * Math.sqrt(k) * 0.85;
```

- [ ] **Step 5: 테스트 통과** — `node --test forge-core.test.js` → PASS.

- [ ] **Step 6: 커밋**

```bash
git add forge-core.js forge-core.test.js
git commit -m "feat(forge): 예측 run에 RSI 방향 드리프트(analyzeRSI.bias·TF가중) 보조항"
```

---

### Task 3: hero 작도 `_drawRsiLayers` + 차트·오버레이 분기 + 시연 단계

**Files:** Modify `forge.html` (헬퍼는 `_drawElliottLayers`(`:2635`) 근처; RSI 분기 차트 `:2719`·오버레이 `:2759`; `analysisSteps` elliott 케이스(`:3120`) 다음).

**Interfaces:** Consumes `ForgeCore.analyzeRSI`·`rsiSteps`(Task 1), `_evLabel`. Produces `_drawRsiLayers(c, rsi, M)`.

- [ ] **Step 1: 공용 헬퍼 추가** — `_drawElliottLayers` 근처에 삽입

```js
  // RSI hero 작도(차트·오버레이, reveal). 다이버전스 선 + 값/구간 배지.
  function _drawRsiLayers(c, rsi, M) {
    c.save();
    const { fiToX, pToY, nowFi, fiMin = 0, reveal = Infinity, xRight } = M;
    if (reveal >= 1 && rsi.divergence.type && rsi.divergence.pricePts) {
      const a = rsi.divergence.pricePts[0], b = rsi.divergence.pricePts[1];
      const xa = fiToX(Math.max(fiMin, a.idx)), ya = pToY(a.price), xb = fiToX(Math.max(fiMin, b.idx)), yb = pToY(b.price);
      if ([xa, ya, xb, yb].every(isFinite)) {
        const col = rsi.divergence.type === "bullish" ? "#46c28e" : "#e06a6a";
        c.strokeStyle = col; c.lineWidth = 1.8; c.setLineDash([5, 4]); c.beginPath(); c.moveTo(xa, ya); c.lineTo(xb, yb); c.stroke(); c.setLineDash([]);
        _evLabel(c, (rsi.divergence.type === "bullish" ? "강세" : "약세") + " 다이버전스", xb, yb, col, "left");
      }
    }
    if (reveal >= 2) {
      const zt = rsi.zone === "overbought" ? "과열" : rsi.zone === "oversold" ? "과매도" : "중립";
      const col = rsi.zone === "overbought" ? "#e06a6a" : rsi.zone === "oversold" ? "#46c28e" : "#8a92b2";
      const xb = (xRight != null ? xRight : fiToX(nowFi));
      _evLabel(c, "RSI " + Math.round(rsi.last) + " · " + zt, xb, 28, col, "right");
    }
    c.restore();
  }
```

- [ ] **Step 2: 차트 RSI 분기 교체** — `forge.html:2719` 의 차트(`toXh`/`toY` 스코프) RSI 분기 내부를 아래로 교체

```js
        } else if (n.blockType === "rsi") {
          const rsi = ForgeCore.analyzeRSI(price, { period: (n.params && n.params.period) || 14 });
          _drawRsiLayers(c, rsi, { fiToX: fi => toXh(Math.max(0, Math.min(Hn - 1, fi - off))), pToY: v => toY(v), nowFi: P - 1, fiMin: off, reveal: _playing ? (_evReveal[n.id] || 0) : Infinity, xRight: g.padX + g.plotW });
          legend.push({ col, t: EV_LABEL.rsi + "(전문)" });
```

- [ ] **Step 3: 오버레이 RSI 분기 교체** — `forge.html:2759` 의 오버레이(`xOf`/`yOf`/`clipY` 스코프) RSI 분기 내부를 아래로 교체

```js
        } else if (n.blockType === "rsi") {
          const rsi = ForgeCore.analyzeRSI(price, { period: (n.params && n.params.period) || 14 });
          _drawRsiLayers(c, rsi, { fiToX: fi => xOf(fi), pToY: v => clipY(yOf(v)), nowFi: P - 1, fiMin: 0, reveal: _playing ? (_evReveal[n.id] || 0) : Infinity, xRight: g.rightX || (g.ox + g.dw) });
          legend.push({ col, t: EV_LABEL.rsi + "(전문)" });
```

- [ ] **Step 4: `analysisSteps` RSI 케이스 추가** — `analysisSteps`의 elliott 케이스(`:3120`) **다음**에 삽입

```js
    if (n.blockType === "rsi" && Array.isArray(price) && price.length >= 2) {
      const rsi = ForgeCore.analyzeRSI(price, { period: (n.params && n.params.period) || 14 });
      const texts = ForgeCore.rsiSteps(rsi), layers = [1, 1, 1, 2, 2];
      return texts.map((t, i) => ({ text: t, layer: layers[i] }));
    }
```

- [ ] **Step 5: 검증** — 인라인 `<script>` `node --check` 무오류. `git diff <base> HEAD -- forge.html`로 따옴표 부수변경 0 확인. 보고 기재. (legend-only 구 코드 완전 교체 확인.)

- [ ] **Step 6: 커밋**

```bash
git add forge.html
git commit -m "feat(forge): hero RSI 작도(_drawRsiLayers·다이버전스선·구간배지) + analysisSteps RSI"
```

---

### Task 4: RSI 오실레이터 서브패널 (마크업 + fcDrawRsi + toggle + renderChart 훅)

**Files:** Modify `forge.html` (패널 마크업 — `fcFoldPanel`(`:568`) 다음; `fcDrawRsi` — `fcDrawFold`(`:2082`) 근처; `toggleRsiPanel` — `togglePhasefoldPanels`(`:3317`) 근처; renderChart 훅 — `togglePhasefoldPanels()` 호출부(`:2985`)).

**Interfaces:** Consumes `ForgeCore.analyzeRSI`, `fcFit`, `FC_DIM`. Produces `fcDrawRsi(rsi)`, `toggleRsiPanel()`, `#fcRsiPanel`.

- [ ] **Step 1: 패널 마크업 추가** — `fcFoldPanel` `<div>`의 **닫는 `</div>` 다음**(결과 패널 `.fc-wrap` 내부 마지막 패널 뒤)에 삽입

```html
        <div class="fc-rgutter" title="드래그하여 패널 높이 조절 · 더블클릭 초기화"></div>
        <div class="fc-panel" id="fcRsiPanel" style="display:none">
          <div class="fc-phead">
            <span class="fc-t"><b>RSI 오실레이터</b> · 0~100 · 30/70</span>
            <span class="fc-meta" id="fcRsiMeta">–</span>
          </div>
          <div class="fc-pbody"><canvas id="fcRsi" class="fc-cv"></canvas></div>
        </div>
```

- [ ] **Step 2: `fcDrawRsi` 추가** — `fcDrawFold` 함수(`:2082`) 근처에 삽입

```js
  function fcDrawRsi(rsi) {
    const cv = document.getElementById("fcRsi"); if (!cv) return;
    const ch = cv.clientHeight || 120, c = fcFit(cv, ch), cw = cv.clientWidth || 400;
    c.clearRect(0, 0, cw, ch);
    const s = (rsi && rsi.series) || [];
    if (s.length < 2) { c.fillStyle = FC_DIM; c.font = "11px ui-monospace,monospace"; c.textAlign = "center"; c.fillText("RSI 데이터 없음", cw / 2, ch / 2); c.textAlign = "left"; return; }
    const padL = 6, padR = 28, padV = 8, plotW = cw - padL - padR, plotH = ch - 2 * padV;
    const yOf = v => padV + (1 - v / 100) * plotH, xOf = i => padL + (i / (s.length - 1)) * plotW;
    c.fillStyle = "rgba(224,106,106,.10)"; c.fillRect(padL, yOf(100), plotW, yOf(70) - yOf(100));
    c.fillStyle = "rgba(70,194,142,.10)"; c.fillRect(padL, yOf(30), plotW, yOf(0) - yOf(30));
    c.strokeStyle = "#2b3647"; c.lineWidth = 1; c.setLineDash([3, 3]);
    [30, 50, 70].forEach(lv => { const y = yOf(lv); c.beginPath(); c.moveTo(padL, y); c.lineTo(padL + plotW, y); c.stroke(); c.fillStyle = "#8a92b2"; c.font = "9px ui-monospace,monospace"; c.fillText(lv, padL + plotW + 3, y + 3); });
    c.setLineDash([]);
    c.strokeStyle = "#e8b463"; c.lineWidth = 1.8; c.beginPath();
    s.forEach((v, i) => { const x = xOf(i), y = yOf(v); i ? c.lineTo(x, y) : c.moveTo(x, y); }); c.stroke();
    const lx = xOf(s.length - 1), ly = yOf(rsi.last);
    c.fillStyle = "#ffd24d"; c.beginPath(); c.arc(lx, ly, 3, 0, 7); c.fill();
    if (rsi.divergence && rsi.divergence.pricePts) {
      const col = rsi.divergence.type === "bullish" ? "#46c28e" : "#e06a6a";
      rsi.divergence.pricePts.forEach(p => { const i = p.idx; if (i < 0 || i >= s.length) return; const x = xOf(i), y = yOf(s[i]); c.fillStyle = col; c.beginPath(); c.arc(x, y, 3, 0, 7); c.fill(); });
    }
    const meta = document.getElementById("fcRsiMeta"); if (meta) meta.textContent = "RSI " + Math.round(rsi.last) + " · " + (rsi.zone === "overbought" ? "과열" : rsi.zone === "oversold" ? "과매도" : "중립");
  }
```

- [ ] **Step 3: `toggleRsiPanel` 추가** — `togglePhasefoldPanels`(`:3317`) 함수 **다음**에 삽입

```js
  function toggleRsiPanel() {
    const has = boardState.nodes.some(n => n.blockType === "rsi");
    const p = document.getElementById("fcRsiPanel"); if (p) p.style.display = has ? "" : "none";
  }
```

- [ ] **Step 4: renderChart 훅** — `forge.html:2985` 의 `togglePhasefoldPanels();` 줄 **다음**에 삽입

```js
    toggleRsiPanel();
    { const _rn = boardState.nodes.find(n => n.blockType === "rsi"); if (_rn) fcDrawRsi(ForgeCore.analyzeRSI((data && data.price) || [], { period: (_rn.params && _rn.params.period) || 14 })); }
```

- [ ] **Step 5: 검증** — 인라인 `<script>` `node --check` 무오류. `git diff <base> HEAD -- forge.html`로 따옴표 부수변경 0 확인. RSI 블록 없을 때 패널 숨김(마크업 `display:none` 기본) 확인. 보고 기재.

- [ ] **Step 6: 커밋**

```bash
git add forge.html
git commit -m "feat(forge): RSI 오실레이터 서브패널(fcDrawRsi·30/70·다이버전스 마커, RSI 블록시 표시)"
```

---

## Self-Review

**Spec coverage:**
- §4 `analyzeRSI`(series·zone·trend·cross50·divergence·bias) → Task 1 + 테스트 ✅
- §5 예측(rsiDrift 0.06·단일경로·TF·없으면 0) → Task 2 + 격리 테스트 ✅
- §6 hero 작도(다이버전스선1·배지2·reveal) → Task 3 ✅ (차트·오버레이 둘 다)
- §7 오실레이터 서브패널(fcDrawRsi·30/70·50선·다이버전스 마커·RSI블록시 표시) → Task 4 ✅
- §8 시연(`rsiSteps` 5단계 + `analysisSteps` RSI) → Task 1(rsiSteps)·Task 3(analysisSteps) ✅
- §9 테스트 → Task 1·2 ✅. 편집기 period 기존 유지(변경 불요).

**Placeholder scan:** 코드/명령 구체값. 다이버전스 테스트 데이터 튜닝 여지는 단언 불변으로 한정. 작도/패널은 단위테스트 불가 → 구문검사+라이브 시각검증.

**Type consistency:** `analyzeRSI` 반환(`series`/`last`/`zone`/`trend`/`cross50`/`divergence{type,pricePts}`/`bias`)을 Task 2(`_rsi.bias`)·Task 3(`rsi.divergence`,`rsi.zone`,`rsi.last`)·Task 4(`rsi.series`,`rsi.divergence.pricePts`,`rsi.zone`)·`rsiSteps`에서 동일 사용. `_drawRsiLayers` M·`_evLabel`·`xRight`는 기존 helper와 동일. period 정수 그대로(다른 도구의 swing/100과 달리 RSI period는 변환 없음).
