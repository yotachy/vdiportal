# 스쿱포지 인증(Google OAuth + 사용자별 데이터) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Google OAuth 로그인 + HMAC 쿠키 세션 + 사용자별 문서/이미지 분리 + 비로그인 체험 모드를 구현한다(스펙 `2026-07-13-auth-design.md`).

**Architecture:** `forge-auth-lib.php`(쿠키 검증 공유) + `forge-auth.php`(login/cb/me/logout) 신규, `forge-api.php`가 쿠키로 사용자 파일 분기. **무중단 스위치**: `forge_google_oauth.json` 미존재 시 인증 게이트 전체 비활성(종전 전역 문서 동작) — GCP 키 업로드 순간 활성화. 클라이언트는 `?me=1`로 모드 결정, 게스트는 저장 계열 no-op + 서버 401(이중 방어).

**Tech Stack:** PHP 8.4(cafe24)·바닐라 JS. 외부 라이브러리 없음. Google OAuth2 authorization code flow(cURL).

## Global Constraints

- 서버 생성/사용자 데이터 불가침: `forge_data*.json`·`forge_images*.json`·`forge_predlog.json`·키 파일들 — 업로드/git 금지. 신규 gitignore: `forge_google_oauth.json`·`forge_auth_secret.txt`·`forge_admin.txt`·`forge_data_*.json`·`forge_images_*.json`.
- 인증 검증 fail-closed(변조=거부). 단 **인증 미설정(`$AUTH_ENABLED=false`)이면 종전 동작 그대로**(회귀 0).
- `forge_predlog.json`·OHLC/실적 캐시·`forge_jobs.json`은 전역 유지. `?ohlc`/`?earndate`/`?predledger`/`?jobs`는 비로그인 허용.
- 게이팅 없음(체험 모드도 전체 분석 기능 동작 — 저장·워치리스트 영속·logpred만 로그인).
- 배포 4파일: forge-auth-lib.php(신규)·forge-auth.php(신규)·forge-api.php·forge-app.js(+state/ui/html 캐시버스터).

---

### Task 1: forge-auth-lib.php + forge-auth.php

**Files:** Create `map/forge-auth-lib.php`, `map/forge-auth.php` · Modify 루트 `.gitignore`

**Interfaces (Produces):** `fauth_enabled(): bool` · `fauth_email(): ?string` · `fauth_uid(string $email): string` · `fauth_issue(string $email): void`(쿠키 설정) · `fauth_clear(): void` · `fauth_admin(): ?string`

- [ ] **Step 1: forge-auth-lib.php 작성**

```php
<?php
// 스쿱포지 인증 공유 라이브러리 — HMAC 서명 쿠키(무상태). forge-auth.php·forge-api.php 공용.
// fauth 쿠키 = b64url(email) . "|" . exp . "|" . b64url(HMAC-SHA256(email."|".exp, secret))
// 인증 스위치: forge_google_oauth.json 존재 = 활성. 미존재 = 전 게이트 비활성(종전 동작·무중단 전환).
function _fb64e($s){ return rtrim(strtr(base64_encode($s), "+/", "-_"), "="); }
function _fb64d($s){ return base64_decode(strtr($s, "-_", "+/")); }
function fauth_oauth_conf(){ $f = __DIR__ . "/forge_google_oauth.json"; if (!is_file($f)) return null;
  $j = json_decode(file_get_contents($f), true);
  return (is_array($j) && !empty($j["client_id"]) && !empty($j["client_secret"])) ? $j : null; }
function fauth_enabled(){ return fauth_oauth_conf() !== null; }
function fauth_secret(){ $f = __DIR__ . "/forge_auth_secret.txt";
  if (!is_file($f)) { $s = bin2hex(random_bytes(32)); @file_put_contents($f, $s, LOCK_EX); @chmod($f, 0600); }
  return trim((string)@file_get_contents($f)); }
function fauth_sign($email, $exp){ return _fb64e(hash_hmac("sha256", $email . "|" . $exp, fauth_secret(), true)); }
function fauth_email(){
  if (!fauth_enabled()) return null;
  $c = isset($_COOKIE["fauth"]) ? $_COOKIE["fauth"] : ""; if ($c === "") return null;
  $p = explode("|", $c); if (count($p) !== 3) return null;
  $email = _fb64d($p[0]); $exp = (int)$p[1];
  if (!$email || $exp < time()) return null;
  if (!hash_equals(fauth_sign($email, $exp), $p[2])) return null;   // fail-closed
  return strtolower($email);
}
function fauth_uid($email){ return substr(sha1(strtolower($email)), 0, 16); }
function fauth_issue($email){ $exp = time() + 30 * 86400;
  setcookie("fauth", _fb64e($email) . "|" . $exp . "|" . fauth_sign($email, $exp),
    ["expires" => $exp, "path" => "/map/", "secure" => true, "httponly" => true, "samesite" => "Lax"]); }
function fauth_clear(){ setcookie("fauth", "", ["expires" => time() - 3600, "path" => "/map/", "secure" => true, "httponly" => true, "samesite" => "Lax"]); }
function fauth_admin(){ $f = __DIR__ . "/forge_admin.txt"; return is_file($f) ? strtolower(trim((string)file_get_contents($f))) : null; }
```

- [ ] **Step 2: forge-auth.php 작성**

```php
<?php
// 스쿱포지 인증 엔드포인트 — Google OAuth(authorization code). ?login=1 / 콜백(?code=) / ?me=1 / ?logout=1
require __DIR__ . "/forge-auth-lib.php";
function aout($a){ header("Content-Type: application/json; charset=utf-8"); echo json_encode($a, JSON_UNESCAPED_UNICODE); exit; }
$SELF = "https://" . $_SERVER["HTTP_HOST"] . strtok($_SERVER["REQUEST_URI"], "?");
$HOME = dirname($SELF) . "/forge.html";

if (isset($_GET["me"])) {
  if (!fauth_enabled()) aout(["ok" => false, "enabled" => false]);
  $e = fauth_email();
  aout($e ? ["ok" => true, "enabled" => true, "email" => $e] : ["ok" => false, "enabled" => true]);
}
if (isset($_GET["logout"])) { fauth_clear(); header("Location: " . $HOME); exit; }

$conf = fauth_oauth_conf();
if (!$conf) aout(["ok" => false, "error" => "oauth_unset", "hint" => "forge_google_oauth.json({client_id,client_secret}) 서버 업로드 필요"]);

if (isset($_GET["login"])) {
  $st = time() . "." . bin2hex(random_bytes(8));
  setcookie("fauth_st", $st . "|" . fauth_sign("st:" . $st, time() + 600), ["expires" => time() + 600, "path" => "/map/", "secure" => true, "httponly" => true, "samesite" => "Lax"]);
  $q = http_build_query(["client_id" => $conf["client_id"], "redirect_uri" => $SELF, "response_type" => "code",
    "scope" => "openid email", "state" => $st, "prompt" => "select_account"]);
  header("Location: https://accounts.google.com/o/oauth2/v2/auth?" . $q); exit;
}
if (isset($_GET["code"])) {
  // state 검증(CSRF) — 쿠키의 서명·만료 확인 후 파라미터와 대조
  $ok = false;
  if (isset($_COOKIE["fauth_st"], $_GET["state"])) { $p = explode("|", $_COOKIE["fauth_st"]);
    if (count($p) === 2 && hash_equals(fauth_sign("st:" . $p[0], 0) === "" ? "x" : fauth_sign("st:" . $p[0], (int)explode(".", $p[0])[0] + 600), $p[1]) === false) { /* 서명식이 exp 포함이라 아래 재계산 */ } }
  if (isset($_COOKIE["fauth_st"], $_GET["state"])) { $p = explode("|", $_COOKIE["fauth_st"]);
    if (count($p) === 2 && $p[0] === $_GET["state"]) {
      $exp0 = (int)explode(".", $p[0])[0] + 600;
      if ($exp0 > time() && hash_equals(fauth_sign("st:" . $p[0], $exp0), $p[1])) $ok = true; } }
  if (!$ok) { header("Location: " . $HOME . "?login=fail"); exit; }
  $ch = curl_init("https://oauth2.googleapis.com/token");
  curl_setopt_array($ch, [CURLOPT_POST => true, CURLOPT_RETURNTRANSFER => true, CURLOPT_TIMEOUT => 12,
    CURLOPT_POSTFIELDS => http_build_query(["code" => $_GET["code"], "client_id" => $conf["client_id"],
      "client_secret" => $conf["client_secret"], "redirect_uri" => $SELF, "grant_type" => "authorization_code"])]);
  $raw = curl_exec($ch); curl_close($ch);
  $tok = json_decode((string)$raw, true);
  $email = null;
  if (is_array($tok) && !empty($tok["id_token"])) {   // id_token payload(서버가 구글에서 직접 받았으므로 서명 재검증 불요)
    $seg = explode(".", $tok["id_token"]);
    if (count($seg) === 3) { $pl = json_decode(_fb64d($seg[1]), true);
      if (is_array($pl) && !empty($pl["email"]) && !empty($pl["email_verified"])) $email = strtolower($pl["email"]); } }
  if (!$email && is_array($tok) && !empty($tok["access_token"])) {   // 폴백: userinfo
    $ch = curl_init("https://openidconnect.googleapis.com/v1/userinfo");
    curl_setopt_array($ch, [CURLOPT_RETURNTRANSFER => true, CURLOPT_TIMEOUT => 12, CURLOPT_HTTPHEADER => ["Authorization: Bearer " . $tok["access_token"]]]);
    $ui = json_decode((string)curl_exec($ch), true); curl_close($ch);
    if (is_array($ui) && !empty($ui["email"])) $email = strtolower($ui["email"]);
  }
  if (!$email) { header("Location: " . $HOME . "?login=fail"); exit; }
  fauth_issue($email); fauth_clear_state();
  header("Location: " . $HOME . "?login=ok"); exit;
}
function fauth_clear_state(){ setcookie("fauth_st", "", ["expires" => time() - 3600, "path" => "/map/", "secure" => true, "httponly" => true, "samesite" => "Lax"]); }
aout(["ok" => false, "error" => "noop"]);
```

(주: state 검증 블록의 첫 불완전 if는 작성 시 제거하고 두 번째 블록만 남긴다 — 실행 단계에서 정리.)

- [ ] **Step 3: 루트 .gitignore에 추가**

```
map/forge_google_oauth.json
map/forge_auth_secret.txt
map/forge_admin.txt
map/forge_data_*.json
map/forge_images_*.json
```

- [ ] **Step 4: 검증** — `node`로 두 PHP 브레이스/괄호 균형 0 확인. 배포 후 라이브: `curl ?me=1` → `{"ok":false,"enabled":false}`(키 미업로드 상태), `curl ?login=1` → oauth_unset JSON.
- [ ] **Step 5: Commit** `feat(forge): 인증 엔드포인트 — Google OAuth·HMAC 쿠키(키 미업로드 시 비활성)`

### Task 2: forge-api.php 사용자별 분기

**Files:** Modify `map/forge-api.php`

**Interfaces (Consumes):** `fauth_email()`·`fauth_uid()`·`fauth_admin()`·`fauth_enabled()` (Task 1)

- [ ] **Step 1: 상단 통합** — `$f` 정의(11행) 직후에:

```php
require __DIR__ . "/forge-auth-lib.php";
$AUTH_ON = fauth_enabled();               // 인증 스위치(키 미업로드 = 종전 전역 문서 동작)
$AUTH_EMAIL = $AUTH_ON ? fauth_email() : null;
$UID = $AUTH_EMAIL ? fauth_uid($AUTH_EMAIL) : null;
$IMGF = __DIR__ . "/forge_images.json";   // 이미지 경로도 변수화(putimg·?images 공용)
if ($AUTH_ON && $UID) {
  $uf = __DIR__ . "/forge_data_" . $UID . ".json";
  $uimg = __DIR__ . "/forge_images_" . $UID . ".json";
  // 레거시 1회 이관: admin 첫 로그인 시 기존 전역 문서·이미지를 계정 파일로 복사(원본 보존)
  if ($AUTH_EMAIL === fauth_admin()) {
    if (!is_file($uf) && is_file($f)) @copy($f, $uf);
    if (!is_file($uimg) && is_file($IMGF)) @copy($IMGF, $uimg);
  }
  $f = $uf; $IMGF = $uimg;
}
```

- [ ] **Step 2: GET 게이트** — `?images` 분기를 `$IMGF` 사용 + 비로그인(`$AUTH_ON && !$UID`)이면 `{}`. 기본 문서 GET(`if (is_file($f)) readfile...`) 앞에 `if ($AUTH_ON && !$UID) { echo "null"; exit; }`. `?check`/`?jobs`/`?ohlc`/`?earndate`/`?predledger`는 무변경(공용).
- [ ] **Step 3: POST 게이트** — `$op` 확정 직후: `if ($AUTH_ON && !$UID && in_array($op, ["replace","upsert","delete","reorder","meta","putimg","logpred"], true)) { http_response_code(401); jout(["ok"=>false,"error"=>"login"]); }`. putimg 내부 `$imgf` → `$IMGF`로 교체. jobs 계열(enqueue/claim/result)은 무변경(비전 워커 경로).
- [ ] **Step 4: 검증** — 브레이스 균형 델타 0 · 배포 후 라이브: 비로그인 상태에서 문서 GET이 **기존 문서 그대로**(키 미업로드=스위치 꺼짐) + `?ohlc` 정상 = 회귀 0 확인.
- [ ] **Step 5: Commit** `feat(forge): api 사용자별 문서/이미지 분기 — 인증 스위치·admin 1회 이관·게스트 401`

### Task 3: 클라이언트 — AUTH 부팅·체험 모드·헤더

**Files:** Modify `map/forge-state.js`(AUTH·저장 게이트·boot), `map/forge-app.js`(authSoon·logPrediction·scanAlerts 게이트·복귀 토스트), `map/forge.html`(캐시버스터), `map/forge.css`(로그인 뱃지 소폭)

- [ ] **Step 1: forge-state.js** — `SERVER_OK` 선언부에 `let AUTH = { checked: false, enabled: false, on: false, email: null };` + `async function fetchAuth() { try { const r = await fetch("forge-auth.php?me=1", { cache: "no-store" }); const j = await r.json(); AUTH = { checked: true, enabled: !!j.enabled, on: !!j.ok, email: j.email || null }; } catch (e) { AUTH = { checked: true, enabled: false, on: false, email: null }; } }` · `function canSave() { return !AUTH.enabled || AUTH.on; }`(스위치 꺼짐=종전 동작).
- [ ] **Step 2: 저장 게이트** — `markDirty`/`writeBackActive`/`saveMeta`/`putImg` 선두에 `if (!canSave()) { guestNudge(); return; }`(putImg는 IMAGES 메모리 등록은 유지). `let _nudged = false; function guestNudge() { if (_nudged) return; _nudged = true; if (typeof bToast === "function") bToast("체험 모드 — 로그인하면 포지가 저장됩니다"); }` · `setSaveState`에 게스트 분기: `AUTH.enabled && !AUTH.on` 이면 배지 상시 `"● 체험 모드 · 저장 안 됨"`(class `save-badge guest`).
- [ ] **Step 3: boot 경로** — `boot()` 첫 줄에 `await fetchAuth();`. 게스트면 서버 GET이 null → 기존 "빈 부팅=샘플 시드" 경로가 그대로 체험 모드가 됨(추가 코드 최소). boot 끝 `setSaveState` 게스트 분기 반영.
- [ ] **Step 4: forge-app.js** — `authSoon()` 교체: 로그인 상태면 무동작, 아니면 `AUTH.enabled ? location.href="forge-auth.php?login=1" : bToast("로그인 준비 중 — 서버 OAuth 설정 대기")`. 헤더 갱신 `updateAuthUI()`: 로그인 시 `.auth-btn` 내용은 `email 앞부분 · 로그아웃`(클릭=`forge-auth.php?logout=1`). boot 후 호출. `logPrediction`·`scanAlerts` 선두에 `if (typeof AUTH !== "undefined" && AUTH.enabled && !AUTH.on) return;`(원장 오염 방지·게스트 스캔 무의미). 복귀 처리: 부팅 IIFE에서 `location.search`의 `login=ok`→토스트 "로그인됨"·`login=fail`→"로그인 실패" 후 `history.replaceState`로 쿼리 제거.
- [ ] **Step 5: CSS** — `.save-badge.guest{color:var(--gold)}` `.auth-btn .au-mail{font-size:11px;color:var(--eth)}` 수준 최소.
- [ ] **Step 6: 검증** — `new Function` 파스(state/app) · 헤드리스: ①스위치 꺼짐 mock(`{ok:false,enabled:false}`) → 종전 동작(저장 배지 평소) ②게스트 mock(`enabled:true,ok:false`) → 체험 배지·markDirty no-op·토스트 1회 ③로그인 mock → 헤더 이메일 표시. 캐시버스터 `20260713e`.
- [ ] **Step 7: Commit** `feat(forge): 클라 인증 — 체험 모드(저장 게이트·샘플 시드)·헤더 로그인/로그아웃·복귀 토스트`

### Task 4: 배포·라이브 검증·문서

- [ ] 배포: forge-auth-lib.php·forge-auth.php·forge-api.php·forge-state.js·forge-app.js·forge.css·forge.html.
- [ ] 라이브 스모크: `?me=1`=`{ok:false,enabled:false}` · 문서 GET=기존 문서(스위치 꺼짐 회귀 0) · `?login=1`=oauth_unset. **forge_admin.txt(moneyscdev@gmail.com) 서버 업로드**(lftp put — 로컬 임시 생성 후 업로드, git 제외).
- [ ] 사용자 안내: GCP OAuth 클라이언트 생성 절차(리다이렉트 URI `https://parksvc.mycafe24.com/map/forge-auth.php`) → `forge_google_oauth.json` 업로드 시 활성화됨을 보고.
- [ ] 메모리 갱신(scoopforge-rel-axis 또는 신규 auth 메모) + Commit.

## 후속(범위 외)

키 업로드 후 실계정 E2E(admin 이관 확인) · 결제 스펙 · 사용자별 비전 큐.
