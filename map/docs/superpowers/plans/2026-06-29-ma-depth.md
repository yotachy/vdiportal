# 이동평균 분석 심화 (MA Depth) Implementation Plan — Plan A

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 단일 SMA 한 줄짜리 이동평균을 다중MA(단/중/장)·크로스·배열·동적 S/R·종합 bias로 심화하고, 예측(TF가중)·작도에 일관 반영한다.

**Architecture:** MA 분석 수학을 코어 순수 함수 `ForgeCore.analyzeMA`로 모아 예측(`run`)과 작도(`_drawMALayers`)가 공유. 추세선이 만든 패턴(단일출처·TF가중·다레이어 작도·좌표 매퍼)을 그대로 따른다.

**Tech Stack:** Vanilla JS(무빌드), `node:test`/`node:assert`, HTML5 Canvas.

**범위 주의:** 본 플랜은 스펙 §4·5·6(MA 분석·예측·작도)만 구현한다. 스펙 §7(시연 상세화 프레임워크 — 단계 내레이션·누적 로그·레이어 출현)은 **별도 Plan B**에서 진행한다. 본 플랜 완료 시 MA 분석은 기존 시연/작도 경로로 정상 표시·예측 반영된다(시연 한 줄은 후속 Plan B에서 다단계화).

## Global Constraints

- 바닐라 JS · 빌드도구/외부 라이브러리 금지 · 단일 `forge.html` + 단일 `forge-core.js`.
- UI 텍스트 한국어. 다크 토큰 색만: MA 블루 `#7fb0ff`/`#5b8def`/`#3b62c0`, 골든 `#46c28e`, 데드 `#e06a6a`, eth `#8a92b2`, 잉크 `rgba(11,15,20,.85)`.
- `analyzeMA`는 **순수 함수**(부수효과 없음). `P<2` → 빈 결과(bias 0). 가격/값 `≤0` 방어 불필요(가격 양수 가정, 그러나 division by 0은 `||1` 가드).
- 작도·예측은 같은 `analyzeMA` 공유(정합). 작도 좌표 규약은 추세선 `_drawTrendLayers`와 동일(시계열 = 인덱스 공간, `M.fiToX`/`M.pToY`/`fiMin`).
- 예측 MA 기여는 **한 곳**(`run`의 보조 드리프트 항)에서만 적용(이중계상 방지), `trendProfileForTF(tf).trendScale`로 TF 가중.
- 단위 테스트: `node --test forge-core.test.js`.

---

### Task 1: 코어 `analyzeMA()` + EMA 헬퍼 + export + 단위 테스트

**Files:**
- Modify: `forge-core.js` (`sma` 함수(`:54`) 다음에 `ema`·`analyzeMA` 삽입; export `:580`)
- Test: `forge-core.test.js` (끝)

**Interfaces:**
- Consumes: 기존 `sma(arr,len)`, `linfit(y)→{a:절편,b:기울기}`.
- Produces:
  ```
  analyzeMA(price:number[], opts?:{len=20,ema=false,srPct=0.015}) → {
    mas:{ short:{period,series:number[],slope,last}|null, mid:{...}|null, long:{...}|null },
    cross:{ type:"golden"|"dead"|null, barsAgo:number|null },
    align:{ order:"bull"|"bear"|"mixed", score:number },
    sr:{ ma:"short"|"mid"|"long"|null, side:"support"|"resistance"|null, distPct:number|null },
    bias:number   // -1..1
  }
  ```
  - 값 규약: 각 `series`는 전체 길이(인덱스 = 봉). `slope`/`bias`는 -1..1.

- [ ] **Step 1: 실패 테스트 작성** — `forge-core.test.js` 끝에 추가

```js
test("analyzeMA: 정배열 상승 → bull, bias>0, long.slope>0", () => {
  const price = Array.from({ length: 80 }, (_, i) => 100 + i * 2);
  const r = ForgeCore.analyzeMA(price, { len: 5 });
  assert.strictEqual(r.align.order, "bull");
  assert.ok(r.bias > 0);
  assert.ok(r.mas.long.slope > 0);
});

test("analyzeMA: 역배열 하락 → bear, bias<0", () => {
  const price = Array.from({ length: 80 }, (_, i) => 300 - i * 2);
  const r = ForgeCore.analyzeMA(price, { len: 5 });
  assert.strictEqual(r.align.order, "bear");
  assert.ok(r.bias < 0);
});

test("analyzeMA: 최근 하락→상승 전환 → 골든크로스 감지", () => {
  const down = Array.from({ length: 30 }, (_, i) => 100 - i);      // 100→71
  const up = Array.from({ length: 14 }, (_, i) => 71 + (i + 1) * 4); // 가파른 반등
  const r = ForgeCore.analyzeMA(down.concat(up), { len: 5 });
  assert.strictEqual(r.cross.type, "golden");
  assert.ok(r.cross.barsAgo >= 0 && r.cross.barsAgo < 14);
});

test("analyzeMA: EMA vs SMA → 다른 last(둘 다 유한)", () => {
  const price = Array.from({ length: 60 }, (_, i) => 100 + Math.sin(i / 3) * 5 + i);
  const s = ForgeCore.analyzeMA(price, { len: 10, ema: false });
  const e = ForgeCore.analyzeMA(price, { len: 10, ema: true });
  assert.ok(isFinite(s.mas.short.last) && isFinite(e.mas.short.last));
  assert.notStrictEqual(s.mas.short.last, e.mas.short.last);
});

test("analyzeMA: 소량 데이터(P<len) → 예외 없음·유한 bias", () => {
  const r = ForgeCore.analyzeMA([10, 11, 12], { len: 20 });
  assert.ok(isFinite(r.bias));
  assert.ok(["bull", "bear", "mixed"].includes(r.align.order));
});
```

> 골든크로스 테스트의 `up` 구간 길이/기울기는 "최근 골든크로스"가 생기도록 한 값이다. 만약 환경에서 교차 시점이 어긋나면 **`up`의 길이/계수만** 조정해 최근 교차를 만들어라(단언은 유지).

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test forge-core.test.js`
Expected: FAIL — `ForgeCore.analyzeMA is not a function`

- [ ] **Step 3: 구현** — `forge-core.js`의 `sma` 함수(`:54`) 정의 **다음**에 `ema`·`analyzeMA` 삽입

```js
  function ema(arr, len) {
    const out = [], a = 2 / (len + 1); let prev;
    for (let i = 0; i < arr.length; i++) { const v = arr[i]; prev = (i === 0) ? v : (prev + a * (v - prev)); out.push(prev); }
    return out;
  }

  function analyzeMA(price, opts) {
    opts = opts || {};
    const len = opts.len || 20, useEma = !!opts.ema, srPct = opts.srPct != null ? opts.srPct : 0.015;
    const P = price.length;
    const EMPTY = { mas: { short: null, mid: null, long: null }, cross: { type: null, barsAgo: null }, align: { order: "mixed", score: 0 }, sr: { ma: null, side: null, distPct: null }, bias: 0 };
    if (P < 2) return EMPTY;
    const mk = period => {
      const series = useEma ? ema(price, period) : sma(price, period);
      const last = series[P - 1];
      const w = Math.max(2, Math.min(period, P - 1));
      const seg = series.slice(P - w);
      const f = linfit(seg);   // {a:절편, b:기울기}
      const slope = Math.max(-1, Math.min(1, Math.tanh((f.b / (Math.abs(last) || 1)) * 100)));
      return { period, series, slope, last };
    };
    const short = mk(len), mid = mk(len * 3), long = mk(len * 6), pl = price[P - 1];
    let cross = { type: null, barsAgo: null };
    const lim = Math.min(P - 1, len * 6);
    for (let i = P - 1; i >= Math.max(1, P - 1 - lim); i--) {
      const ds = short.series[i] - long.series[i], dp = short.series[i - 1] - long.series[i - 1];
      if (ds === 0) continue;
      if (dp <= 0 && ds > 0) { cross = { type: "golden", barsAgo: (P - 1) - i }; break; }
      if (dp >= 0 && ds < 0) { cross = { type: "dead", barsAgo: (P - 1) - i }; break; }
    }
    const pairs = [pl > short.last, short.last > mid.last, mid.last > long.last];
    const upCnt = pairs.filter(Boolean).length;
    const bull = pl > short.last && short.last > mid.last && mid.last > long.last;
    const bear = pl < short.last && short.last < mid.last && mid.last < long.last;
    let order = "mixed", score = 0;
    if (bull) { order = "bull"; score = upCnt / 3; }
    else if (bear) { order = "bear"; score = (3 - upCnt) / 3; }
    const cand = [["short", short.last], ["mid", mid.last], ["long", long.last]];
    let near = null, nd = Infinity;
    for (const [name, val] of cand) { const d = Math.abs(pl - val) / (Math.abs(pl) || 1); if (d < nd) { nd = d; near = name; } }
    let sr = { ma: null, side: null, distPct: null };
    if (near && nd <= srPct) { const val = near === "short" ? short.last : near === "mid" ? mid.last : long.last; sr = { ma: near, side: pl >= val ? "support" : "resistance", distPct: nd }; }
    const alignDir = order === "bull" ? 1 : order === "bear" ? -1 : 0;
    let crossDir = cross.type === "golden" ? 1 : cross.type === "dead" ? -1 : 0;
    if (crossDir !== 0 && cross.barsAgo != null) crossDir *= Math.max(0, 1 - cross.barsAgo / (len * 6));
    const bias = Math.max(-1, Math.min(1, 0.5 * alignDir * score + 0.3 * crossDir + 0.2 * long.slope));
    return { mas: { short, mid, long }, cross, align: { order, score }, sr, bias };
  }
```

- [ ] **Step 4: export 추가** — `forge-core.js:580` return 객체 끝에 `analyzeMA` 삽입

```js
  return { version, makeDemoSeries, buildDAG, evalBlocks, detrendNorm, pdmTheta, scanPeriod, run, runSteps, visionBiasFrom, sampleSeries, sampleGraph, analyzeTrend, trendProfileForTF, analyzeMA };
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test forge-core.test.js`
Expected: PASS (신규 5개 포함 전체)

- [ ] **Step 6: 커밋**

```bash
git add forge-core.js forge-core.test.js
git commit -m "feat(forge): ForgeCore.analyzeMA — 다중MA·크로스·배열·동적S/R·bias"
```

---

### Task 2: 예측 `run` — MA 방향 드리프트(TF 가중) + 테스트

**Files:**
- Modify: `forge-core.js` (`run` 내: `_prof` 선언부 `:479` 다음에 MA 산출 추가; 예측 루프 `m` 합산 `:490` 부근)
- Test: `forge-core.test.js`

**Interfaces:**
- Consumes: `analyzeMA`(Task 1), `trendProfileForTF`(기존). `run` 스코프 `price`, `graph`, `opts`, `_prof`, `k`, `futW`.
- Produces: MA 블록이 있으면 예측 경로가 MA 국면 방향으로 보조 편향.

- [ ] **Step 1: 실패 테스트 작성** — `forge-core.test.js` 끝에 추가

```js
test("run: MA 블록 상승 국면 → 타깃 상향 + TF 월봉>5분", () => {
  const G = { nodes: [
    { id: "p", kind: "block", blockType: "price" },
    { id: "m", kind: "block", blockType: "ma", params: { len: 5 } },
    { id: "o", kind: "block", blockType: "predict" }
  ], edges: [{ from: "p", to: "m" }, { from: "m", to: "o" }] };
  const up = { price: Array.from({ length: 70 }, (_, i) => 100 + i * 1.5) };   // 정배열 상승
  const rM = ForgeCore.run(G, up, { futW: 12, timeframe: "월봉" });
  const rI = ForgeCore.run(G, up, { futW: 12, timeframe: "5분" });
  assert.ok(rM.prediction.path.every(isFinite) && rI.prediction.path.every(isFinite));
  const gain = r => r.prediction.target / r.prediction.anchor;
  assert.ok(gain(rM) > 1, "상승 국면 → target>anchor");
  assert.ok(gain(rM) > gain(rI), "월봉(MA 국면 강가중)이 5분보다 상향");
});

test("run: MA 블록 없으면 MA 기여 0(기존 동작 보존)", () => {
  const G = { nodes: [{ id: "p", kind: "block", blockType: "price" }, { id: "o", kind: "block", blockType: "predict" }], edges: [{ from: "p", to: "o" }] };
  const data = { price: Array.from({ length: 40 }, (_, i) => 100 + i) };
  const r = ForgeCore.run(G, data, { futW: 8, timeframe: "월봉" });
  assert.ok(r.prediction.path.every(isFinite));
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test forge-core.test.js`
Expected: FAIL — `gain(rM) > gain(rI)` (현재 MA가 예측 방향 보조항으로 반영 안 됨)

- [ ] **Step 3: MA 드리프트 산출** — `forge-core.js`의 `const _prof = trendProfileForTF(opts && opts.timeframe);` 줄(`:479`) **다음**에 삽입

```js
    const _mn = (graph.nodes || []).find(nd => nd.kind === "block" && nd.blockType === "ma");
    const _ma = _mn ? analyzeMA(price, { len: (_mn.params && _mn.params.len) || 20, ema: !!(_mn.params && _mn.params.ema) }) : null;
    const maDrift = _ma ? _ma.bias * _prof.trendScale * 0.10 : 0;   // MA 국면 방향 드리프트(±10% 상한·TF가중)
```

- [ ] **Step 4: 예측 루프 합산** — `forge-core.js`의 `const m = rev + mom + trend + sig + seas, sd = ...` 줄(`:490`)을 교체

```js
      const m = rev + mom + trend + sig + seas + maDrift * (k / futW), sd = Math.sqrt(sigBand * sigBand + 0.36 * trChSig * trChSig) * Math.sqrt(k) * 0.85;
```

> `rev/mom/trend/sig/seas/sd/k/futW/sigBand/trChSig` 변수명·기존 항은 그대로. `maDrift * (k / futW)`만 가산(horizon 비례).

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test forge-core.test.js`
Expected: PASS (전체)

- [ ] **Step 6: 커밋**

```bash
git add forge-core.js forge-core.test.js
git commit -m "feat(forge): 예측 run에 MA 방향 드리프트(analyzeMA.bias·TF가중) 보조항"
```

---

### Task 3: 작도 공용 헬퍼 `_drawMALayers` + 차트 모드 + 편집기 `ema`

**Files:**
- Modify: `forge.html` (헬퍼는 `_drawTrendLayers`(`:2477`) 근처에 추가; 차트 ma 분기 `:2552`; 편집기 `:1205`)

**Interfaces:**
- Consumes: `ForgeCore.analyzeMA`(Task 1). 차트 기하 `g`(`_mainGeo`), `toXh`/`toY`/`Hn`/`off`/`P`/`price`/`col`.
- Produces: `_drawMALayers(c, ma, M)` — `M={fiToX,pToY,nowFi,fiMin}`. 두 모드 공용(Task 4가 오버레이 호출).

- [ ] **Step 1: 공용 헬퍼 추가** — `forge.html`의 `_drawTrendLayers` 함수 위(또는 아래)에 삽입

```js
  // 다중 MA 작도(차트·오버레이 공용). M=좌표 매퍼.
  function _drawMALayers(c, ma, M) {
    c.save();
    const { fiToX, pToY, nowFi, fiMin = 0 } = M;
    const COL = { short: "#7fb0ff", mid: "#5b8def", long: "#3b62c0" };
    const WID = { short: 1.4, mid: 2.0, long: 2.6 };
    function strokeSeries(series, key, srOn) {
      c.beginPath(); let started = false;
      for (let fi = Math.max(fiMin, 0); fi <= nowFi; fi++) {
        const v = series[fi]; if (!isFinite(v)) continue;
        const x = fiToX(fi), y = pToY(v); if (!isFinite(x) || !isFinite(y)) continue;
        started ? c.lineTo(x, y) : c.moveTo(x, y); started = true;
      }
      c.stroke();
    }
    function drawMA(s, key) {
      if (!s) return;
      const srOn = ma.sr.ma === key;
      if (key === "long") { c.setLineDash([]); c.strokeStyle = "rgba(11,15,20,.85)"; c.lineWidth = WID[key] + 1.9; strokeSeries(s.series, key); }
      c.strokeStyle = COL[key]; c.lineWidth = srOn ? WID[key] + 1.2 : WID[key]; c.setLineDash(srOn ? [5, 4] : []);
      strokeSeries(s.series, key); c.setLineDash([]);
    }
    drawMA(ma.mas.long, "long"); drawMA(ma.mas.mid, "mid"); drawMA(ma.mas.short, "short");
    // 크로스 마커
    if (ma.cross.type && ma.cross.barsAgo != null && ma.mas.short) {
      const fi = Math.max(fiMin, nowFi - ma.cross.barsAgo), v = ma.mas.short.series[fi];
      if (isFinite(v)) {
        const x = fiToX(fi), y = pToY(v);
        if (isFinite(x) && isFinite(y)) {
          const gold = ma.cross.type === "golden";
          c.fillStyle = gold ? "#46c28e" : "#e06a6a";
          c.beginPath(); c.arc(x, y, 4, 0, 7); c.fill();
          c.font = "700 9.5px ui-monospace,monospace"; c.textAlign = "center";
          c.fillText((gold ? "골든 " : "데드 ") + ma.cross.barsAgo + "봉", x, y - 7);
        }
      }
    }
    // 배열 라벨
    if (ma.mas.short) {
      const x = fiToX(nowFi), y = pToY(ma.mas.short.last);
      if (isFinite(x) && isFinite(y)) {
        const o = ma.align.order;
        c.fillStyle = o === "bull" ? "#46c28e" : o === "bear" ? "#e06a6a" : "#8a92b2";
        c.font = "700 9.5px ui-monospace,monospace"; c.textAlign = "left";
        c.fillText((o === "bull" ? "정배열 ▲" : o === "bear" ? "역배열 ▼" : "혼조 –") + (ma.sr.ma ? " · " + (ma.sr.side === "support" ? "지지" : "저항") : ""), x + 4, y - 6);
      }
    }
    c.restore();
  }
```

- [ ] **Step 2: 차트 ma 분기 교체** — `forge.html:2552` 의 `if (n.blockType === "ma") { ... }` 분기 내부(단일선 그리기 + legend.push)를 아래로 교체

```js
        if (n.blockType === "ma") {
          const ma = ForgeCore.analyzeMA(price, { len: (n.params && n.params.len) || 20, ema: !!(n.params && n.params.ema) });
          _drawMALayers(c, ma, { fiToX: fi => toXh(Math.max(0, Math.min(Hn - 1, fi - off))), pToY: v => toY(v), nowFi: P - 1, fiMin: off });
          legend.push({ col, t: EV_LABEL.ma + "(다중)" });
        }
```

> 차트 분기 상단에 `toXh`/`toY`/`Hn`/`off`/`P`/`price`/`col` 이 이미 선언됨(추세선 분기와 동일 스코프). 기존 ma 분기가 `_sma`로 단일선을 그리던 코드는 위 블록으로 전체 대체.

- [ ] **Step 3: 편집기 `ema` 파라미터 추가** — `forge.html:1205` 의 ma 행을 아래로 교체

```js
    if (n.blockType === "ma") {
      rows.push(numRow("len", "이동평균 길이", (n.params && n.params.len) ?? 10));
      rows.push(numRow("ema", "EMA 사용(0/1)", (n.params && n.params.ema) ? 1 : 0));
    }
```

> `numRow` 공통 핸들러가 `n.params[key]=Math.round(Number(value))` 로 저장하므로 `ema`는 0/1 정수로 영속되고, 소비측 `!!(n.params.ema)` 로 해석된다(1→true, 0→false).

- [ ] **Step 4: 구문 검증**

`forge.html` 인라인 `<script>` 추출 → `node --check` 무오류. 명령·결과를 보고에 기재. (캔버스 단위테스트 없음 — 컨트롤러가 Task 4 후 헤드리스/라이브 시각검증)

- [ ] **Step 5: 커밋**

```bash
git add forge.html
git commit -m "feat(forge): 차트모드 다중MA 작도(_drawMALayers·크로스·배열·S/R) + ema 파라미터"
```

---

### Task 4: 오버레이 모드 배선 + 범례 라벨

**Files:**
- Modify: `forge.html` (오버레이 ma 분기 `:2607`; `EV_LABEL` `:2410`)

**Interfaces:**
- Consumes: `_drawMALayers`(Task 3). 오버레이 기하 `g`(`_coneGeo`), `xOf`/`yOf`/`clipY`/`P`/`price`/`col`.

- [ ] **Step 1: 오버레이 ma 분기 교체** — `forge.html:2607` 의 오버레이 `} else if (n.blockType === "ma") { ... }` 분기 내부를 아래로 교체

```js
        } else if (n.blockType === "ma") {
          const ma = ForgeCore.analyzeMA(price, { len: (n.params && n.params.len) || 20, ema: !!(n.params && n.params.ema) });
          _drawMALayers(c, ma, { fiToX: fi => xOf(fi), pToY: v => clipY(yOf(v)), nowFi: P - 1, fiMin: 0 });
          legend.push({ col, t: EV_LABEL.ma + "(다중)" });
        }
```

> 오버레이 분기 상단에 `xOf`/`yOf`/`clipY`/`P`/`price`/`col` 이 이미 선언됨. `fiMin:0`은 오버레이 `xOf`가 전구간 매핑이라 기본값과 동일(명시).

- [ ] **Step 2: 범례 상수 갱신(선택적 일관화)** — `forge.html:2410` `EV_LABEL` 의 ma 라벨 확인

`EV_LABEL.ma`는 "이동평균"으로 둔다(작도에서 "(다중)"을 동적으로 덧붙이므로 상수 변경 불필요). 변경 없음 — 이 스텝은 확인만.

- [ ] **Step 3: 구문 검증**

`forge.html` 인라인 `<script>` 추출 → `node --check` 무오류. 차트 분기(Task 3) 미변경 확인. 보고에 기재.

- [ ] **Step 4: 커밋**

```bash
git add forge.html
git commit -m "feat(forge): 오버레이모드 다중MA 작도 배선(_drawMALayers 공용)"
```

---

## Self-Review

**Spec coverage (Plan A 범위 = §4·5·6):**
- §4 `analyzeMA`(3중MA·크로스·배열·동적S/R·bias) → Task 1 + 테스트 5개 ✅
- §4.1~4.5 산출/크로스/배열/SR/bias 세부 → Task 1 구현 ✅
- §5 예측(MA 드리프트 단일경로·TF가중·MA없으면 0) → Task 2 + 테스트 ✅
- §6.1 `_drawMALayers`(3선·크로스·배열·S/R) → Task 3 ✅
- §6.2 차트·오버레이 분기 교체 → Task 3(차트)·Task 4(오버레이) ✅
- §6.3 편집기 `len`/`ema` → Task 3 Step 3 ✅
- §7 시연 상세화 → **Plan B(별도)**, 본 플랜 범위 외(명시).
- §8 테스트(크로스·배열·기울기·bias·EMA·소량·예측 TF) → Task 1·2 테스트 ✅

**Placeholder scan:** 코드/명령/기대출력 구체값. 작도 검증은 단위테스트 불가 영역이라 구문검사+라이브 시각검증 명시. 골든크로스 테스트 데이터 튜닝 여지는 단언 불변 조건으로 한정.

**Type consistency:** `analyzeMA` 반환 키(`mas.{short,mid,long}.{period,series,slope,last}`/`cross.{type,barsAgo}`/`align.{order,score}`/`sr.{ma,side,distPct}`/`bias`)를 Task 2(`_ma.bias`)·Task 3/4(`ma.mas.*.series`,`ma.cross.*`,`ma.align.order`,`ma.sr.*`)에서 동일 사용. `M` 매퍼(`fiToX/pToY/nowFi/fiMin`)는 `_drawTrendLayers`와 동일 형태 ✅.
