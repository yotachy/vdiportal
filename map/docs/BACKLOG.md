# 스쿱포지 백로그 (살아있는 문서)

> 운영 규칙: 사용자가 작업을 수십 개 단위로 투하 → 여기 등록·우선순위 → 위에서부터 실행. 완료는 ✅로 이동(커밋 해시 병기). 세션이 바뀌어도 이 문서가 단일 출처.
> 불변 규율: 검증 관문(순증분·자명규칙·OOS/LOSO) · 정직 기록(실패 포함, 스코어카드) · 커밋+배포 한 세트 · 열린 엔진(validatedAxes 레지스트리) · 게이팅은 맨 마지막.

## 🔥 진행 중 / 대기

- ~~**[엔진·성능 ★높음] `analyzeCycle` O(n²) — 전량 이력 분석이 17.6초**~~ ✅ **완료(2026-08-26, 47e2913·f0cd01d)**: cycle 검출 입력을 최근 600봉으로 창(백테스트 LOOKBACK 600 정합) — dftSpectrum·PDM 상수화, cycle 2187ms→~40ms. 결과는 전체 배열 좌표로 변환(phi+=w·off·icpt−=slope·off·fit 재생성)해 cycleSeries·작도 정합. **§② 무하락 구조적 증명**: 백테스트는 ≤600봉만 넣으므로 off=0→구·신 바이트 동일(7픽스처 대조 확인), 58.1% 불변. + **지표 계산 캐시**: run() _ev(evalBlocks 는 driftWeights 무관→커스텀 2회 실행 공유) + 앱 루프 tick 제거(채점 전용 호출은 동기). **전체: 커스텀 17.6→5.4초·심화 11.1→4.5초(3배+)**. 앱 `../forge-core.js` 캐시버스터 신설. forge-core 259/259. **남은 비용(선택 후속)**: computeInd+evalBlocks 가 32지표를 전량 이력에서 각각 계산 — 추가 단축은 입력 윈도우(§15 봉전송정책) 또는 두 경로 통합. ↓ 원 진단 보존:
  - **[원 진단]** 모바일 커스텀 가중치 패널 성능 조사 중 확인 — NVDA 전량(5027봉) 커스텀 analyze 가 **17.6초(데스크톱! 폰이면 40~60초)**. 지표별 실측: **`analyzeCycle`(pmax:0=주기 자동탐색 n/2까지) 단독 2187ms**, 그 외 전 지표 0~9ms. cycle 이 O(n²)(후보 주기 × 전 구간 상관). 커스텀은 지표를 3회(앱 루프+run 기준+run 가중) 돌려 cycle 만 ~6.5초+, aggUpProb/캘리브레이션 재실행까지 17.6초.
  - **영향 범위**: 미리보기뿐 아니라 **채점되는 최종 실행(runCustom·deep)도 전량 이력이라 같은 비용**. 모바일 임시 완화(미리보기 400봉 창, 1490d4a)로 사용자 보고 문제는 막았지만, **실 채점 실행은 여전히 전량서 느리다**(reveal 애니메이션이 일부 가릴 뿐).
  - **근본 해결(엔진 트랙·§②)**: `forge-core.analyzeCycle` 이 `pmax:0` → `Math.floor(P/2.5)`(5027봉이면 2010) 까지 스캔, scanPeriod 가 후보 주기마다 O(P) → O(P²). pmax 를 고정 상한(예 200~300봉)으로 캡하면 O(n×상수), 캡 후 전량 analyze ~1초 예상.
  - ★**출력이 바뀐다 — "무해한 캡"이 아니다**(2026-08-25 실측): 캡 없으면 지배주기 1885(NVDA)·1141(AAPL)·1207(TSLA)·895(MSFT)·1259(BTC) — 5000봉에 2~3주기뿐인 통계적으로 약한 초장주기. 캡 200/300 씌우면 160~294봉으로 바뀌고 **cycle bias 가 크게 변하며 TSLA·MSFT·BTC 는 부호가 뒤집힌다**. 캡 200 vs 300 도 서로 다른 결과. 짧은 이력(005930 426봉)은 무변화. → **cycle 은 채점 드리프트에 쓰이므로 백테스트·스코어카드 관문 필수**: 캡값(200/250/300) 선택도 백테스트로, 방향 성적 무하락(오히려 초장주기 과적합 제거로 개선 가설) 확인 후 확정. **관문 없이 절대 캡 넣지 말 것.**
  - **점검 결론(2026-08-25)**: 전 32지표 개별 실측 — cycle(1951ms)만 O(n²), 나머지 31지표 전부 ≤12ms(stochastic 12·trend 11·bollinger 7 그다음). 티어: basic 61ms(cycle 없음)·deep 11.1초·custom 17.6초(둘 다 cycle 오염). 시그널 스캔 0ms·차트 2ms 정상. **다른 성능 폭탄 없음 — 이 하나가 전부.**
  - **부수 이득**: 이 하나로 PC 웹·모바일·채점 실행 전부 빨라짐(열린 엔진 — 공유 원본). **2레인 중 A(엔진) 트랙 우선순위 상단.**

- ~~**[앱] 차트 패널 하단이 하단 메뉴에 가려짐**~~ ✅ **완료(2026-08-29)**: 사용자 제보. 글자 확대(`#msApp{zoom:1.12}`)를 켠 직후 생긴 회귀 — `app-forge-frame.sync()` 와 막대 툴팁이 `getBoundingClientRect()`(시각 px) 차이를 그대로 `style`(zoom 안의 CSS px)에 넣어 **12% 크게·아래로** 배치됐다. `MS.ui.zoomOf()`(computed zoom)로 나눠 보정. 실측: fz1 에서 iframe 폭 437→390, 하단 635 < 탭바 749. **규율**: `#msApp` 안에서 rect→style 변환은 반드시 `zoomOf()` 로 나눈다(같은 함정이 새 오버레이마다 재발한다).

- ~~**[앱] 글자 크기 설정이 동작하지 않음**~~ ✅ **완료(2026-08-29)**: 사용자 점검 요청. 원인 — `data-fz="1"` 속성은 붙는데 **CSS 에 키우는 규칙이 없었다**(헤더 간격·라벨 숨김 두 줄뿐). 토스트만 "12% 키웠어요"라고 말하고 아무것도 안 커졌다. 화면이 전부 px 폰트라 루트 폰트로는 못 키움 → `#msApp{zoom:1.12}`. vh 는 zoom 에 나눠지지 않아 세로가 넘치므로 vh 규칙 3곳을 `100dvh/1.12 − 헤더px` 로 보정(첫 시도 `(100dvh−58)/1.12` 는 헤더도 zoom 되어 7px 넘쳤다 — 실측으로 잡음). 헤드리스 실측: compact·expanded 배율 1.13, 가로·세로 넘침 없음, 탭바=뷰포트 하단.
  - **남은 폴리시**: 확대 시 홈 히어로 카드 등락 숫자가 `▲0.2…` 로 말줄임(카드 폭 대비). 축소 배율(0.9 등) 옵션은 미구현 — 설정이 '보통/크게' 2단뿐.

- ~~**[앱] 경험치 손맛·레벨업 풀충전·정시 출석**~~ ✅ **완료(2026-08-29, 361fd7c)**: 사용자 정책 3건. ① 헤더 게이지 26×3px → 수치 병기+5px+펄스, 획득 버스트에 게이지(이전%→새%, 레벨업이면 100%). ② `w_levelup_fill` — 상한까지·레벨당 1회(멱등)·로그인만·0 지급도 레벨 소비. **레벨은 클라 신고**(레벨 수×상한=남용 천장 작음) → 'XP 서버 검증'과 같은 §15 과제. ③ `last_checkin` 을 시간 슬롯(`Y-m-dTH`, UTC 정시=KST 정시)로 — 시간당 +1·상한이 자연 제한·**연속일은 일 단위 유지**(사용자 확정)·`nextSlotAt` 카운트다운·정시 경계 자동 갱신. **Q13(6h 주기) 폐기.** 옛 앱 경로 `wallet-api.php` 디스패처 테스트도 슬롯 계약으로 갱신.
  - **주의**: 옛 일 단위 기록(`Y-m-d`)은 첫 정시에 바로 받을 수 있다(마이그레이션 무해). 시간당 +1 이라 상한 15 는 하루 안에 찬다 — 광고·소비 설계와 균형은 실사용 데이터로 재조정(§15).

- **[앱·정책] 레벨별 혜택 차등(D) — 사용자 결정 대기(2026-08-29)** — 지금은 모든 레벨업이 '상한 +2'로 같다. 후보: Lv2 페르소나 하루 답 +1 · Lv3 심화 분석 하루 1회 무료 · Lv4 커스텀 프리셋 저장 슬롯 · Lv5 리더보드 레벨 배지 / 꾸미기(Lv3~ 캐릭터 색 테마·홈 카드 스킨) / Lv2 캐릭터 이름 짓기. 스쿱 경제(§4-B)에 닿는 항목은 협의 후 `POLICY` 로. 연출(A·B·C)은 완료.

- **[앱] 페르소나 '오늘 N답' 정합(2026-08-29)** — 별도 일일 카운터(`dayCounters.personaToday`)가 진행도와 따로 놀아 "미진행인데 끝났다고 나옴". 이제 답 항목에 `t`(시각)를 싣고 `MS.state.personaToday()` 가 오늘 답을 답 자체에서 센다(동기화·이전 답 고치기·자정 넘긴 탭 모두 안전). 재답변은 XP 재지급 없음. **잔여**: `dayVisit`·`drawXp` 는 아직 부팅 시 롤오버에 의존 — 자정을 넘긴 열린 탭에서 '오늘 첫 방문 ✓'가 남을 수 있다. 같은 방식(시각 기반)으로 옮길 것.

- **[인증] 콜백 즉시 병합(2026-08-29, e9f25f6)** — "로그인했는데 로그인이 안 됨" 3번째 사고(새 기기 id `dba4yusj…`, 구글 이름 'Tachy' 인증 완료·미병합). 앱 폴링에 링크를 걸어 두는 구조가 원인이라 **콜백에서 서버가 바로 w_merge**(논스는 안 태움 → 폴링은 멱등 통과 후 sync_put). 이제 앱은 부팅 `wallet_state` 만으로 linked·gname 을 본다. 이 사고 클래스는 이것으로 닫혀야 한다 — 재발하면 병합 자체(device-claimed·busy)를 의심할 것.
  - **테스트 위생**: 헤드리스 라이브 검증이 첫 `goto` 에서 무작위 기기 id 로 계정을 만든다(오늘 2건 `dqlgm2…`·`dsgnij…`). **`addInitScript` 로 첫 로드 전에 `ms_device_id=smoketest01` 을 심을 것** — reload 방식은 늦다.

- **[인증] 구글 로그인 안정화 — 2026-08-28 라운드 7건 + 남은 구조 과제**: 활성화 당일 사용자 제보로 연쇄 수정. 원장(wallet.db 읽기 사본)으로 진단한 결론 — **실패 16건 전부 '구글 완료·미병합' 상태 + 6~17초 연타**. 서버가 완료 논스를 어느 폴링에서든 주워 병합하도록(`app_nonce_completed` · `auth_start completed:true`) 고쳐 탭 수명·연타와 무관하게 만들었다(f31f1ca). 그 전 6건: 콜백 막다른 길(37ee52e) · 대기 논스 영속(df823d6) · 로그아웃 서버 반영(2a3194c) · busy 재시도(f1e2fa1) · 버튼 잠금+팝업 차단(87bc293) · JSON 구분자 오타 수정 후 업로드.
  - **남은 구조 과제 ①(계정 모델)**: 원장에서 사용자가 **구글 계정 2개(…310 / …721)를 번갈아** 쓰는 게 보였다. 현 모델은 기기당 계정 1행·구글당 1행이라 A 로 연결된 기기에 B 로 로그인하면 `device-claimed`(409)로 거절된다 — 로그아웃 후에만 B 가 된다. 또 로그아웃은 구글 행의 링크를 끊어 **같은 구글 계정을 쓰는 다른 기기도 게스트**가 된다. 기기↔계정 매핑 테이블이 있어야 기기별 로그아웃·계정 전환이 자연스럽다. 실사용자가 다기기를 쓰기 전에.
  - **남은 구조 과제 ②(merged-away 계정)**: 사용자 폰 기기(`d-8997…`, 옛 앱 형식 ID)의 계정은 이미 두 번 흡수(merge_discard)된 빈 껍데기인데, 새 구글 계정으로 다시 병합하면 `w_merge` 가 **그 껍데기를 직접 claim** 한다(잔액 0). 데이터는 흡수된 쪽에 있어 화면 잔액이 0 으로 보일 수 있다. `app_acct_resolve` 의 해석과 `w_merge` 의 대상이 어긋나는 지점 — 정리 필요.
  - **운영 주의**: 오늘 이 IP 의 신규 계정이 **24/30**(W_IP_DAILY). 대부분 내 헤드리스 테스트가 만든 기기 ID 다. **테스트는 기존 기기 ID(smoketest01 등)를 재사용할 것** — 한도에 걸리면 `auth_start` 가 429 로 죽어 "연결을 시작하지 못했어요"가 뜬다(사용자 증상과 다른 제3의 실패). 한도는 UTC 자정(KST 09:00) 리셋.
  - **진단 도구**: 앱 콘솔 `MS.auth.trace()`(최근 30건 로그인 이벤트). 서버 쪽은 SFTP 로 `data/wallet.db` 를 **읽기 사본**으로 받아 `auth_nonce` 를 보면 시도별 단계(①발급/②구글완료·미병합/③병합)가 그대로 남아 있다 — 사본은 구글 sub 가 들어 있으니 보고 즉시 지운다.

- ~~**[앱] 종목 추가 패널 — 실검색**~~ ✅ **완료(2026-08-28)**: 사용자 제보 *"몇 개 샘플 종목만 추가할 수 있고 미국주식·암호화폐가 대부분 검색이 안 된다"*. 실제로 시트는 `MASTER` **11종만** 필터하면서 카피는 *"미국 전 종목 · 주요 암호화폐"* 라고 적고 있었다(사실과 다름).
  - **서버**: `forge-api.php?search=` 신설. Yahoo 검색은 이미 '심볼 못 찾음' 실패 경로에서 쓰고 있었는데 정식 노출한 것. **필터가 핵심** — 그대로 노출하면 담은 종목이 차트에서 죽는다: 선물(`BTC=F`)·해외 중복상장(`APC.F`·`APC.DE`·`AAPL19.BK`)·지수 제외, 미국 거래소+CCC만, `BTC-USD`→`BTC/USD` 정규화, 같은 심볼 중복 제거, 상한 10. 검색어별 파일 캐시 TTL 24h(Yahoo 무인증 남용 방지). 순수 로직은 `forge-search-lib.php` 분리 — 테스트 27건.
  - **앱**: `app-data.createSearch`(세션 캐시·실패는 캐시 안 함·테스트 5건) + 시트 배선(디바운스 250ms·2글자·늦게 온 응답 무시·상태 3종). 검색 결과에는 **시세를 붙이지 않는다**(10건마다 OHLC 는 비용 폭증) — 담긴 종목에만.
  - **범위**: 미국주식+ETF+암호화폐(사용자 확정). **한국주식은 별건** — Yahoo 가 한글 검색을 못 하고(실측 '삼성전자' → 0건) 심볼 체계도 다르다(`005930.KS` vs `005930`). KRX 마스터 번들이 필요하다.
  - ★**오진 1건 기록**: `BRK-B` 가 검색은 되는데 차트가 안 떠 "Yahoo 폴백이 점↔하이픈 변환을 안 한다"고 진단했으나 **틀렸다** — 7종목을 버스트로 부르다 TwelveData 분당 한도(8회)에 걸린 일시 오류였다. 간격을 두고 3회 재시도 전부 정상. 프록시는 손대지 않았다.
  - 라이브 E2E: 'bitcoin' → `BTC/USD·BCH/USD·GBTC·IBIT·BITO` → 담기 → 차트 진입. 관문 1062건.
  - **남은 것**: PC 포지 티커 자동완성(`#tkSugg`)은 아직 실패 경로 전용 — 같은 엔드포인트로 통일하면 PC 도 검색이 된다(별건). 시트가 키 입력마다 `innerHTML` 을 통째로 다시 그려 캐럿을 복원하는 기존 패턴도 정리 여지.

- ~~**[작도] 다중스케일 작도 시연 — 좁게·중간·넓게 3단 카메라**~~ ✅ **완료(2026-08-28)**: 사용자 관찰 *"가장 최근(좁게)만 작도하고 마치 그게 다인 것처럼 끝낸다"*. 설계 `docs/superpowers/specs/2026-08-28-multiscale-drawing-demo-design.md` · 플랜 `docs/superpowers/plans/2026-08-28-multiscale-drawing-demo.md`.
  - **스케일**: 좁게 120 · 중간 **600(판정 스케일 — '판정 근거' 배지가 여기만)** · 넓게 전량. 축약 `P>900→3단 / 240<P≤900→2단 / P≤240→1단`(실측 005930 429봉 = 2단).
  - **실측이 설계를 두 번 바꿨다**: ① 스케일마다 기울기 **부호가 뒤집힌다**(TSLA 120봉 −0.109% vs 600봉 +0.132%, BTC 전량만 양수) — 이 충돌이 곧 보여줄 정보다. ② R² 는 창이 커질수록 올라가(전량 0.78~0.92 vs 120봉 0.16~0.58) **스케일 간 우열을 R² 로 매기면 항상 넓은 쪽이 이긴다** → 강조는 합류로.
  - **테스트가 구현의 결함 둘을 잡아 스펙을 정정**: 합류 tol 을 '비교값 범위×1.2%' → '가격 수준×1.2%'(값 2~3개일 때 범위 기준은 순환한다) · '되돌림 구간' 등 해석은 **3단에서만**(2단 불일치는 1 대 1 이라 근거 없음 → 나열).
  - ★**선행 버그 발견**: `_frameFromRegion` Y 패딩이 선형이라 광범위 창에서 **음수 가격**(`_yScale.lo −3.57`) → 로그축 매핑이 깨져 캔들이 상단에 눌림. **340봉 상한이 이를 가려왔고 상한 해제로 드러났다.** 배수 패딩 + 양수 하한으로 수정.
  - **엔진 판정 불변**: 새 순수 함수 3종(`scaleSet`·`scaleConfluence`·`scaleVerdictText`)은 `run()` 미호출. `analyzeFib` win 추가는 백테스트 픽스처 **325 케이스 바이트 동일** 실증. 관문 1030건.
  - **앱은 코드 0줄** — `forge.html?embed=app` iframe 이라 forge 세트 배포로 자동 상속.
  - **남은 폴리시**: 겹침 단계에서 라벨 3개가 우상단에 몰려 붙는다(읽히지만 간격 조정 여지) · 스케일명+지배창명이 붙어 `좁게 단기` 로 읽히는 것(의도대로지만 혼동 여지).

- **[엔진 ★] 입력 길이 민감도 전수 감사 — 검증 영역(≤600봉) vs 출하(전량)**(2026-08-28 신설·부분 완료): 1.11.1 추세창 수정에서 나온 일반 문제. 백테스트는 매 시점 **최근 ≤600봉만** 넣는데(backtest.js `LOOKBACK 600`) 라이브는 **전량 이력**을 준다 → 전 이력을 스캔하는 지표는 **검증된 적 없는 값**으로 판정에 참여한다.
  - **감사 실측(9종목·미국주식+암호화폐, 엔진 실파라미터)**: 32지표 중 **27종은 Δ 0 또는 ≤0.04**(무시 가능). **구조 탐색형 5종만 갈렸다**:

    | 지표 | 평균Δbias | 최대Δ | 부호 반전 |
    |---|---|---|---|
    | elliott | 0.209 | 0.610 | **6/9** |
    | structure | 0.200 | 0.600 | 3/9 |
    | volume | 0.134 | 0.403 | 1/9 |
    | fib | 0.079 | 0.261 | 1/9 |
    | pattern | 0.035 | 0.316 | 1/9 |

    **공통 원인 하나** — `detectSwings`/`detectPatterns` 가 전 이력에서 구조를 찾는다. (검증 영역에서의 발화율도 측정: structure·fib·volume 100% · elliott 36.6% · pattern 10.6% — 즉 elliott 은 "검증에서 침묵하는 지표"가 아니라 **간헐 발화**하는 지표다. 초기 가설은 5종목 표본에서 나온 착각이었고 454창 측정으로 정정.)
  - ✅ **완료분(2026-08-28, 엔진 1.11.2)**: `analyzeElliott` · `analyzeStructure` 600봉 창 + 인덱스 전체 좌표 복원. **§② 구조적 무하락**: 87종×4시점 **325 케이스 바이트 동일**. 라이브 변화 — elliott 6종 중 5종 `±0.14~0.61 → 0.00`, structure AAPL·META 부호 반전, deep 판정 AAPL 중립→상승·META 중립→하락. `draw:true` 다중스케일 티어는 전 이력 유지(작도 전용 의도).
  - **남은 3종(다음 판)**: **volume**(OBV 가 전 이력 누적 + `series`/`obv` 배열이 작도 패널과 길이로 묶여 있어 **패딩 필요** — 단순 슬라이스 금지) · **fib**(스윙 탐색이 전 이력, `len 120` 은 폴백일 뿐 · `degrees` 인덱스 확인 필요) · **pattern**(`geom` 좌표 복원). 같은 처방·같은 구조적 증명이 그대로 적용된다.
  - **일반 규율로 승격 검토**: 새 지표를 추가할 때 "전 이력을 스캔하는가"를 체크리스트에 넣을 것. 안 그러면 이 문제가 지표마다 반복된다.

- **[작도/엔진] 추세선 작도 정비 — 의미없는 선 제거 + 각도 정확도**(2026-08-25 등록, 사용자 제보): 앱 시연에서 추세선 작도를 보다가 발견 — **① 의미없는 추세선이 그려진다**(그리면 안 될 것을 긋는다 — 예: R²가 0에 가까운 회귀선, long/mid/short 3창을 무차별로 다 긋기) **② 추세선 각도·시작점 자체가 부정확한 구간이 있다**. 실측 진단으로 두 증상이 각각 어느 쪽 문제인지부터 분해할 것:
  - **작도 도메인**(`forge-draw.js` `_drawTrendLayers` 렌더/선택) — "의미없는 선을 안 긋는다"는 대개 여기. **채점 판정·예측 콘 불변**이라 `./tests/run.sh`만 통과하면 됨.
  - **엔진 도메인**(`forge-core.js` `analyzeTrend` 창 선택·기울기) — 추세는 **드리프트로 채점에도 쓰인다**. 여길 고치면 판정이 움직이므로 **§② 엔진 변경 프로토콜**(`./tests/run.sh` + backtest·스코어카드 관문, 방향 성적 무하락 확인) 필수. **작도만 고쳐 눈속임 금지**(그려진 선 ≠ 채점에 쓰인 값이 되면 정직 표기가 깨진다).
  - ★**모바일은 자동 상속** — 앱은 `forge.html?embed=app` iframe 으로 PC forge 코드(forge-core·forge-draw)를 **그대로** 실행하고 값만 받는다(앱 자체 작도 없음, app-chart.js 폐기). **PC에서 고치고 forge 세트 배포 → 앱은 같은 서버 스냅샷을 받아 함께 개선.** 모바일 코드 수정 0 — 대응은 ①엔진 커밋이 서버 반영됐는지 확인(미배포 시 앱이 옛 엔진/사망 — 2026-08-24 `aggUpProb` 전례) ②캐시버스터 bump ③앱 시연 육안 확인뿐.
  - **[상태] 작도 절반 완료(2026-08-28, 939ee31)** — 진단 결과 **두 증상이 한 원인**이었다: **적합 구간 ≠ 표시 구간.** 장기창=전 이력(NVDA 5,030봉=20년)·중기창=절반인데 화면은 84~180봉. 실측(프로덕션): NVDA 장기선은 플롯 밴드 **밖 −2.1배** 지점을 지나고 현재가 대비 −59%(TSLA +89%·BTC +47%·MSFT 중기 +25%, 밴드 통과 0%). 기존 weak 게이트(R²<0.15)는 이 선들의 R²log 가 0.66~0.94라 **한 번도 발동하지 않았다** — R² 는 자기 적합 구간을 설명하는 정도이지 표시 구간 적합도가 아니다. 게다가 `_drawTrendLayers` 는 Gann 과 달리 **클립이 없어** 밴드 밖 선·라벨이 플롯 밖에 그려졌다.
    - **한 것(작도·엔진 판정 불변)**: `forge-core.trendScreenFit`(표시 구간 적합도 — **run() 미호출**, 테스트 6건) + 플롯 클립 + 밴드 미통과 시 선 생략하고 **"장기 추세 화면 밖 아래 · 현재가 대비 −59%" 배지**(생략만 하면 "엔진은 쓰는데 화면엔 없다"가 되어 정직 표기가 반대로 깨진다) + coverage<0.2 weak 스타일 + 전체 라벨에 창 봉수. 값(기울기·절편)은 손대지 않았다.
    - ~~**남은 것(엔진 · §② 관문 필수)**~~ ✅ **완료(2026-08-28, 61330a9)**: `long = winFit(min(P,600))` · `mid = winFit(min(P/2,300))`. **cycle 600봉 창과 같은 처방·같은 구조적 증명** — 백테스트는 ≤600봉만 넣으므로 캡이 no-op(픽스처 87종×3시점 **248 케이스 구·신 바이트 동일** 실측) → 58.1% 불변. 채널 절편은 전체 인덱스 좌표로 되돌려(bRaw −= slopeRaw·startIdx) 소비처 계약 유지, 잔차 σ 도 창 안에서만. **라이브 변화(정직 표기)**: 장기 기울기 NVDA 0.138→0.132 · MSFT 0.071→0.007 · **BTC +0.095→−0.083(부호 반전)** — 20년 로그추세는 상승, 최근 2.4년은 하락. 화면 적합 NVDA·TSLA·BTC '밖→안'(coverage 0→1). 428봉짜리는 불변. ~~⚠ 엔진 백서 CHANGELOG 반영 미완~~ ✅ **완료** — v1.11.1 항목 반영(e765355), 2026-08-31 백서 #proof 전체 현행화(96e9125)에서 지표군·검증축을 레지스트리/validatedAxes 파생으로 돌려 이런 수동 동기 자체를 없앴다.
    - **원 지시(보존)**: **창 정의 자체**(`long = winFit(P)` · `mid = winFit(P*0.5)`)의 재검토. 20년 회귀가 "장기 추세"로 적절한가 — 예컨대 `min(P, 750/250)` 캡. 이건 `blend.slopeLog` → 드리프트 → **채점 판정이 움직인다** → 백테스트·스코어카드 관문(방향 성적 무하락) 통과 후에만. 캡값 선택도 백테스트로. **관문 없이 창을 바꾸지 말 것.**

- ~~**[문서] 스코어카드에 웹↔모바일 비교표 — 같은 엔진, 두 제품**~~ ✅ 완료(2026-08-18): 사용자 요청("스쿱포지·머니스쿱앱의 이용 방식·분석 절차·방법론 차이를 표로"). `forge-scorecard.html` 의 **분석 엔진 구조 바로 뒤**에 섹션 추가 — 엔진을 설명한 직후에 "그 엔진을 쓰는 두 제품"이 오는 순서. 데이터는 페이지 관례대로 JS 배열(`PRODUCTS`) → 표 렌더, 4묶음 **공통(엔진·데이터·표기 규율) / 이용 방식 / 분석 절차 / 방법론 차이**. 공통 묶음은 두 제품 칸을 `colspan=2` 로 합쳐 "여긴 안 갈린다"가 눈에 보이게 했다. 각 행에 **왜 다른가** 한 줄(예: 티어는 "한 번의 분석이 곧 비용이라 단계가 필요해진다").
  - **엔진 CHANGELOG(v1.x)에는 넣지 않았다** — 엔진 변경이 아니라 문서 추가다. 버전 이력에 섞으면 버전별 개선 추이 그래프가 없던 버전을 말하게 된다.
  - ★**갱신 트리거가 이 저장소의 다른 쪽에 있다** — 표는 **모바일 현행 배포 버전**을 적은 것이고, 앱 개편(P0~P5)이 화면·과금·단계를 바꾸는 순간 거짓말이 된다. 그래서 기준일을 `PRODUCTS_ASOF` 상수 하나로 빼고, **갱신 의무를 `mobile/docs/rebuild/PROGRESS.md` 불변 규율 6번**에 걸었다(개편 세션이 매 태스크 읽는 파일). 웹 문서의 수명을 모바일 페이즈에 묶은 것.
  - 검증: `./tests/run.sh` 1546건 통과(문서만 만졌으므로 무회귀 확인용) · 헤드리스 렌더 육안 확인(4묶음·`colspan` 병합·기준일 배지).

- ~~**[차트] 차트 위 플로팅 창 3종을 차트 안으로 — 이동·접기·경계 가둠**~~ ✅ 완료(2026-08-08~09, 2f290a4·6c032aa·ea4afbc·2a666d3, 배포 786317f·fa081b8·2a666d3): 사용자 요구 "도구 패널은 차트 내에 있어야 하고 이동 가능해야 해" → 이어서 "접기 버튼" → "시뮬레이션 HUD랑 프리셋 창도". **셋(`#drawPop`·`#playHud`·`#chartPresetPop`) 다 이미 `.chart-pane` 자식이었는데 클래스가 `position:fixed`라 화면에 붙어 있었다** — ID 규칙으로 `absolute` 전환(공통 `.chart-pop`은 손대지 않음. 그 클래스를 바꾸면 넷이 같이 움직인다). 헤더 드래그는 **뷰포트가 아니라 팬 박스로 가둔다**(헤더 바가 닿지 않는 곳으로 못 나감). 접기 버튼 `–`/`+`는 헤더만 남기고 **접힘 상태를 저장**(상주 창이라 새로고침마다 다시 접게 하면 접기의 의미가 없다). 공통 `_clampPanel(el)` + 팬에 `ResizeObserver` **하나**로 리사이즈·거터·전체화면·패널접기를 한꺼번에 받는다.
  - ★**펼칠 때 재클램프가 핵심** — 접은 채 팬 하단에 두고 펼치면 본문 높이(약 240px)가 한꺼번에 늘어 팬 밖으로 나가고, `.chart-pane`의 `overflow:hidden`에 잘려 **버튼이 보이지도 눌리지도 않는다**. 실측 `top 873 → 664`(아래 끝이 팬 경계에 정확히 맞음).
  - ★**저장 좌표의 의미가 바뀐다** — 기존 `scoopforge_hud_play`·`_preset`은 **뷰포트** 좌표(예: `left:1318px`)라 팬 기준으로 읽으면 밖이다. 가두기만 하면 창이 말없이 구석으로 밀린다 → **새 키**(`play2`·`preset2`)로 옮겨 한 번만 기본 위치로 돌아간 뒤 정상 저장. 옛 키 3종은 부팅 시 제거. 좌표계를 바꿀 땐 키도 바꾼다.
  - **`_alignPanels` 복구 + 도구창 포함** — 도구창만 팬 기준이던 동안은 정렬에서 뺐었다(섞으면 누적 `x += offsetWidth`가 두 좌표계를 섞어 **나머지 창 간격까지 틀어진다**). 셋 다 팬 기준이 되어 이유가 사라져 팬 좌표로 다시 쓰고 도구창을 넣었다. 한 줄이 팬 폭을 넘으면 다음 줄로 접는다 — 안 그러면 `overflow:hidden`이 삼킨다.
  - **죽은 코드 제거** — `#analyzeLog`는 마크업에도 동적 생성에도 없다(로그는 `#playHud` 안 `.ph-logsec`로 흡수). 그 드래그·접기 배선 + `scoopforge_hud_log` 키 + `_alignPanels` 항목 + `.analyze-log` CSS·`@keyframes lgkey` 삭제. 살아있는 `#analyzeLogBody`는 유지.
  - ★**차트 팬이 0px로 눌리던 문제**(창을 차트 안으로 옮기고서야 드러남 — 화면 고정일 땐 차트 폭이 0이어도 창은 멀쩡히 보였다). 원인 둘: ① `initGutter` 상한이 **지표 레일 178px 몫을 안 뺐다** → 레일이 켜져 있으면 차트가 `MINC 340`이 아니라 **162px**까지 눌림(레일은 `hide-rail`·모바일에서 사라지므로 상수 대신 실측 차감) ② `.chart-pane`이 `min-width:0`인데 `.board-pane`은 `flex-shrink:0`이라, 보드560+거터7+레일178=745px를 뺀 나머지를 차트가 다 뒤집어씀. → **차트에 실질 최소폭 340px, 보드가 먼저 양보**(shrink 0→1, 자기 `min-width` 320px까지). 셋 다 최소치면 845px로, 세로 스택 전환선 860px 아래에서만 모자라고 그 구간은 모바일 미디어쿼리가 `min-width`를 0으로 되돌려 받는다. **실측(전→후)**: 창900 기본 **0→340** · 창1600 거터우측끝 165→343 · 창900 거터우측끝 165→340 · 창854 세로스택 844(정상).
  - 검증: 창 3종 각각 팬 안 포함 / ±3000px 드래그 12회 전부 경계 정지 / 로그 120줄+창높이 700px에서 본문 스크롤(scrollHeight 2360 vs client 525) / 거터·뷰포트 축소 재클램프 / 정렬 수직·수평 겹침 0쌍 / **추세선 무장 상태로 세 창 헤더 6회 드래그 시 차트 하위 트리 포인터 이벤트 0건**(창을 옮기다 선이 그어지지 않음) / 전체화면·모바일 414px. forge-tools 81/0 · forge-core 251/0.

- ~~**[차트] 드로잉 도구 2차 — 조작 개선·도구 확장·시각 고급화**~~ ✅ 완료(2026-08-07, c7e8157..16f174e): 1차 배포 후 사용자 요구 5건(연속 그리기·진행 안내·수평/수직선·색굵기·되돌리기 + "트레이딩뷰보다 특별한 느낌"). **연속 그리기**(완성해도 무장 유지, `Esc`/같은 도구 재클릭으로 해제) + 플롯 좌상단 **진행 칩**(단계별 문구, 새 상태 없이 `_armed`/`_drag.stage`에서 파생) / **수평선·수직선**(1클릭 완성, `b` 생략·축 잠금) / **색 5·굵기 3 스와치**(캔버스, `color`·`w` 둘 다 선택적 → 기존 그림 무마이그레이션) / **`Ctrl+Z`** 그리기 전용 스냅샷 스택 30단. **시각은 장식 추가가 아니라 정밀계기 규약 복귀** — 선폭 1.6→`CW.base 1.25`, 점선 `[4,3]`→`CDASH.fine [1,3.5]`+라운드캡, 끝점 속 빈 링, ✕ 배지 평시 alpha .45, 채움 페이드. **트레이딩뷰와 의도적으로 갈라진 둘: 호버는 굵기가 아니라 알파만 / 선택은 굵기가 아니라 아래 깔린 헤일로**("굵기는 데이터의 것, 상태는 빛으로 말한다"). 68/0 + forge-core 251/0. spec/plan `2026-08-06-drawing-tools-v2*`.
  - ★**연속 그리기는 되돌렸다(2026-08-07)** — 완성해도 무장을 유지하던 동작이, 배포본을 써 본 사용자 판단으로 뒤집혔다. 무장이 안 풀리니 차트 위 클릭이 전부 `drawsPointerDown`의 `_armed` 분기(히트테스트보다 앞선다)에 삼켜져 **이미 그린 도형을 마우스로 선택·수정할 수가 없었다** — 도구를 다시 고르는 마찰보다 대가가 컸다. 새 계약: **완성하면 도구가 풀리고 그 도형이 선택 상태로 남는다**(완성 4지점이 `_completeNew()` 한 곳 경유, 무장 UI 갱신은 `_setArmed`로 `drawsArm`과 공유). **취소(`Esc`·같은 자리 `<6px` 재클릭·`Delete`)는 도구를 남긴다** — `_cancelNew`가 `_armed`를 지우면 `drawsArm`의 토글 판정이 뒤집혀 활성 도구 재클릭이 재무장이 된다. 회귀 테스트 11건 추가(79/0). 플래그·수정키로 되살리지 말 것. spec §2.1 정정 참고.
  - **다시실행 미구현** — 최초 스펙이 `Ctrl+Shift+Z`(=`Ctrl+Y`)까지 적었으나 스택이 하나뿐이라 세 키가 모두 undo 를 불렀다(redo 를 기대하고 누르면 이력이 더 사라짐). 사용자 결정으로 **`Ctrl+Z`만 남기고** 나머지는 미바인드(브라우저로 통과). redo 는 후속 과제.
  - ★**Task 4 는 부분 출하였다** — `trend`/`channel`/`range`/`period` 4종에서 색·굵기 스와치가 **장식일 뿐 실제 페인트에 반영되지 않았다**(generic 분기가 `COL[d.type]`·고정 1.6/2.2 사용). 검증이 `drawStyle()` **반환값**만 보고 페인트를 안 봐서 리뷰까지 통과. **교훈: 캔버스 작업은 recording-ctx 심으로 실제 페인트를 단언할 것.** 같은 계열이 '굵기' 축에도 남아 있어 최종 리뷰가 잡음.
  - ★**스와치 행 배치 = 파괴적 오클릭의 온상** — ① 행이 ✕ 히트원과 겹쳐 **색을 누르면 도형이 삭제**(9227974) ② 행이 끝점 핸들을 덮어 **핸들 클릭이 색변경**(정상구간 2.42%) ③ ✕ 히트반경이 페인트보다 2px 넓어 **핸들 클릭이 삭제**(136,880건) ④ hline 본체 위 2px. 해법은 판정 뒤집기가 아니라 **배치**(`_swatchRects` 후보 스캔 — 재정렬로는 ④를 못 고친다, 충돌 상대가 `body`라서). 전수 스윕 위반 **1,726,751 → 0**. 스윕은 `map/forge-tools.sweep.js`(`node forge-tools.sweep.js`, 약 3분 / `--module=<경로>`로 수정 전 사본과 대조) — 배포 제외.
  - **백로그**: 전체화면에서 진행 칩이 `.fc-verdict-bar` 뒤로 가림(Task 2/CSS 교차) · 오른쪽 끝점이 클립 밖일 때 값 라벨만 클램프 없이 잘림 · 배지 여백이 hline/vline 본체 클릭을 삼킴 · `_cancelNew()` 잔여 스냅샷 1개.

- ~~**[차트] 드로잉 도구 4종 + 마그넷**~~ ✅ 완료(2026-08-06, c9e5dbf..33661c8): 추세선·평행채널·등락폭 재기·기간 재기 + 마그넷. **앵커=(날짜, 가격)**이라 일/주/월 전환·줌·로그축을 자동 추종(봉 번호 저장이면 일→주에서 1/5로 어긋남). 신규 `forge-tools.js`(UMD — 순수 헬퍼는 `node --test`, 렌더는 `#fcDraws` pointer-events:none). 포인터는 기존 `fcMainChart` 분기 앞에 위임 한 줄만 끼워 그림 0개·도구 꺼짐이면 종전 팬과 동일. `dc.draws`로 종목별 영구 저장. 툴바는 우측 팝오버(좌측 세로 레일은 시연 HUD 가림 사고 회피). spec/plan `2026-08-06-chart-drawing-tools*`.

- ~~**[데이터] OHLC 폴백 소스 교체 — Stooq(봇차단) → Yahoo Finance**~~ ✅ 완료(2026-08-06): 사용자 "불러오기 실패" 제보(실제로는 `APPL` 오타 — 애플은 `AAPL`) 조사 중 **Stooq가 CSV 대신 JS 프루프-오브-워크 봇 차단 페이지를 반환**하는 것을 확인 → 미국주식 2차 안전망이 죽어 있었고 **TwelveData 단일 소스** 상태였다. TD 무료는 **분당 8회** 한도라 새 종목 연속 조회(워치리스트 신호 스캔 최대 30종목)에서 줄줄이 실패 가능(실제로 KO가 한 번 실패 후 잠시 뒤 정상 = 분당 한도 정황). → `forge-api.php` 폴백을 **Yahoo Finance chart v8**(무키 JSON)로 교체. 주기 매핑 `1day/1week/1month → 1d/1wk/1mo`, range `20y`(월봉 `max`), 결측 봉(null) 스킵, **거래소 `gmtoffset`로 현지 날짜 환산**(UTC 직접 사용 시 개장시각에 따라 하루 밀림), `BTC/USD → BTC-USD` 표기 변환. **주의: Yahoo는 User-Agent 없으면 429** — 기존 `$fetch` 헬퍼가 UA를 붙여 그대로 통과. 라이브 검증: TD가 404를 내는 심볼로 폴백 강제(`7203.T` 일 4915/주 1045/월 328봉, `SAP.DE` 5078봉, `src=yahoo`), `?since=` 계약 유지(6봉 full=false), 기존 경로 무회귀(AAPL·BTC-USD 5014/3265봉 twelvedata, 005930·KOSPI naver), 없는 심볼은 여전히 notfound.
  - **[후속 여지]** TD 429 감지 시 backoff 재시도 · 워치리스트 스캔 분당 한도(8회) 맞춘 간격 제어. 폴백이 생겨 급하진 않음.

- ~~**[성능] OHLC 증분 전송 — 클라↔서버 구간 델타화**~~ ✅ 완료(2026-08-05, b41f382..3f5795f): 사용자 요구("받은 데이터는 서버 저장, 갱신분만 추가") 조사 결과 **서버↔외부 API 증분은 이미 있었음**(`f5ff27b`, TTL+`outputsize=300`+t머지, AAPL 5014봉 누적 실측). 남은 비효율은 **클라↔서버가 매 요청 전량**이었던 것 — `fetchOHLC`가 `cache:"no-store"`·gzip 없음·세션 캐시 없음이라 종목 전환·`rankMomentum` N종목마다 AAPL 394KB 왕복. → ① `forge-api.php` **`?since=` 응답 필터 + `full` 플래그**(비교는 `>=` — `>`면 진행 중 봉의 종가 갱신을 영원히 못 받음. **캐시 파일엔 항상 전량 저장** — 자르면 20년 누적본 소실) ② **응답 gzip**(403,934B→105,575B, 26.1%) ③ 클라 **`fetchOHLC` 세션 캐시 관문**(호출 7곳이 모두 지나는 단일 지점, LRU 12, 일봉 5분/주월 30분, `force`=불러오기 버튼, 반환 shape 불변=항상 누적 전량) ④ 머지 규칙을 `ForgeCore.mergeCandles`로 추출(서버·클라 단일 출처, 251/251). 결과 **394KB → 캐시 히트 0B / 델타 ~242B / 첫 로드 105KB**. 라이브 검증: 델타 3봉·미래날짜 0봉·불량 since→전량·**전량 재요청 5014봉 = 기준선(캐시 무손상)**·실서버 델타 머지가 전량과 완전 일치(1봉·6봉). spec/plan `2026-08-05-ohlc-incremental-transfer*`.
  - **[알려진 잔여]** 증분 도입 이전에 쓰인 캐시 파일은 `full` 키가 없어 TTL 내 `readfile` 직행 시 그대로 나감 — 클라가 `full !== false`로 판정해 전량 교체로 안전하게 떨어지고, TTL 만료(일봉 1h) 시 자연 해소.
  - **[범위 밖]** localStorage 영속(AAPL 하나가 394KB, 쿼터 대비 이득 나쁨) · Naver/Stooq 서버↔외부 증분(Stooq는 전체 CSV 단일 요청, Naver는 400봉 캡).

- ~~**[랜딩] 제품 랜딩 페이지 `map/index.html`**~~ ✅ 완료(2026-07-22): 시안(`Naru Landing.dc.html`) 레이아웃 채용, **서사는 교체**. 시안 히어로의 "방향 적중률 62%"가 스코어카드 실측(**58.1% · 기준선 60.8% 미달**)과 정면 충돌 → "우리는 방향을 맞힌다고 말하지 않습니다 / 대신 얼마나 움직일지는 맞힙니다"로 재구성. KPI=변동성 69.0%·낙폭 67~69%·ECE 1.9%p, ✓/✕ 대비표에 기각 5건(인터마켓·내부자·공매도·PEAD)까지 노출. 가격·플랜 제한·"신용카드 불필요"·가짜 시세 마퀴 전량 삭제(베타 무료만). 라이트/다크 양 테마 완전 토큰(18키 패리티). **브랜드명 미정 → `BRAND` 상수 1곳 격리**(한 줄 교체). 인증 비활성이라 로그인 미노출·CTA는 forge.html 직행. `landing.test.js` 26건(수치-라벨 페어링·죽은 링크 0·금지 카피·토큰 패리티·WCAG 대비 계산·좌측 accent bar 금지). 헤드리스 4장 육안검증. spec/plan `2026-07-22-landing-page*`.
  - **[후속]** 브랜드명 확정(나루 vs 스쿱포지) · 액센트 블루↔골드 결정 · 인증 활성화 후 로그인 버튼 노출 · Basic/Auth/Redesign 시안 3건 적용 여부.

- ~~**[엔진 정리] 실적(종목 이벤트) 축 제거**~~ ✅ 완료(2026-07-17): v1.9.6~1.9.7 실적 인지 축(갭+6.3/급변+3.4/변동성+2.6%p, 유일 실증 외부데이터 승자)을 **엔진에서 완전 제거** — 이유는 정확도 아닌 **리스크**(종목별 라이브 실적일 fetch 의존=외부API·미국주식만·재현불가·불투명). forge-core 증강모델(`_EVF/_ESPK/_EGAP·_earnFeats·earnAug`)·클라 fetch/스레딩/📅배지/알림규칙·PHP `?earndate` 프록시 제거 → 세 예보 **순수 가격 곡선 복귀**. 트레이드오프 갭 −6.3%p. 246/246·잔존참조0·캐시버스터 bump. spec `2026-07-17-remove-earnings-axis-design.md`. **→ 방향-신호 데이터 슬라이스 탐색(내부자·VIX·공매도·PEAD 대형+소형 전부 기각) 종료. 엔진 개선은 작도·UI·검증 규율 축으로 전환.**

0. ~~데이터기반 다중스케일 작도 + 중요도 위계 (Gann 첫 적용)~~ ✅ 완료(2026-07-15, 0137d76..1999d9c): `collectAnchors`(민감도 사다리·고정창 없음)+중요도 스코어 → Gann 다중앵커 부챗살·강조/디밍·윈도우 클램프(창밖 무음 해소). **엔진 불변**(anchors opt-in→run 비용0·bias/baseline 불변). 배포·헤드리스 검증 완료. [[scoopforge-multiscale-drawing]]
  - ~~**[확장]** 같은 위계를 S/R·structure 작도에 적용~~ ✅ 완료(2026-07-16, c9081e7..11a9009): `collectLevels`(스윙 수평 클러스터·터치수/유의도)+`collectStructure`(대/중/소 티어·HH/HL/LH/LL·BOS/CHoCH) 작도 전용 함수 추가(`opts.draw` 게이팅·엔진 bias 불변·비용격리). S/R 강조/디밍+피벗 P만 참고선, structure 3티어. 244/244·opus 최종리뷰 Ready·헤드리스 파이프라인 검증·배포 완료. spec `2026-07-16-multiscale-sr-structure-design.md`.
    - ~~**[남은 확장]** 추세선·fib 위계 통일~~ ✅ 완료(2026-07-16, ecfef88·84ae333): trend 지배창(ta.dominant/최고 R²)만 강조·나머지 디밍·라벨 지배창만·피봇선 디밍정렬 / fib 중·장 bespoke STY를 공통 규약 정렬 + **교차-degree 합류 강조**(단·중·장 겹침=최강 S/R·bold+✦). 작도 전용(엔진 불변)·244/244·sonnet 리뷰 Ready·헤드리스 검증(지배창 라벨·합류 5존)·배포 완료. **→ S/R·구조·추세선·fib 4지표 시각 언어 통일 완료.** spec `2026-07-16-trend-fib-hierarchy-unify-design.md`.
  - ~~**[엔진]** bias/예측을 다중스케일 구조 반영으로~~ ❌ **기각(2026-07-16, d86fe4b)**: structure 첫 검증 랩(`map/backtest/multiscale-struct-lab.js`). run() 기본-off `_msStruct` 플래그로 격리 측정(off==default 245/245·sanity 30/30). walk-forward 55종 4816시점: 다중스케일 구조신호는 단일 대비 미세 우위(+1%p 안팎, 지평·stride 의존적)나 **엔진 방향 순증분 = 정확히 0.0%p 전 지평·LOSO 0/55·전후반 0/0** → structure 드리프트(cap 0.08) 몫이 작아 sign(score) 불변. 5관문 ①④⑤ FAIL. **작도엔 다중스케일 유효, 엔진 방향엔 무효**(‘지표 조합으로 방향 못 바꿈’ 재확인). `_msStruct` 플래그는 기본 off 검증 인프라로 보존. 스코어카드 탐구표 기록. spec `2026-07-16-multiscale-struct-bias-lab-design.md`.
    - **[후속 여지]** pivot/gann/fib/trend의 다중스케일 bias도 같은 결과 예상(구조와 동형·드리프트 각 cap 작음). 재시도 전 "드리프트 몫이 작아 sign 불변"이라는 이번 진단부터 반증 필요.
  - ~~**[엔진] 상대강도 축 다중스케일화**~~ ❌ **기각(2026-07-16)**: rel은 이미 다중 lookback(mom 20/60/120/250·v20/v120). 상대비율 R=P/SPY의 스윙-티어(대/중/소) 5피처 증분(`rel-multiscale-lab.js`·OOS 60/40). base-25 재현(56.2/53.7/54.1%). **증분 Δ +0.4/+0.1/−0.6pp**(부호혼조·+1.5pp 미달) → 상대모멘텀과 중복·기각. spec `2026-07-16-rel-multiscale-feature-lab-design.md`.
  - ~~**[엔진] 실적 축 다중스케일화**~~ ❌ **기각(2026-07-16, 4a8ddc3)**: 실적 근접은 이미 다중버킷(D≤10·D≤20+연속램프). 촘촘/확장 버킷(D≤3·5·40) 증분(`earn-multiscale-lab.js`·earnings-lab 재사용·종목내60/40+종목외LOSO). base 재현(갭67.9·급변68.8·변동성71.4%). **3타깃 전부 증분 −0.1~+0.1%p**(종목외도 0.0·+1%p 관문 미달) → 연속램프가 근접 그래디언트 이미 포착·같은 실적일 재인코딩=중복·기각. spec `2026-07-16-earn-multiscale-bucket-lab-design.md`.
  - ~~**[엔진] 나머지 지표(pivot·gann·volume) 다중스케일**~~ ❌ **기각 확정(2026-07-16, 드리프트 천장 랩)**: bespoke 다중스케일 대신 **일반 상한 증명** — `run()` 기본-off `_biasSet` 플래그로 각 지표 bias를 ±1 극단 강제, `sign(verdict.score)` 뒤집힘 비율=천장. `drift-ceiling-lab.js`(55종·9589시점·sanity OK). **pivot·gann·volume 천장 전부 0.00%(0/9589)** — 극단으로 강제해도 엔진 방향 한 번도 안 바뀜(캡 0.04~0.05 몫 미미). → 다중스케일 포함 **어떤 재공식화도 무효** 수학적 확정. spec `2026-07-16-drift-influence-ceiling-lab-design.md`.
    - **[최종 종결]** 작도 다중스케일 이니셔티브의 엔진 이식 = **구조·상대강도·실적·(피벗/Gann/거래량 천장) 다섯 갈래 전부 무효** 확정. 공통 진단: 다중스케일은 대개 이미 인코딩됨(rel mom·실적 램프·fib/trend degree) or 개별 드리프트 몫 작아 sign 불변(천장 0%). **개별 지표 재공식화로 방향 개선은 근본적으로 불가**(천장 증명). 엔진 방향개선은 **가격 밖 새 데이터 슬라이스**에서만([[scoopforge-earnings-axis]] +6.3pp 유일 실증). **다중스케일 재시도 전면 불필요.**
  - ~~**[폴리시]** emph 앵커 reason 라벨이 예측 콘 배지와 겹침(occlusion) — 라벨 위치 오프셋.~~ ✅ 완료(2026-07-19): 생 `fillText`(회피 미참여) → 공용 `_evLabel` 위임 — 축 눈금·현재가 pill·콘 배지가 예약된 `_evLabelBoxes`와 충돌 시 **빈 슬롯으로 자동 회피**하고 자기 박스도 등록(다른 라벨도 이를 피함) + pill 배경으로 가독↑. Gann reason("대형 스윙" 등)이 `_KEYLBL` 비매칭이라 key 모드서 사라지는 회귀를 막으려 `_evLabel(..., force)` 인자 추가(강조 앵커만 true). 실소스 추출 검증 4케이스 통과(force 표시·비force 생략 유지·all모드·박스 등록). · 그린 1×1(collectAnchors[0])이 엔진 bias 앵커와 다를 수 있음(설계상 의도, 인지).
1. **[대기: 사용자] GCP OAuth "웹 애플리케이션" 클라이언트 재생성** — 리디렉션 URI `https://parksvc.mycafe24.com/map/forge-auth.php`. JSON 다운로드되면 자동 감지→검증→업로드→활성화.
2. **[1 이후] 인증 실계정 E2E** — admin 첫 로그인 → 레거시 포지 이관 확인 → 라이브 검증.
3. **결제 스펙**(토스페이먼츠/포트원 정기결제) — 유사투자자문업·통신판매업 신고 진행과 연동. 브레인스토밍부터.
4. **[오프라인: 사용자] 유사투자자문업 신고 준비** — 협회 교육 이수 → 금감원 신고. 전문가 확인 항목 6개(`2026-07-13-monetization-tier1-legal.md` §4).

## 📋 예정 (우선순위 미정 — 배치 투하 시 여기 삽입)

- **[데이터 버그] `ETH/USD` 월봉 오염 — OHLC 프록시/캐시**
  월봉(`tf=1month`) 요청에 **주 단위 봉 548개**가 돌아온다(최근 날짜 `2026-08-01 · 08-03 · 08-10 · 08-17`).
  같은 요청에서 `BTC/USD`는 144봉으로 정상이라 **종목별 캐시 파일 문제**로 보인다(`forge_ohlc_cache_*.json`, `source: yahoo+inc`).
  증분 병합(`+inc`) 경로가 월봉 캐시에 주봉을 덧붙였을 가능성.
  - 영향: 포지 4패널의 **ETH 월봉 판정과 TF 매트릭스 월 열이 전부 틀린 값**. 발견 경로는 앱 요건정의의 샘플 종목 국면 실측(2026-08-19).
  - **PC 서버 파일(`forge-api.php`) 쪽 문제이므로 여기서 고치면 모바일은 같은 서버를 쓰기 때문에 자동으로 따라온다** — 앱 쪽 작업 없음.
  - 캐시는 배포 불가침이라 임의 삭제 전에 원인부터 확인할 것(지우면 재현이 사라진다).
  - 재현: `curl "https://parksvc.mycafe24.com/map/forge-api.php?ohlc=1&symbol=ETH%2FUSD&tf=1month"` · 대조군 `symbol=BTC%2FUSD`

- ~~**[새 데이터 슬라이스] 내부자 매수 중소형주 재검증**~~ ❌ **기각(2026-07-16)**: 대형주 기각(98% 매도)이 유니버스 문제라, 내부자 매수 90%+인 중소형주 39종 선정(DERA 마이닝)·IJR 소형주지수 대비 재검증(`insider-smallcap-lab.js`·32k행). **증분 +0.3~−0.2pp·LOSO 18/39·전후반 음수·단변량<50%** → 매수 신호가 넘치는 유니버스에서도 무증분(생존편향이 유리한데도 null=강한 기각). **내부자거래(Form4)는 진짜 새 데이터지만 방향 예측엔 무효 — 대형·중소형 두 유니버스로 확정.** 인프라·데이터 보존([[scoopforge-insider-slice]]). spec `2026-07-16-insider-smallcap-validation-design.md`.
  - ~~**[열린 방향] 옵션 포지셔닝 슬라이스**~~ ❌ **기각(2026-07-17)**: 종목별 put/call·dealer gamma는 전체 체인 OI 이력=유료라 막힘(선행 "종목별 옵션 막힘" 재확인). 무료 대체 VIX 기간구조(FRED VIXCLS/VXVCLS)를 변동성 예보 증분 검증(`vix-vol-lab.js`·`vix-verify-all.js`). realized-vol만 위에선 갭+1.2·변동성+1.3pp(통과처럼)이나 **적대적 재검증(deployed base=vol+earn 위)**: 3타깃 전부 증분 +0.3~0.6pp(관문 미달)·후반 음수 → **실적 신호와 중복** 기각. VIX=시장 1시계열이라 종목별 증분도 약함. spec `2026-07-17-vix-termstructure-vol-lab-design.md`.
    - **[방법론 교훈]** 증분은 반드시 **deployed base 위에서** 측정(맨바닥=vol-only 위 착시를 적대적 검증이 걸러냄). 이후 모든 슬라이스 랩 이 원칙 적용.
  - ~~**[열린 방향] 공매도잔고 슬라이스**~~ ❌ **기각(2026-07-17)**: FINRA 공매도잔고(무료·격주·공시일 lag) 고공매도 51종(DTC 8~24·`mine/collect/fetch/lab-shortint`)·IJR 벤치·2019~2026. 날것 dtcLevel 45.9%(공매도자 약弱 informed)이나 **증분 −0.3~+0.3pp·LOSO 20/51·전후반 엇갈림** → 가격/모멘텀에 흡수·기각. 서바이버십(상폐=숏승리 누락)로 신호 과소평가 가능하나 극단은 거래불가. spec `2026-07-17-shortinterest-rel-slice-design.md`.
    - **[종합]** 포지셔닝 데이터(내부자·공매도) 3검증 전부 순증분 0 — **진짜 새 정보라도 이미 가격/모멘텀에 반영**. 가격 밖 승자는 **실적일(비공선 이벤트)** 뿐.
  - ~~**[실적 축 심화] PEAD(실적후 드리프트) 대형주**~~ ❌ **기각(2026-07-17)**: 실적일 반응(발표전후 수익률)→수주 드리프트를 rel25 위 증분(`pead-feats.js`/`pead-rel-lab.js`·earn-ohlc 30대형주·SPY). **날것 원신호 50.2%(창내)·증분 −0.7~+0.3pp·LOSO 12/30·전후반 −0.6/−0.6** → 대형주 PEAD 소멸(차익거래·문서화된 감쇠)+대형주는 SPY와 동조라 상대방향 자체 코인플립(base~50%). 가이던스·IV crush는 무료데이터 없어 막힘. spec `2026-07-17-pead-rel-lab-design.md`.
    - ~~**[실적 축 심화] PEAD 소/중형주**~~ ❌ **기각(2026-07-17)**: 대형주 소멸은 차익거래 탓이라, PEAD가 강하다는 중소형주(고SI 51종·바이오/밈/성장)서 재검증. **실적일=EDGAR 8-K item 2.02 전체이력 수집**(`collect-earnings-edgar.js`·45/51종·2170건·다수 2004년~)·IJR 벤치마크(`pead-smallcap-lab.js`·pead-feats 재사용). **원신호 49.0%(창내·동전이하)·증분 최대+0.2/최소−0.4pp·LOSO 15/51·전후반 −0.7/−0.1** → 소형주 base rel25는 53~55%(대형50%보다 예측여지 있음)이나 그 예측력은 구조·모멘텀서 나올 뿐 실적반응 신호는 무증분(흡수). **PEAD 대형·소형 두 유니버스로 무효 확정.** 서바이버십(상폐 소형주 누락) 경고. spec/plan `2026-07-17-pead-smallcap-lab`.
  - **[열린 방향] 새 데이터 슬라이스 탐색**: 실적일이 유일 승자(실적 인지 축 +6.3pp). 내부자·VIX·공매도·PEAD(대형+소형) 전부 기각 — **방향-신호 후보 소진**. 남은 무료 여지: 다른 비공선 이벤트(FDA 승인일 바이오·인덱스 리밸런싱 강제매매). 옵션체인·가이던스·에널추정=유료막힘. 재개 시 브레인스토밍.


- ~~Gann 각도 지표(범주 A 연속 지표)~~ ✅ 완료(2026-07-15, 4cea06c..515716e + 스코어카드 3d24279): ATR 정규화 부채꼴·레일 선택형·기본전략 미편입(백테스트 베이스라인 불변)·cap 0.05. 배포·라이브 검증 완료. 설계 `specs/2026-07-15-chart-geometry-patterns-design.md`.
- ~~차트 패턴 블록(범주 B) + 구조·패턴 성격 태그~~ ✅ 완료(2026-07-15, 526346e..07bc19b): H&S·역H&S·불/베어 플래그(`detectSwings` 재사용·감지 시만 작도). 오탐 게이팅 강화(랜덤워크 32%→7.5%). structure·elliott·smc·pattern에 '구조·패턴' 태그. 레일 선택형·기본전략 미편입(baseline 불변). 배포·헤드리스 검증 완료.
  - ~~**[폴리시]** 레일 '구조·패턴' 태그 폭이 지표명을 truncate~~ ✅ 완료(2026-07-17): 텍스트 태그 "구조·패턴"(~40px) → 컴팩트 SVG peaks 아이콘 배지(15px)로 아이콘화 + ir-wval 27→24px·태그 여백 5→3px. 4행(structure/elliott/smc/pattern) 전부 이름 온전 표시(헤드리스 trunc:false 확증·스마트머니 포함). 툴팁 유지.
  - **[Gann 후속]** ~~1×1이 ATR라 부챗살이 가격에서 뜸~~ ✅ 수정(2026-07-15, 90a0031: 1×1=지배 추세 기울기). ~~남은 별건: 앵커가 가시 윈도우 왼쪽 밖이면 `fiToX(aIdx)`=NaN → Gann 팬 무음 미표시~~ ✅ **이미 해결됨 — 항목이 stale이었음(2026-07-19 확인)**: 클램프가 `a1d8354`(Gann 다중앵커 + "윈도우 클램프(창밖 무음 해소)")에 이미 포함. `_drawGannLayers`가 `startFi = Math.max(fiMin, an.idx)`로 좌단 클램프하고 `y1`을 각도로 외삽해 **좌단 진입점부터 작도**. 실소스 로직 검증: 창밖 `idx=40 → startFi=100·x1=8.0(유한)·정상 작도` vs 옛 방식 `fiToX(40)=NaN(무음)`. 창 오른쪽 밖(미래) 앵커는 스킵이 정상.
  - **[확장 여지]** 차트 패턴 라이브러리: 이중천장/바닥·삼각수렴·쐐기·컵&핸들. Gann 시간축.
  - **[승격 후보]** Gann·차트 패턴 실사용 엣지 실측되면 기본전략 편입·백테스트 축 등록(별도 브레인스토밍).
- 라이브 원장 첫 실전 성적 리뷰(만기 표본 누적 대기, ~2026-08 초).
- 1h 자체 지평 변동성 예보(데이터 확보됨 — 단타 도구 제품 질문 정리 후).
- CPI 캘린더 2027년분 갱신(MACRO_CAL — 2026-12 소진 전).
- 사용자별 비전 큐 분리(인증 활성 후).
- **[보류: 나중에] "사용자 선택 기준/시각" 웹분석 UI (R3 재정의)** — 원래 R3(회고 대장→재조합 후보 토글)는 토대(지표 재조합)가 +0.00pp로 종결돼 연료 0. 컨셉("사용자가 기준 골라 분석이 달라짐")은 **검증된 축 위에서 새로 정의**: ①방향 벤치마크 선택(절대/시장SPY/섹터ETF — 섹터 +3.0~3.9pp) ②예측 지평 선택(2주/1달/2달). 착수 시 브레인스토밍부터. 부분 선례=범례 체크→2차 예측선(`_get2ndPred`).
- ~~회고 존 v2 실데이터 재수집→종결 대장~~ ✅ 완료(2026-07-14): drop·add 전 국면 +0.00pp, **membership 종결 확정**. 스코어카드 탐구표 갱신.
- **[결정됨 2026-07-14] 지표 재조합(넣고빼기/재가중) 방향 개선 노선 종결.** v1(빼기)+v2(넣기) 실측이 전부 무영향 → **v3 combination은 착수 안 함(사용자 결정 B: 마무리).** 방향은 이미 통하는 상대방향(시장·업종 대비, [[scoopforge-rel-axis]])으로 우회하는 현 노선 유지. 회고 존 인프라(map/backtest/retro/)는 "재조합=무효"를 증명한 도구로 보존. (승격 게이트 강화는 채택 후보가 나올 때만 필요 — 현재 채택 0이라 비활성.)
- **회고 존 게이트 강화(승격 전 필수)**: 자명규칙 전체(지속성·±모멘텀·안티드리프트)+BSS+LOSO를 `gate.js`에 추가 — v1 축소본 마감. 채택 후보가 실측된 뒤 착수.
- 회고 존 v2: downweight·누락지표(add) 재조합 + overconfident/missing 진단.

## ✅ 완료 (최근)

- **구간 고점 대비 낙폭 기본 표기**(2026-08-05, 05a8116·06b2e33) — 4패널 가격차트에 '보이는 범위(`_chartWin`) 최고가 대비 현재가' 상시 표기. **실제 고점 봉 위**에 `고점 {가격} · −N%` 라벨(고점권이면 `구간 고점 {가격}`) + 고점 헤어라인·▼ 캡 마커. OHLC 있으면 고가, 없으면 종가 기준. 과거로 팬하면 그 구간 끝 종가가 기준. 세로 스케일이 고점을 잘라내면 상단 클램프 + `▲` 접두. 값은 `_mainGeo.dd`로 노출(후속 재사용). **작도 전용·엔진 불변**(246/246). 헤드리스 4케이스(하락·고점권·라이트테마·예측콘+범례) 육안검증.
  - 함정 2건(첫 배포가 사용자 화면에 안 보였던 원인): ① `forge.html`의 `?v=` **캐시버스터를 안 올리면 파일만 올려도 옛 스크립트가 쓰임** — 정적 JS 수정 시 필수. ② 좌상단은 예측 범례(`.fc-legend`, absolute·z-index 6)가 덮음 → 차트 구석 표기는 범례 점유를 먼저 확인할 것.

- **예측 불확실성 지형** — 확률 감쇠 레일(하단)+분위수 팬(50%/68%)+신뢰 지평 이후 점묘 해체+끝점 캘리브레이션 확률+범례 6토글. **작도 전용·엔진 불변**(246/246 유지, 정확도 변화 없음). 구현 중 설계 정정 3건(정보량 z→밴드 상대확장 / 레일 숫자 saturate 제거·하단 이동 / 지평선 토글 종속) 스펙에 기록. spec `2026-07-21-prediction-uncertainty-terrain-design.md`, plan `2026-07-21-prediction-uncertainty-terrain.md`

- 회고 존 v2(누락 add + up 기반 membership 종결) — 8모듈 in-place 진화·28/28·opus Ready to merge. **후속: 실데이터 재수집(~3h)→종결 대장.**
- 회고 존 v1(오류귀속 지표 재조합 파이프라인·격리 백테스트·정직 게이트) — 8모듈 TDD, 최종리뷰 Ready to merge. 실데이터 실측: drop 방향개선 0(55종·9,369). **승격 전 후속**: 게이트에 자명규칙/BSS 추가
- 인증 마감 3건(401 중도 강등·부팅 토스트 억제·로그인 유도 펄스) — 8c474f2
- 인증 v1(무중단 스위치) — c3d6d19
- FOMC/CPI 캘린더 알림 + 법적 브리핑 — 3e8ed63
- 매크로 캘린더 축 기각 — 3a06134 · 인트라데이 기각 — b212bb5
- 신호 알림 자동화 — fbcc160 · 상태 신호 UX — e0c3b39
- 라이브 트랙레코드 표면화 — e10fb60
- v1.11 섹터 상대강도 — d681ef9 · 워치리스트 확률 순위 — 75354a2
- v1.10 시장 상대강도 + 3트랙 실측 — 4b04719

## [엔진 철학 · 이후 과제] 분석 비결정성 (2026-08-25 사용자 요건)
스쿱엔진은 현재 결정론적 순수 함수(같은 입력=같은 출력, 실측 3회 동일). 사용자 요건: 매번 해석이 달라져야 하며 동일 조건 결과 재현/캐시 금지. 채점 원장(예측 안정성 전제)과 얽혀 있어 §② 프로토콜·§15 협의 필요 — 비결정 도입 지점·채점 대상 정의·정직 표기·백테스트 관문을 함께 정한 뒤 착수. 상세: docs/design-v2/PROGRESS.md 이월.
