forge_td_key.txt
forge_ohlc_cache_*.json

# 백테스트 대용량 생성 캐시(피처/레코드) — 재생성 가능, 저장소 미포함
backtest/support-features.json
backtest/confluence-records.json
backtest/regime-hold-records.json
backtest/cone-precision-recs.json
backtest/retro/retro-records.json
backtest/retro/retro-catalog.json

# SDD 세션 스크래치(렛저·브리프·리뷰 diff) — 저장소 미포함
.superpowers/
__pycache__/

# PotFlow 로컬 데이터(문서·썸네일 캐시) — 사용자 생성, 저장소 미포함
potflow/potflow_data.json
potflow/potflow_thumbs/

# 라이브 트랙레코드 원장(서버 생성) — 불가침
forge_predlog.json
backtest/cone-cal-recs.json

# 실적일 캐시(서버 생성·Nasdaq) — 불가침
forge_earn_cache_*.json

# 실적 검증 데이터(yfinance 수집·대용량) — 재수집 가능
backtest/earn-ohlc.json
backtest/earnings-dates.json
backtest/earn-surprise.json
backtest/earn-div.json

# 8-K 이벤트 검증 데이터(SEC EDGAR 수집) — 재수집 가능(collect-8k.js)
backtest/8k-events.json

# 머니스쿱 모바일 — 생성물(엔진 사본은 sync-engine.mjs 가 만든다)
_archive/mobile-2026-08-19/node_modules/
_archive/mobile-2026-08-19/www/vendor/
_archive/mobile-2026-08-19/android/build/
_archive/mobile-2026-08-19/android/app/build/
_archive/mobile-2026-08-19/android/.gradle/
_archive/mobile-2026-08-19/android/local.properties
_archive/mobile-2026-08-19/android/app/src/main/assets/public/

# 브라우저 관문 — 자체서명 인증서·공유 라이브러리 캐시(생성물, 재생성 가능)
_archive/mobile-2026-08-19/tools/.gate/

# 브라우저 관문 — 프로브 페이지(gate-browser.mjs 의 probe() 가 www/ 에 만들었다 try/finally
# 로 지운다, 정상 종료 시엔 없다). 크로미움이 중간에 죽는 등 비정상 종료 시 남을 수 있어
# 프로덕션 디렉터리에 실수로 커밋되는 것을 막는다(리뷰 Minor)
_archive/mobile-2026-08-19/www/__gate_*.html

# 시안 원본 압축본·해제본 — 산출물이 아니라 입력이므로 커밋하지 않는다(용량)
docs/design-v2/raw/
docs/design-v2/*.zip
scan/scanner.config.json
