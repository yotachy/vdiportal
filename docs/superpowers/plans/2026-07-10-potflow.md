# PotFlow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `map.html` 기반의 로컬 동영상 노드 관리 도구 `potflow.html` + Python 로컬 헬퍼(`potflow-helper.py`)를 만들어, 노드(마우스 선택)로 PotPlayer 재생·동시재생(타일 배치)하고 오른쪽 파일탐색기로 로컬 폴더를 탐색한다.

**Architecture:** UI는 `map.html`을 복사·개조한 단일 HTML(`potflow.html`). 로컬 실행·파일접근·썸네일·문서저장은 Python 표준라이브러리 헬퍼(`potflow-helper.py`, `localhost:8770`)가 담당하고 정적 파일도 서빙한다. 브라우저는 `fetch`로 헬퍼 API(`/ping /tree /thumb /play /doc`)를 호출한다.

**Tech Stack:** Vanilla HTML/CSS/JS(빌드도구 없음), Python 3 표준라이브러리(`http.server`, `subprocess`, `ctypes`), 외부 명령 `PotPlayer`·`ffmpeg`.

## Global Constraints

- 외부 라이브러리·프레임워크·번들러 금지. Python은 **표준라이브러리만**.
- 단일 HTML 파일 유지(`potflow.html`). 기존 `map.html`·`map_data.json`·`map_images.json` 등 map 자산은 **수정 금지**.
- **좌측 accent 라인(accent bar/rail, `box-shadow:inset Npx 0 0`, `::before` 세로 마커) 절대 금지.** 활성/선택 표시는 배경·텍스트·체크·아웃라인으로만.
- UI 텍스트 한국어. 골드(`--gold`)/네이비 토큰 재사용. 들여쓰기 2 spaces, 큰따옴표, 케밥케이스.
- 배포 불가침(git·배포 파이프라인엔 코드만): `potflow_data.json`, `potflow_thumbs/`. → `.gitignore`에 추가.
- 헬퍼 응답 헤더 `Access-Control-Allow-Origin: *`(로컬 전용).
- 헬퍼 CONFIG(파일 상단 상수): `PORT=8770`, `POTPLAYER_PATH`, `FFMPEG_PATH`, `THUMB_DIR="potflow_thumbs"`, `DATA_FILE="potflow_data.json"`, `VIDEO_EXTS`.
- 대상 플랫폼: Windows(창 타일 배치는 user32/ctypes). 비Windows는 타일 없이 실행만.

---

## File Structure

- `map/potflow-helper.py` — 로컬 헬퍼 HTTP 서버. 라우팅 + 정적 서빙 + `/ping /tree /thumb /play /doc`. 순수 함수(스캔·타일 기하·썸네일 커맨드 생성)는 테스트 대상.
- `map/test_potflow_helper.py` — 헬퍼 순수 함수 pytest.
- `map/potflow.html` — UI. `map.html` 복사 후 리브랜드·키분리 + `videoPath` 필드 + 오른쪽 파일탐색기 + 재생 트리거 + 헬퍼 클라이언트.
- `map/potflow_data.json` — 헬퍼가 관리하는 문서 저장(런타임 생성, 커밋 제외).
- `map/potflow_thumbs/` — 썸네일 캐시(런타임 생성, 커밋 제외).
- `.gitignore` — 위 두 항목 추가.

테스트 러너: `python -m pytest`(설치돼 있다고 가정). 없으면 각 태스크 검증 커맨드에 `python -m pytest || python -m unittest` 대안 명시.

---

## Task 1: 헬퍼 스켈레톤 + /ping + 정적 서빙

**Files:**
- Create: `map/potflow-helper.py`
- Test: `map/test_potflow_helper.py`

**Interfaces:**
- Produces: `find_exe(candidates:list[str]) -> str|None` — 후보 경로/이름 중 실행가능한 첫 항목 반환(절대경로면 존재확인, 이름이면 `shutil.which`). `PONG` 딕셔너리 생성 `ping_payload() -> dict`(`{"ok":True,"potplayer":bool,"ffmpeg":bool}`).
- Produces: `make_server(port:int) -> http.server.HTTPServer` — 라우팅 핸들러 바인딩.

- [ ] **Step 1: Write the failing test**

`map/test_potflow_helper.py`:
```python
import importlib.util, pathlib
spec = importlib.util.spec_from_file_location("helper", pathlib.Path(__file__).parent / "potflow-helper.py")
helper = importlib.util.module_from_spec(spec); spec.loader.exec_module(helper)

def test_find_exe_returns_none_for_missing():
    assert helper.find_exe(["definitely-not-a-real-exe-xyz"]) is None

def test_find_exe_finds_python():
    import sys
    assert helper.find_exe([sys.executable]) == sys.executable

def test_ping_payload_shape():
    p = helper.ping_payload()
    assert p["ok"] is True
    assert set(p) == {"ok", "potplayer", "ffmpeg"}
    assert isinstance(p["potplayer"], bool) and isinstance(p["ffmpeg"], bool)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: FAIL (module load error / attributes not defined)

- [ ] **Step 3: Write minimal implementation**

`map/potflow-helper.py`:
```python
#!/usr/bin/env python3
"""PotFlow 로컬 헬퍼 — 정적 서빙 + 재생/탐색/썸네일/문서저장."""
import os, sys, json, shutil, subprocess
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
from urllib.parse import urlparse, parse_qs

# ── CONFIG ────────────────────────────────────────────────
PORT = 8770
POTPLAYER_PATH = os.environ.get("POTPLAYER_PATH") or r"C:\Program Files\DAUM\PotPlayer\PotPlayerMini64.exe"
FFMPEG_PATH = os.environ.get("FFMPEG_PATH") or "ffmpeg"
ROOT = os.path.dirname(os.path.abspath(__file__))
THUMB_DIR = os.path.join(ROOT, "potflow_thumbs")
DATA_FILE = os.path.join(ROOT, "potflow_data.json")
VIDEO_EXTS = {".mp4", ".mkv", ".avi", ".mov", ".wmv", ".webm", ".flv", ".m4v", ".ts", ".mpg", ".mpeg"}

def find_exe(candidates):
    for c in candidates:
        if not c:
            continue
        if os.path.isabs(c) and os.path.isfile(c):
            return c
        w = shutil.which(c)
        if w:
            return w
    return None

def ping_payload():
    return {
        "ok": True,
        "potplayer": find_exe([POTPLAYER_PATH]) is not None,
        "ffmpeg": find_exe([FFMPEG_PATH]) is not None,
    }

class Handler(BaseHTTPRequestHandler):
    def _send(self, code, body, ctype="application/json", raw=False):
        data = body if raw else json.dumps(body).encode()
        self.send_response(code)
        self.send_header("Content-Type", ctype)
        self.send_header("Access-Control-Allow-Origin", "*")
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)

    def do_GET(self):
        u = urlparse(self.path)
        if u.path == "/ping":
            return self._send(200, ping_payload())
        # 정적 서빙
        rel = u.path.lstrip("/") or "potflow.html"
        fp = os.path.join(ROOT, rel)
        if os.path.isfile(fp) and os.path.commonpath([ROOT, os.path.abspath(fp)]) == ROOT:
            ctype = "text/html" if fp.endswith(".html") else "application/octet-stream"
            with open(fp, "rb") as f:
                return self._send(200, f.read(), ctype, raw=True)
        return self._send(404, {"ok": False, "error": "not found"})

    def log_message(self, *a):
        pass

def make_server(port):
    return ThreadingHTTPServer(("127.0.0.1", port), Handler)

if __name__ == "__main__":
    os.makedirs(THUMB_DIR, exist_ok=True)
    srv = make_server(PORT)
    print(f"PotFlow helper: http://localhost:{PORT}/potflow.html")
    srv.serve_forever()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add map/potflow-helper.py map/test_potflow_helper.py
git commit -m "feat(potflow): Python 헬퍼 스켈레톤 + /ping + 정적 서빙"
```

---

## Task 2: /tree 폴더·동영상 스캔

**Files:**
- Modify: `map/potflow-helper.py`
- Test: `map/test_potflow_helper.py`

**Interfaces:**
- Produces: `scan_tree(path:str) -> dict` → `{"ok":True,"path":abspath,"parent":str|None,"folders":[{"name","path"}],"files":[{"name","path","size"}]}`. 폴더는 이름순, 파일은 `VIDEO_EXTS`만 이름순. 접근불가/미존재면 `{"ok":False,"error":...}`.
- Consumes: `VIDEO_EXTS`.

- [ ] **Step 1: Write the failing test**

`test_potflow_helper.py`에 추가:
```python
def test_scan_tree_lists_videos_and_folders(tmp_path):
    (tmp_path / "sub").mkdir()
    (tmp_path / "a.mp4").write_bytes(b"x")
    (tmp_path / "note.txt").write_text("no")
    r = helper.scan_tree(str(tmp_path))
    assert r["ok"] is True
    assert [f["name"] for f in r["folders"]] == ["sub"]
    assert [f["name"] for f in r["files"]] == ["a.mp4"]
    assert r["files"][0]["size"] == 1

def test_scan_tree_missing_path():
    r = helper.scan_tree("/no/such/path/xyz-123")
    assert r["ok"] is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: FAIL (`scan_tree` not defined)

- [ ] **Step 3: Write minimal implementation**

`potflow-helper.py`에 `find_exe` 아래 추가:
```python
def scan_tree(path):
    try:
        ap = os.path.abspath(path)
        if not os.path.isdir(ap):
            return {"ok": False, "error": "not a directory"}
        folders, files = [], []
        for name in sorted(os.listdir(ap), key=str.lower):
            fp = os.path.join(ap, name)
            if os.path.isdir(fp):
                folders.append({"name": name, "path": fp})
            elif os.path.splitext(name)[1].lower() in VIDEO_EXTS:
                try:
                    size = os.path.getsize(fp)
                except OSError:
                    size = 0
                files.append({"name": name, "path": fp, "size": size})
        parent = os.path.dirname(ap)
        return {"ok": True, "path": ap, "parent": parent if parent != ap else None,
                "folders": folders, "files": files}
    except OSError as e:
        return {"ok": False, "error": str(e)}
```
`do_GET`의 `/ping` 분기 아래(정적 서빙 위)에 추가:
```python
        if u.path == "/tree":
            qs = parse_qs(u.query)
            return self._send(200, scan_tree(qs.get("path", [ROOT])[0]))
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
git add map/potflow-helper.py map/test_potflow_helper.py
git commit -m "feat(potflow): /tree 폴더·동영상 스캔"
```

---

## Task 3: /thumb ffmpeg 썸네일 (커맨드 + 캐시)

**Files:**
- Modify: `map/potflow-helper.py`
- Test: `map/test_potflow_helper.py`

**Interfaces:**
- Produces: `thumb_path_for(video_path:str) -> str` — `THUMB_DIR` 안 캐시 jpg 절대경로(입력 경로 해시 기반, 확장자 `.jpg`).
- Produces: `ffmpeg_thumb_cmd(ffmpeg:str, video_path:str, out:str) -> list[str]` — `[ffmpeg,"-y","-ss","5","-i",video,"-frames:v","1","-vf","scale=320:-1",out]`.
- Produces: `get_thumb(video_path:str) -> tuple[bytes|None, str]` — 캐시 있으면 즉시, 없으면 ffmpeg 실행 후 바이트/에러. (테스트는 앞 두 순수함수만.)

- [ ] **Step 1: Write the failing test**

```python
def test_thumb_path_stable_and_in_dir():
    p1 = helper.thumb_path_for(r"D:\v\a.mkv")
    p2 = helper.thumb_path_for(r"D:\v\a.mkv")
    assert p1 == p2 and p1.endswith(".jpg")
    assert os.path.abspath(helper.THUMB_DIR) in os.path.abspath(p1)
    assert helper.thumb_path_for(r"D:\v\b.mkv") != p1

def test_ffmpeg_thumb_cmd_shape():
    cmd = helper.ffmpeg_thumb_cmd("ffmpeg", "a.mkv", "out.jpg")
    assert cmd[0] == "ffmpeg" and "a.mkv" in cmd and cmd[-1] == "out.jpg"
    assert "-frames:v" in cmd and "1" in cmd
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: FAIL (functions not defined)

- [ ] **Step 3: Write minimal implementation**

```python
import hashlib

def thumb_path_for(video_path):
    h = hashlib.md5(video_path.encode("utf-8")).hexdigest()
    return os.path.join(THUMB_DIR, h + ".jpg")

def ffmpeg_thumb_cmd(ffmpeg, video_path, out):
    return [ffmpeg, "-y", "-ss", "5", "-i", video_path,
            "-frames:v", "1", "-vf", "scale=320:-1", out]

def get_thumb(video_path):
    out = thumb_path_for(video_path)
    if os.path.isfile(out) and os.path.getsize(out) > 0:
        with open(out, "rb") as f:
            return f.read(), ""
    ff = find_exe([FFMPEG_PATH])
    if not ff or not os.path.isfile(video_path):
        return None, "ffmpeg or video missing"
    os.makedirs(THUMB_DIR, exist_ok=True)
    try:
        subprocess.run(ffmpeg_thumb_cmd(ff, video_path, out),
                       stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=30)
    except Exception as e:
        return None, str(e)
    if os.path.isfile(out) and os.path.getsize(out) > 0:
        with open(out, "rb") as f:
            return f.read(), ""
    return None, "thumb failed"
```
`do_GET`에 추가(`/tree` 아래):
```python
        if u.path == "/thumb":
            qs = parse_qs(u.query)
            data, err = get_thumb(qs.get("path", [""])[0])
            if data:
                return self._send(200, data, "image/jpeg", raw=True)
            return self._send(404, {"ok": False, "error": err})
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: PASS (7 passed)

- [ ] **Step 5: Commit**

```bash
git add map/potflow-helper.py map/test_potflow_helper.py
git commit -m "feat(potflow): /thumb ffmpeg 썸네일 + 캐시"
```

---

## Task 4: /play 실행 + 타일 기하 + Windows 창 배치

**Files:**
- Modify: `map/potflow-helper.py`
- Test: `map/test_potflow_helper.py`

**Interfaces:**
- Produces: `tile_rects(n:int, screen_w:int, screen_h:int) -> list[tuple[int,int,int,int]]` — n개 창의 `(x,y,w,h)` 균등 격자. n=1→전체, n=2→좌우, n∈{3,4}→2x2(3은 3칸 사용), 그 이상 `ceil(sqrt)` 격자.
- Produces: `launch_players(paths:list[str]) -> dict` → `{"ok":True,"launched":k}`; PotPlayer 없으면 `{"ok":False,...}`. Windows에서 실행 후 `arrange_windows(pids, rects)` 시도(ctypes, 실패는 무시).
- Consumes: `find_exe`, `POTPLAYER_PATH`.

- [ ] **Step 1: Write the failing test**

```python
def test_tile_rects_counts():
    assert len(helper.tile_rects(1, 1920, 1080)) == 1
    assert len(helper.tile_rects(4, 1920, 1080)) == 4
    for r in helper.tile_rects(4, 1920, 1080):
        assert len(r) == 4 and all(isinstance(v, int) for v in r)

def test_tile_rects_two_side_by_side():
    rects = helper.tile_rects(2, 1000, 800)
    assert rects[0][0] == 0 and rects[1][0] == 500  # x 좌/우
    assert rects[0][2] == 500 and rects[1][2] == 500  # 각 폭 절반

def test_tile_rects_full_when_one():
    assert helper.tile_rects(1, 1000, 800)[0] == (0, 0, 1000, 800)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: FAIL (`tile_rects` not defined)

- [ ] **Step 3: Write minimal implementation**

```python
import math

def tile_rects(n, screen_w, screen_h):
    n = max(1, n)
    if n == 1:
        return [(0, 0, screen_w, screen_h)]
    if n == 2:
        w = screen_w // 2
        return [(0, 0, w, screen_h), (w, 0, screen_w - w, screen_h)]
    cols = math.ceil(math.sqrt(n))
    rows = math.ceil(n / cols)
    cw, ch = screen_w // cols, screen_h // rows
    rects = []
    for i in range(n):
        r, c = divmod(i, cols)
        rects.append((c * cw, r * ch, cw, ch))
    return rects

def _screen_size():
    try:
        import ctypes
        u = ctypes.windll.user32
        return u.GetSystemMetrics(0), u.GetSystemMetrics(1)
    except Exception:
        return 1920, 1080

def arrange_windows(pids, rects):
    try:
        import ctypes, time
        from ctypes import wintypes
        u = ctypes.windll.user32
        time.sleep(1.2)  # 창이 뜰 시간
        want = {pid: rects[i] for i, pid in enumerate(pids) if i < len(rects)}
        placed = {}
        EnumProc = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HWND, wintypes.LPARAM)
        def cb(hwnd, _):
            if not u.IsWindowVisible(hwnd):
                return True
            pid = wintypes.DWORD()
            u.GetWindowThreadProcessId(hwnd, ctypes.byref(pid))
            if pid.value in want and pid.value not in placed:
                x, y, w, h = want[pid.value]
                u.SetWindowPos(hwnd, 0, x, y, w, h, 0x0040)  # SWP_SHOWWINDOW
                placed[pid.value] = True
            return True
        u.EnumWindows(EnumProc(cb), 0)
    except Exception:
        pass

def launch_players(paths):
    exe = find_exe([POTPLAYER_PATH])
    if not exe:
        return {"ok": False, "error": "PotPlayer not found"}
    valid = [p for p in paths if p and os.path.isfile(p)]
    if not valid:
        return {"ok": False, "error": "no valid videos"}
    sw, sh = _screen_size()
    rects = tile_rects(len(valid), sw, sh)
    pids = []
    for p in valid:
        try:
            proc = subprocess.Popen([exe, p])
            pids.append(proc.pid)
        except Exception:
            pass
    if os.name == "nt" and len(pids) > 1:
        import threading
        threading.Thread(target=arrange_windows, args=(pids, rects), daemon=True).start()
    return {"ok": True, "launched": len(pids)}
```
`do_POST` 메서드 추가(`Handler` 안, `do_GET` 아래):
```python
    def do_POST(self):
        u = urlparse(self.path)
        length = int(self.headers.get("Content-Length", 0))
        body = json.loads(self.rfile.read(length) or b"{}")
        if u.path == "/play":
            return self._send(200, launch_players(body.get("paths", [])))
        return self._send(404, {"ok": False, "error": "not found"})
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: PASS (10 passed)

- [ ] **Step 5: Commit**

```bash
git add map/potflow-helper.py map/test_potflow_helper.py
git commit -m "feat(potflow): /play PotPlayer 실행 + 타일 배치 기하"
```

---

## Task 5: /doc 문서 저장·로드 (potflow_data.json)

**Files:**
- Modify: `map/potflow-helper.py`
- Test: `map/test_potflow_helper.py`

**Interfaces:**
- Produces: `load_doc() -> dict|None` — `DATA_FILE` 파싱, 없으면 `None`.
- Produces: `save_doc(doc:dict) -> bool` — `DATA_FILE`에 기록.
- HTTP: `GET /doc` → `load_doc()`(없으면 `null`), `POST /doc {doc}` → `save_doc` 후 `{"ok":true}`.

- [ ] **Step 1: Write the failing test**

```python
def test_save_and_load_doc(tmp_path, monkeypatch):
    f = tmp_path / "d.json"
    monkeypatch.setattr(helper, "DATA_FILE", str(f))
    assert helper.load_doc() is None
    assert helper.save_doc({"canvases": [1, 2]}) is True
    assert helper.load_doc() == {"canvases": [1, 2]}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: FAIL (`load_doc`/`save_doc` not defined)

- [ ] **Step 3: Write minimal implementation**

```python
def load_doc():
    if not os.path.isfile(DATA_FILE):
        return None
    try:
        with open(DATA_FILE, "r", encoding="utf-8") as f:
            return json.load(f)
    except (OSError, ValueError):
        return None

def save_doc(doc):
    try:
        with open(DATA_FILE, "w", encoding="utf-8") as f:
            json.dump(doc, f, ensure_ascii=False)
        return True
    except OSError:
        return False
```
`do_GET`에 `/doc` 추가(정적 서빙 위):
```python
        if u.path == "/doc":
            return self._send(200, load_doc())
```
`do_POST`에 `/doc` 추가(`/play` 아래):
```python
        if u.path == "/doc":
            return self._send(200, {"ok": save_doc(body.get("doc", {}))})
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: PASS (11 passed)

- [ ] **Step 5: `.gitignore` + commit**

`.gitignore`에 다음 줄이 없으면 추가:
```
map/potflow_data.json
map/potflow_thumbs/
```
```bash
git add map/potflow-helper.py map/test_potflow_helper.py .gitignore
git commit -m "feat(potflow): /doc 문서 저장·로드 + gitignore"
```

---

## Task 6: potflow.html 복사·리브랜드·키/저장 분리

**Files:**
- Create: `map/potflow.html` (from `map/map.html`)

**Interfaces:**
- Produces: 전역 `HELPER` 베이스 URL 상수 `const HELPER=""`(동일 오리진 헬퍼 서빙이므로 상대경로). 이후 태스크가 `fetch(HELPER+"/play")` 형태로 사용.
- Produces: 문서 저장이 헬퍼 `/doc`을 사용(아래 치환).

**No new tests** — 이 태스크는 복사+치환 기반이라 검증은 브라우저 수동(Step 6). 리뷰어는 diff와 페이지 로드로 판단.

- [ ] **Step 1: 복사**

```bash
cp map/map.html map/potflow.html
```

- [ ] **Step 2: 리브랜드 치환** (`map/potflow.html` 편집)

- `<title>다이어그램 빌더</title>` → `<title>PotFlow — 동영상 재생 관리</title>`
- 브랜드 앵커 `<a class="brand" href="map.html" title="다이어그램 빌더">` → `href="potflow.html" title="PotFlow"`
- 브랜드 텍스트 `<b>다이어그램 <span class="em">빌더</span></b><small>IT기획파트</small>` → `<b>Pot<span class="em">Flow</span></b><small>동영상 재생 관리</small>`
- localStorage 키 상수 `const LS_MODE='diagboard_mode',LS_DOC='diagboard_doc',LS_IMG='diagboard_imgs';` → `const LS_MODE='potflow_mode',LS_DOC='potflow_doc',LS_IMG='potflow_imgs';`
- 구 키 이관 로직이 있으면(있다면 `scoopboard_*`) 삭제 또는 무시(신규 도구라 이관 불필요).

- [ ] **Step 3: 저장 백엔드를 헬퍼로 전환**

`map.html`은 `const API='api.php'`로 op API를 쓴다. PotFlow는 헬퍼 `/doc`만 쓴다. 다음으로 단순화:
- `const API='api.php';` 아래에 `const HELPER='';` 추가.
- `apiGet()`(문서 로드)를 헬퍼용으로 교체:
```js
async function apiGet(){const r=await fetch(HELPER+"/doc");if(!r.ok)throw new Error("doc");const j=await r.json();return j?{canvases:j.canvases||[],meta:j.meta||{}}:null}
```
- 문서 저장 경로(`op:'replace'`/`upsert`/`meta`/`delete`/`putimg`를 쓰던 `apiPost` 호출들)를 **전량 헬퍼 `/doc` 전체저장으로 단일화**한다. 가장 안전한 방법: 기존 서버 저장 분기를 모두 다음 한 함수로 대체.
```js
let docTimer=null;
function serverPersist(){clearTimeout(docTimer);docTimer=setTimeout(async()=>{try{writeBackActive();await fetch(HELPER+"/doc",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({doc:{canvases,meta:metaObj(),images:IMAGES}})});setSaveState('saved')}catch(e){setSaveState('offline')}},400)}
```
그리고 `markDirty()`/`saveMeta()`/`persistDoc()`/`putImg()`의 `SERVER_OK` 분기 안 `apiPost({op:...})` 호출을 `serverPersist()` 호출로 교체(로컬 모드 분기는 그대로 유지). 이미지도 `/doc`에 함께 저장하므로 별도 `putimg` 불필요.
- `loadImages()`(map: `GET ?images=1`)는 `apiGet`이 이미 `images`를 문서에 담으므로 헬퍼 문서의 `images`를 `IMAGES`에 병합하도록 `boot()`에서 처리(`Object.assign(IMAGES, serverDoc.images||{})`). 별도 `loadImages` 호출은 제거하거나 no-op로.

> 주의: PotFlow는 로컬 개인 도구라 이미지/문서를 한 파일(`potflow_data.json`)에 함께 저장한다. cafe24 128KB 제한은 로컬 헬퍼엔 없음.

- [ ] **Step 4: 헬퍼 오프라인 배지**

`boot()` 초입에 헬퍼 생존확인 추가:
```js
let HELPER_OK=false;
async function pingHelper(){try{const r=await fetch(HELPER+"/ping");const j=await r.json();HELPER_OK=!!j.ok;document.body.classList.toggle('nohelper',!HELPER_OK)}catch(e){HELPER_OK=false;document.body.classList.add('nohelper')}}
```
`boot()`에서 `await pingHelper();` 를 문서 로드 전에 호출. 헬퍼 없으면 로컬 모드로 폴백(`storeMode='local'`).

- [ ] **Step 5: 페이지 로드 확인**

Run: `cd map && python potflow-helper.py &`  그리고 브라우저로 `http://localhost:8770/potflow.html` 접속.
Expected: 기존 다이어그램 캔버스가 뜨고 상단 브랜드가 "PotFlow"로 표시. 노드 추가/이동/연결/줌 동작. 콘솔 치명 오류 없음.

- [ ] **Step 6: Commit**

```bash
git add map/potflow.html
git commit -m "feat(potflow): map.html 복사·리브랜드 + 헬퍼 /doc 저장 전환"
```

---

## Task 7: videoPath 필드 + 썸네일 연동 + 재생 클라이언트

**Files:**
- Modify: `map/potflow.html`

**Interfaces:**
- Consumes: `HELPER`, `makeNode`, `nodeHTML`, `putImg`, `render`, `toast`, `state`, `sel`.
- Produces: `playPaths(paths)`, `playSelected()`, `bindVideoToNode(nodeId, path, name)`, `requestThumb(nodeId, path)`.

- [ ] **Step 1: makeNode에 videoPath 확장**

`makeNode(x,y,title,type,iconId)`는 `{id,x,y,title,...}` 생성. 그 뒤 videoPath는 별도 함수로 세팅(코어 시그니처 유지). 추가:
```js
function bindVideoToNode(id,path,name){const n=state.nodes.find(x=>x.id===id);if(!n)return;n.videoPath=path;if(name&&(!n.title||n.title==='새 단계'))n.title=name.replace(/\.[^.]+$/,'');requestThumb(id,path);markDirty();render()}
```

- [ ] **Step 2: 썸네일 요청**

```js
function requestThumb(id,path){if(!HELPER_OK)return;const imgId='vthumb_'+id;fetch(HELPER+"/thumb?path="+encodeURIComponent(path)).then(r=>{if(!r.ok)throw 0;return r.blob()}).then(b=>{const fr=new FileReader();fr.onload=()=>{putImg(imgId,fr.result);const n=state.nodes.find(x=>x.id===id);if(n){n.thumb={imgId,label:n.title||''};render();markDirty()}};fr.readAsDataURL(b)}).catch(()=>{})}
```
> `putImg`/`thumb.imgId` 렌더 경로는 map.html 기존 것을 그대로 사용(`imgSrc(imgId)`).

- [ ] **Step 3: 재생 클라이언트**

```js
function playPaths(paths){const list=(paths||[]).filter(Boolean);if(!list.length){toast('재생할 동영상이 없습니다');return}if(!HELPER_OK){toast('헬퍼가 꺼져 있어 재생할 수 없습니다');return}fetch(HELPER+"/play",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({paths:list})}).then(r=>r.json()).then(j=>{if(j.ok)toast(j.launched+'개 재생');else toast('재생 실패: '+(j.error||''))}).catch(()=>toast('재생 실패'))}
function playSelected(){const paths=sel.map(id=>{const n=state.nodes.find(x=>x.id===id);return n&&n.videoPath}).filter(Boolean);if(!paths.length){toast('선택한 노드에 동영상 경로가 없습니다');return}playPaths(paths)}
```
> `sel`은 map.html의 선택 배열. 이름이 다르면(예: 단일 선택 변수) 실제 선택 상태 소스에 맞춰 수집.

- [ ] **Step 4: 수동 확인**

브라우저 콘솔에서 `playPaths(["C:\\있는영상.mp4"])` 실행(실경로) → PotPlayer 뜨는지. `bindVideoToNode(선택노드id,"경로")` → 노드 썸네일 채워지는지.
Expected: 재생 실행 + 썸네일 표시(ffmpeg 있을 때).

- [ ] **Step 5: Commit**

```bash
git add map/potflow.html
git commit -m "feat(potflow): videoPath·썸네일 연동·재생 클라이언트"
```

---

## Task 8: 재생 트리거(더블클릭·Space·▶버튼)

**Files:**
- Modify: `map/potflow.html`

**Interfaces:**
- Consumes: `playPaths`, `playSelected`, `state`, `nodeAt`/노드 판정, 기존 `world` dblclick 핸들러(`map.html:987`), 키다운 핸들러(`map.html:1019`부근), 헤더 `.tools`.

- [ ] **Step 1: 헤더에 ▶버튼**

`.tools` 안 `＋ 노드` 버튼 옆에 추가:
```html
<button class="btn gold edit-only" onclick="playSelected()" title="선택 노드 재생 (Space)">▶ 선택 재생</button>
```

- [ ] **Step 2: 노드 더블클릭 = 재생**

`map.html`의 world dblclick 핸들러(캐럿 편집 진입)를 다음으로 교체 — 동영상 노드는 재생, 그 외엔 기존 편집:
```js
world.addEventListener('dblclick',e=>{const nodeEl=e.target.closest('.node');if(nodeEl){const n=state.nodes.find(x=>x.id===nodeEl.dataset.id);if(n&&n.videoPath){e.preventDefault();playPaths([n.videoPath]);return}}
  // ↓ 기존 캐럿 편집 로직 유지
  const r=document.caretRangeFromPoint&&document.caretRangeFromPoint(e.clientX,e.clientY); /* …기존 코드… */});
```
> 노드 DOM의 id 접근자(`dataset.id` 등)는 map.html 실제 마크업(`nodeHTML`)에 맞춰 사용.

- [ ] **Step 3: Space = 선택 재생**

키다운 핸들러에 추가(입력 편집 중이 아닐 때):
```js
if(e.key===' '||e.code==='Space'){const ae=document.activeElement;if(ae&&(ae.isContentEditable||ae.tagName==='INPUT'))return;e.preventDefault();playSelected();return}
```
> 팬 기능이 Space+드래그를 쓰므로, **키다운 단독 Space만** 재생으로. Space+드래그 팬은 pointer 로직이 별도라 충돌 없음(재생은 keydown에서 즉시 실행되지만 드래그 시엔 pointerdown이 팬 시작). 실제 팬과 충돌하면 재생 트리거를 `keyup` + "드래그 안 함" 조건으로 조정.

- [ ] **Step 4: 수동 확인**

- 동영상 경로 지정된 노드 더블클릭 → 재생.
- 노드 2~3개 선택 후 Space 또는 ▶버튼 → 동시 재생 + 타일 배치(PotPlayer 다중 인스턴스 허용 설정 시).
- 경로 없는 노드 더블클릭 → 기존 텍스트 편집 진입.

- [ ] **Step 5: Commit**

```bash
git add map/potflow.html
git commit -m "feat(potflow): 재생 트리거(더블클릭·Space·▶버튼)"
```

---

## Task 9: 오른쪽 파일탐색기 사이드바

**Files:**
- Modify: `map/potflow.html`

**Interfaces:**
- Consumes: `HELPER`, `scan_tree` 응답, `bindVideoToNode`, `makeNode`, `render`, `playPaths`, `worldPt`, `nodeAt`.
- Produces: `openFolder(path)`, `renderExplorer(data)`, `explorerDrop(e)`.

- [ ] **Step 1: 마크업 + 레이아웃**

`#app` 그리드에 오른쪽 컬럼 추가. `.stage` 뒤(닫는 태그 앞)에:
```html
<aside class="rside" id="rside">
  <div class="rside-head">
    <input id="rsPath" class="rs-path" placeholder="폴더 경로 입력 후 Enter" />
    <button class="btn ghost sm" onclick="openFolder(document.getElementById('rsPath').value)">열기</button>
  </div>
  <div class="rs-body" id="rsBody"></div>
</aside>
```
CSS(`#app`를 3열로; 기존 값에 맞춰 조정, **좌측 accent 라인 금지**):
```css
#app{display:grid;grid-template-columns:var(--side,260px) 1fr 300px;}
.rside{border-left:1px solid var(--line);display:flex;flex-direction:column;min-height:0;background:var(--panel);}
.rside-head{display:flex;gap:6px;padding:10px;border-bottom:1px solid var(--line);}
.rs-path{flex:1;min-width:0;}
.rs-body{overflow:auto;padding:6px;}
.rs-row{display:flex;align-items:center;gap:8px;padding:6px 8px;border-radius:6px;cursor:pointer;}
.rs-row:hover{background:var(--hover);}
.rs-row.folder{color:var(--gold);}
.rs-file{cursor:grab;}
body.view .rside{display:none;}
```

- [ ] **Step 2: 폴더 열기 + 렌더**

```js
function openFolder(path){if(!HELPER_OK){toast('헬퍼가 꺼져 있습니다');return}fetch(HELPER+"/tree?path="+encodeURIComponent(path||"")).then(r=>r.json()).then(d=>{if(!d.ok){toast('폴더를 열 수 없습니다');return}document.getElementById('rsPath').value=d.path;renderExplorer(d)})}
function renderExplorer(d){const b=document.getElementById('rsBody');let h='';if(d.parent)h+=`<div class="rs-row folder" onclick="openFolder('${d.parent.replace(/\\/g,'\\\\').replace(/'/g,"\\'")}')">⬆ 상위 폴더</div>`;d.folders.forEach(f=>{const p=f.path.replace(/\\/g,'\\\\').replace(/'/g,"\\'");h+=`<div class="rs-row folder" onclick="openFolder('${p}')">📁 ${f.name}</div>`});d.files.forEach(f=>{const p=f.path.replace(/"/g,'&quot;');h+=`<div class="rs-row rs-file" draggable="true" data-path="${p}" data-name="${f.name.replace(/"/g,'&quot;')}" ondblclick='playPaths([this.dataset.path])'>🎬 ${f.name}</div>`});b.innerHTML=h||'<div style="padding:12px;color:var(--eth)">동영상 없음</div>'}
```
`rsPath` Enter로 열기:
```js
document.getElementById('rsPath').addEventListener('keydown',e=>{if(e.key==='Enter')openFolder(e.target.value)});
```

- [ ] **Step 3: 드래그로 노드 생성/바인딩**

파일 항목 dragstart에 경로 실어 보냄, 캔버스에서 drop 처리:
```js
document.getElementById('rsBody').addEventListener('dragstart',e=>{const row=e.target.closest('.rs-file');if(!row)return;e.dataTransfer.setData('text/potflow-video',JSON.stringify({path:row.dataset.path,name:row.dataset.name}))});
stage.addEventListener('dragover',e=>{if(e.dataTransfer.types.includes('text/potflow-video'))e.preventDefault()});
stage.addEventListener('drop',e=>{const raw=e.dataTransfer.getData('text/potflow-video');if(!raw)return;e.preventDefault();const {path,name}=JSON.parse(raw);const hit=nodeAt(worldPt(e.clientX,e.clientY));if(hit){bindVideoToNode(hit.id,path,name)}else{const p=worldPt(e.clientX,e.clientY);const n=makeNode(p.x-W_NODE/2,p.y-30,name.replace(/\.[^.]+$/,''));render();bindVideoToNode(n.id,path,name)}});
```
> `nodeAt`은 월드좌표 노드 판정(map.html 제공). `W_NODE`는 map 상수.

- [ ] **Step 4: 수동 확인**

- 우측 경로 입력 → 폴더트리·파일목록 표시, 폴더 클릭 탐색·상위 이동.
- 파일 더블클릭 → 재생.
- 파일을 빈 캔버스로 드래그 → 새 노드+썸네일. 기존 노드로 드래그 → 그 노드에 경로·썸네일 지정.
- 보기 모드에서 사이드바 숨김.

- [ ] **Step 5: Commit**

```bash
git add map/potflow.html
git commit -m "feat(potflow): 오른쪽 파일탐색기 사이드바(폴더트리·드래그)"
```

---

## Task 10: 셋업 문서 + 최종 검증

**Files:**
- Create: `map/POTFLOW.md`
- Modify: `map/CLAUDE.md`(도구 표에 PotFlow 1줄 추가), `map/README.md`(있으면 1줄)

- [ ] **Step 1: 셋업 문서**

`map/POTFLOW.md` 작성 — 내용: 실행법(`python potflow-helper.py` → `http://localhost:8770/potflow.html`), CONFIG(PotPlayer/ffmpeg 경로 환경변수 `POTPLAYER_PATH`/`FFMPEG_PATH`), **PotPlayer "다중 인스턴스 허용" 설정 필수**(환경설정 → 재생 → 중복 실행 허용), Windows 전제(타일 배치), 배포 불가침 파일, 트러블슈팅(오프라인 배지=헬퍼/포트 확인).

- [ ] **Step 2: CLAUDE.md 도구 표에 추가**

`map/CLAUDE.md` 상단 도구 표에 행 추가:
```
| **PotFlow** | `potflow.html` + `potflow-helper.py` | 로컬 동영상 노드 재생 관리(PotPlayer). map.html 파생, 로컬 헬퍼 전용 |
```

- [ ] **Step 3: 전체 테스트**

Run: `cd map && python -m pytest test_potflow_helper.py -q`
Expected: PASS (11 passed)

- [ ] **Step 4: 엔드투엔드 수동 검증(체크리스트)**

- [ ] 헬퍼 실행 → 페이지 로드, 오프라인 배지 없음.
- [ ] 우측 탐색기로 실제 폴더 열기 → 영상 목록.
- [ ] 파일 드래그로 노드 생성 + 썸네일 표시.
- [ ] 노드 더블클릭 재생 / 다중선택 Space 동시재생 + 타일.
- [ ] 노드 연결·이동·저장 후 새로고침 → `potflow_data.json`에서 복원.
- [ ] 헬퍼 종료 후 새로고침 → 오프라인 배지 + 편집/저장(localStorage) 계속 동작.

- [ ] **Step 5: Commit**

```bash
git add map/POTFLOW.md map/CLAUDE.md map/README.md
git commit -m "docs(potflow): 셋업 문서 + 도구 인벤토리 등록"
```

---

## Self-Review

- **Spec coverage:** 헬퍼(§2,§7)=Task1-5 · 파일/데이터모델(§3,§4)=Task5,7 · 오른쪽 탐색기(§5)=Task9 · 재생/타일(§6)=Task4,7,8 · 디자인/degradation(§7,§8)=Task6,8,9 · 셋업/배포(§9)=Task5(gitignore),Task10. 범위밖(§10)은 미구현 유지. ✅
- **Placeholder scan:** 각 코드 스텝에 실제 코드 포함. "기존 코드 유지" 표기는 map.html의 확인된 앵커(라인 987/1019) 참조로 구체화. ✅
- **Type consistency:** `bindVideoToNode(id,path,name)`·`playPaths(paths)`·`playSelected()`·`requestThumb(id,path)`·`openFolder(path)`·`renderExplorer(d)` 이름을 Task 7/8/9에서 일관 사용. 헬퍼 `scan_tree`/`tile_rects`/`get_thumb`/`launch_players`/`load_doc`/`save_doc` 시그니처 태스크 간 일치. ✅
- **주의(구현 시 확인 필요):** map.html 실제 선택 상태 변수명(`sel` 가정)·노드 DOM id 접근자·`W_NODE`·`nodeAt` 존재는 코드에서 확인 후 사용. Space 팬 충돌 시 Task8 Step3 대안 적용.
